# Pause 2026-08-09 (session C) — feature_026.omt_q_interrogative_first_ops

> Result: **paused at Programming-phase entry (pre-TDD-testlist), resume by reading `.sandbox/pause_2026-08-09_c.md` then invoke `omt_tdd{op:testlist, behaviors:"<JSON array>", feature:"feature_026.omt_q_interrogative_first_ops"}`.**

## Active task

- **Task id / name:** `feature_026.omt_q_interrogative_first_ops` — major_feature (TDD auto-on).
- **In WORK.md?** Yes — line 27 `[~] feature_026.omt_q_interrogative_first_ops`. Currently still carries the STALE "PAUSED 2026-08-09 mid-Design-phase pre-design-doc" copy from pause_b; Step 5 of THIS pause will update it to reflect the Programming-phase-entry + new pause pointer.
- **OMT phase (current):** Programming (declared this session via `omt_phase`).
- **Phase progression this session:** Analysis (done prior session) → Design (design_001 + operation_spec WRITTEN this session + `omt_complete Design→Programming` advanced) → Programming (declared; NO TDD cycles run yet — paused before the testlist call).

## What this feature is (carry the model in one paragraph)

Ship a read-only `omt_q` TS plugin at `.opencode/plugins/omt_q.ts` with three ops: `op:state{feature?, session?, as_of?}` (U1 + U6/U7/U8/U9/U10/U13 folds + U9 `live_smoke_count` named field), `op:plan{path, tool?, session?, as_of?}` (U2 pre-flight via `runBeforeGatesDry` + U11 receipt-detail fold), `op:drift{as_of?}` (U3 AKB/source drift + count-drift direction-b only). Every response envelope carries `as_of_commit:"<HEAD-sha>"` (parsed live via `git rev-parse HEAD`). Single mechanical touch: `gate_driver.runBeforeGates` gains a sibling `runBeforeGatesDry(ctx)` (synthetic-`GateCtx` + catch-and-capture instead of throw; real edits still throw on the existing path — v1 lock preserved). A `kind:"q"` ledger record per call (v1.3 schema + v1.4 `op_set`/`fold_used`/`latency_ms` + v1.5 `as_of:"HEAD"`). Parent design source-of-truth: `.projects/meta/meta_harness_2/PROJECT.md` (v1.5, 513 lines).

## Completed so far (this session — pause C — adds to the pause_b baseline)

1. **Pre-flight IR fingerprint re-verification** at resume — all pass:
   - ledgers: `wc -l .meta/.omt/ledger*.jsonl` = 29 / 415 / 628 (= 1072) — within churn tolerance (the hot ledger grew +6 records from the v1.5 baseline of 23, all from this + the prior pause session's omt_phase/skip records);
   - kinds cross-ledger (grouped): think_consult 517 (97 hot + 413 archive) / phase 192 (104+83+8 hot) / skip 164 (123+24+12+1+5 hot) / complete 101 (45+53+4 hot) / tdd 80 (39+37+4 hot) / tdd_testlist 12 (7+5 hot) — matches v1.5-corrected totals within churn;
   - `jq '.gates | length' .meta/.omt/harness.ir.json` = 9 (7 before + 2 after) — matches v1.5 + verified vs `FALLBACK_GATES`(7) + `FALLBACK_AFTER_GATES`(2);
   - git tree: `M WORK.md` (the stale pause_b marker) + 3 new untracked dirs/files (the feature_026 requirements/analysis dirs from the prior pause, the new pause_b doc, and now the new design_001/operation_spec files written this session) — clean otherwise; last commit `8b384b1 [WIP] Project META HARNESS v2`.
2. **Design doc `design_001_omt_q_first_ops.md` written** (~190 lines) under `.meta/software_development_process/4.design/features/feature_026.omt_q_interrogative_first_ops/`. Sections: Summary, Problem Analysis (Current behavior + substrate inventory + live IR fingerprint correction from Analysis), Components/Files Affected, Static Structure (omt_q.ts after + runBeforeGates refactor after, with behaviour-preservation invariants), Functional Flow (per-op), Operation Specifications (pointer to sibling), Breaking-change risk, Backwards compatibility, Testing strategy (12 golden test specs incl. U1/U2/U3 + U6–U11 + U13 + envelope + behaviour-preserving probe), Open questions/risks, Links. Carried all 9 pre-locked substrate-level decisions from pause_b §56-67 forward verbatim + the line-marker edge case #3 fold spec.
3. **Operation spec `operation_spec_001_omt_q_ops.md` written** (~180 lines) in the same dir. Per-op Pre/Post/Exc contracts for `omt_q`, `omt_state`, `omt_plan`, `omt_drift`, the synthetic-env builder (`buildCtxFromInputs`), and `runBeforeGatesDry(ctx)` — including the `GateDecision` type, the try/catch capture loop, and the behaviour-preservation matrix (8 existing APIs/pins + MVC-pin-impact note confirming TS-only blast radius).
4. **`omt_complete Design → Programming` advanced** — both design artifacts verified by the §12 PHASE_EXIT_REQUIREMENTS matcher.
5. **`omt_phase Programming` declared** (major_feature, tdd=true, scope=implement omt_q.ts + runBeforeGatesDry + golden suite via RED→GREEN→REFACTOR). TDD Mode ACTIVE, cycles 0.
6. **`omt_tdd{op:testlist}` wrapper bug investigated** — root cause found: the `tdd_hats.ts` wrapper itself is fine (it passes `behaviors` through as a CLI string); `scripts/omt/tdd/cli.py:65` runs `behaviors = json.loads(args.behaviors) if args.behaviors else []`. The "Expecting value: line 1 column 1" only fires if the AGENT passes prose (e.g. `behaviors:"foo"`) instead of a JSON array string (e.g. `behaviors:'["U1 snapshot","U2 predict"]'`). Mitigation: ALWAYS pass a JSON array string to `omt_tdd{op:testlist, behaviors, feature}`. No code fix needed.

## NOT completed so far (the immediate next steps — STRICT ORDER)

Resume by reading this file, then run `omt_tdd{op:testlist, behaviors:"<JSON array>", feature:"feature_026.omt_q_interrogative_first_ops"}` with the golden behaviors below.

### Step 1 — TDD testlist (the immediately-blocking next call)

```text
omt_tdd{
  op: "testlist",
  behaviors: '[
    "U1 op:state resume 5-read snapshot",
    "U2 op:plan predicts real before-chain on gate_driver.ts incl g.think self-trigger",
    "U3 op:drift reports count_drift direction-b only (KB>skeleton IS drift; KB<skeleton NOT drift)",
    "U6 op:state stranded_red per-test_node latest-red with no later green",
    "U7 op:state closed_via_skip + cross-feature FP guard (skip for feature_Y does NOT flip feature_X)",
    "U8 op:state decree_health slug_variants + empty_slug + invalid_phase + near-collision guard feature_004 != feature_04",
    "U9 op:state skip_reason_tally top-3 + live_smoke_count named SEPARATE field",
    "U10 op:state known_suite_failures = 6 IDs parsed from state.py:132 frozenset (not hardcoded)",
    "U11 op:plan receipt_detail.stale + refresh_tests + refresh_cmd when path in @var.harness_paths",
    "U13 op:state recent_consults within 8h + consult_needed[] = files-not-recently-consulted",
    "v1.5 envelope as_of_commit == HEAD-sha in every golden; two consecutive calls against unchanged commit = byte-identical",
    "behaviour-preserving runBeforeGatesDry does not break real runBeforeGates throw-on-block (bun probe pattern)"
  ]',
  feature: "feature_026.omt_q_interrogative_first_ops"
}
```

### Step 2 — RED (two-hats: tests/ only)

Create `tests/scripts/omt/test_omt_q.py` with the 12 golden test methods listed in `design_001_omt_q_first_ops.md` §Testing strategy (1)-(12) + a `tests/features/feature_26.omt_q_interrogative_first_ops/test_omt_q_golden_smoke.py` SENTINEL re-export (to satisfy the `omt_complete{Programming→Testing}` pattern matcher `tests/features/<feature>/test_*.py`).

Then declare `omt_tdd{op:red, test_node:"tests/scripts/omt/test_omt_q.py::<ClassName>::<test_method>", target_src:".opencode/plugins/omt_q.ts", feature:"feature_026.omt_q_interrogative_first_ops"}` at EACH failing test node (per GOTCHA_TDD_NODE_GRANULARITY: declare red/green/refactor at the SAME test_node).

### Step 3 — GREEN (two-hats: src/ only)

- Implement `runBeforeGatesDry(ctx)` (sibling export in `.opencode/lib/enforcer/gate_driver.ts` — additive, NOT touching `runBeforeGates` body or `IMPLS`).
- Implement `.opencode/plugins/omt_q.ts` (factory mirroring `omt_nav.ts`; `createQTools()` builds `omt_state`, `omt_plan`, `omt_drift` impls + the registered `omt_q` dispatcher; synthetic-env builder; per-op fold helpers; headSha(); KNOWN_SUITE_FAILURES regex extractor at `scripts/omt/tdd/state.py:132`).
- `omt_tdd{op:green, test_node:"<exact red node>", feature:"feature_026.omt_q_interrogative_first_ops"}` at each red → green.

**Notes:**
- Editing `omt_q.ts` triggers `g.receipt` (the file is in `@var.harness_paths`). Each multi-edit round on it needs ONE fresh `uv run pytest tests/scripts/omt/test_omt_harness_e2e.py -q` run BEFORE the second edit to the same file (GOTCHA_RECEIPT_ROUND_ROBIN — the e2e test file itself is receipt-EXEMPT).
- Editing `gate_driver.ts` triggers `g.receipt` AND `g.think` (the file contains the literal `"TA:"`). The build-sequence bootstrap step `omt_q{op:plan, path:".opencode/lib/enforcer/gate_driver.ts"}` — once `omt_q` is live — is the v1.3 thesis demonstration (it predicts `g.think` on itself).

### Step 4 — REFACTOR

Extract fold primitives (`foldStrandedReds`, `foldClosedViaSkip`, `foldDecreeHealth`, `foldSkipReasonTally`, `foldKnownSuiteFailures`, `foldRecentConsults`, `foldRiskyThoughts`, `buildCtxFromInputs`) into local helpers; keep `omt_q.ts` < 600 lines.

### Step 5 — `omt_tdd{op:done, feature:"feature_026.omt_q_interrogative_first_ops"}` once all reds are green and refactor passes.

### Step 6 — Regression sweep + exit phases

- `uv run pytest` — full 1196+ baseline; `KNOWN_SUITE_FAILURES` allowlist of 6 IDs unchanged.
- Write implementation notes under `5.implementation/features/feature_026.omt_q_interrogative_first_ops/`.
- `omt_complete Programming → Testing`.
- `omt_phase Testing` (feature_026).
- Write test report `6.testing/features/feature_026.omt_q_interrogative_first_ops/test_report.md`.
- `omt_complete Testing → Done`.
- Update WORK.md (rotate feature_026 to `[x] DONE` per CONV_WORK_ROTATE) + `.projects/meta/meta_harness_2/CURRENT_STATE.md` (append iter 13 session-log entry).

## Blockers

- _(none)_ — the `omt_tdd{op:testlist}` wrapper "bug" is NOT a bug in the wrapper; it is the documented contract that `behaviors` MUST be a JSON array string. The pause_b note flagged it as a potential blocker; this session confirmed it's not. Mitigation: invoke `omt_tdd` with `behaviors:'["…","…"]'` (JSON array string), NEVER prose.

## Substrate-level design decisions locked (carried forward from pause_b §56-67 — UNCHANGED this session)

1. **Before-gate count = 7, not 9.** Live `harness.ir.json` today has `g.nav, g.protect, g.receipt, g.tests, g.phase, g.think, g.kb` = 7 before-gates. `op:plan` filters dynamically via `gates.filter(on==="before")` so the count comes from the IR.
2. **Plugin mirrors `omt_nav.ts` structure.** `import { initOmtShared, repoRoot, ... } from "../lib/omt_shared"`; `tool()` built inside `createQTools()` AFTER `initOmtShared`; default export `({directory, worktree}) => { initOmtShared(worktree ?? directory); const {omt_q} = createQTools(); return {tool:{omt_q}} }`.
3. **`gate_driver.runBeforeGates` refactor shape — FINAL (decided this session in design_001 §Static Structure).** Add a SIBLING `runBeforeGatesDry(ctx: GateCtx): Promise<GateDecision[]>` export; the existing `runBeforeGates(env, session, input, output, rawEditPath): Promise<void>` body is byte-identical and throws `OmtBlock` on the first blocking gate. The dryRun variant catches `OmtBlock` per-gate, captures `{gate_id, blocked, msg, skip_ok}`, and continues (so all 7 before-gates get a decision row, not just the first blocker).
4. **`op:state` field set (locked v1.5 surface):** `phase`, `tdd_position` {state, test_node, target_src[], verified, exit_code}, `stranded_red`, `closed_via_skip`, `decree_health`, `skip_reason_tally` + `live_smoke_count` (named), `known_suite_failures` + `known_suite_failures_parse_failed`, `recent_consults` + `consult_needed`, `last_activity_ts`, `risky_thoughts[]` w/ `line_drift`.
5. **`op:plan` field set:** `predicted_chain[]` (7 before-gate decisions via `runBeforeGatesDry`), `receipt_detail` (U11 — when `path ∈ @var.harness_paths`), `first_blocker` (convenience).
6. **`op:drift` field set:** `drift_records[]` (GONE/MOVED/UNTRACKED classification from KB IR vs source re-scan), `count_drift` {kb, skeleton, **direction_b_only: true**}.
7. **`as_of_commit` envelope.** Every `omt_q` response wraps in `envelope: {as_of_commit:"<HEAD-sha>", op, ...}` — Phase-A stub parses `git rev-parse HEAD` live per call. Phase-B U18 (`as_of:"<commit>"` parameter + `git show` reconstruction) is OUT of scope.
8. **`kind:"q"` ledger record.** Per `omt_q` invocation: `op`, `feature?`, `session`, `ts` (v1.3 schema + `op_set:[<folds used>]`, `fold_used:"<U#>"`, `latency_ms:<int>` (v1.4 additions) + `as_of:"HEAD"` (v1.5 stub)).
9. **Golden tests under `tests/scripts/omt/test_omt_q.py`** (harness-level pin-test convention — alongside `test_omt_enforcer_guard_source_pins.py` + `test_omt_harness_e2e.py`). A sentinel re-export under `tests/features/feature_026.omt_q_interrogative_first_ops/test_omt_q_golden_smoke.py` satisfies the `omt_complete{Programming→Testing}` pattern matcher `tests/features/<feature>/test_*.py` (the harness pin tests are NOT under the per-feature dir).

## Test placement note (decided — carried from pause_b)

`tests/scripts/omt/test_omt_q.py` is the canonical golden-suite location. The sentinel under `tests/features/feature_026.omt_q_interrogative_first_ops/test_omt_q_golden_smoke.py` is a thin re-import wrapper (NOT the canonical tests). Investigate the exact sentinel shape at Programming→Testing transition (likely `from tests.scripts.omt.test_omt_q import *` + `pytest.main()` re-run, OR a single `pytest.importorskip` smoke).

## Receipt round-robin note (carried from pause_b)

`.opencode/plugins/omt_` and `.opencode/lib/enforcer/` are both in `@var.harness_paths` → editing `omt_q.ts` OR `gate_driver.ts` triggers `g.receipt`. Each multi-edit round on EITHER file needs a fresh `uv run pytest tests/scripts/omt/test_omt_harness_e2e.py -q` run BEFORE the second edit to the same file. The e2e test file itself is receipt-EXEMPT.

## Artifacts to read first (in order, for resumption)

1. **WORK.md** (the updated `[~] feature_026` row — pointer will point to `.sandbox/pause_2026-08-09_c.md` after Step 5).
2. **This file** — `.sandbox/pause_2026-08-09_c.md` (resumption context).
3. **Parent design source-of truth:** `.projects/meta/meta_harness_2/PROJECT.md` (v1.5, 513 lines — §Architecture Phase-A surface, §Edge cases #1–#11, §Risks #1–#8, §Tasks Phase-A build, §Ready to start next session, §Success criteria).
4. **Analysis doc:** `.meta/software_development_process/3.analysis/features/feature_026.omt_q_interrogative_first_ops/analysis_001_substrate_rederivation_costs.md` (10 re-derivation costs + substrate inventory + live IR fingerprint correction).
5. **Design doc:** `.meta/software_development_process/4.design/features/feature_026.omt_q_interrogative_first_ops/design_001_omt_q_first_ops.md` (~190 lines; the Static Structure + Functional Flow + Testing strategy are the build instructions).
6. **Operation spec:** `.meta/software_development_process/4.design/features/feature_026.omt_q_interrogative_first_ops/operation_spec_001_omt_q_ops.md` (~180 lines; per-op Pre/Post/Exc contracts incl. the synthetic-env builder + `runBeforeGatesDry` invariant + behaviour-preservation matrix).
7. **Mirror template:** `.opencode/plugins/omt_nav.ts` (the exact structure `omt_q.ts` mirrors — `createNavTools()` → `createQTools()`, the four-impl-one-registered-tool pattern, the default export with `initOmtShared(worktree ?? directory)`).
8. **Refactor target:** `.opencode/lib/enforcer/gate_driver.ts` — `runBeforeGates` at L241-270 (untouched), `GateCtx` at L39-48, `IMPLS` at L175-207 (untouched), `FALLBACK_GATES` at L227-235 (untouched). The SIBLING `runBeforeGatesDry(ctx)` will be ADDED as a new export; do NOT modify `runBeforeGates` body.
9. **Behaviour-preservation pins:** `tests/scripts/omt/test_omt_enforcer_guard_source_pins.py` — `TestGateDriverIrPin` + `TestFallbackGatesIrSyncPin` + `TestGateDriverProtectIrMissing` + `TestGateDriverIrRenderedMsg` all stay green through the additive refactor (they assert on `IMPLS` registry + `FALLBACK_GATES` literal + `runBeforeGates` body + e2e bun-probe respectively — none of which the refactor touches).
10. **Substrate helpers:** `.opencode/lib/omt_shared.ts` — `loadIr`, `readLedger`, `appendLedger`, `readThoughtsIndex`, `loadKbIr`, `omtHarnessE2eStatus`, `gateMsg`, `THOUGHT_PATTERN`, `UNLOCK_WINDOW_MS`, `OMT_HARNESS_E2E_TEST`, `OMT_HARNESS_E2E_COMMAND`, `relOf`, `toAbs`. `.opencode/lib/enforcer/session_state.ts` — `getActiveFeaturePhase`, `getActiveUnlock`, `hasNavUnlock`, `createSessionState`, `OmtBlock`, `EnforcerEnv`.
11. **TDD engine:** `.opencode/lib/enforcer/tdd_hats.ts` (line 36-37 documents the contract; line 47-55 shows the wrapper just forwards `behaviors` as a CLI string); `scripts/omt/tdd/cli.py` line 65 `behaviors = json.loads(args.behaviors)`. MITIGATION: pass `behaviors:'["…","…"]'` (JSON array string), never prose.

### Live IR fingerprint (re-verify at resume — drift = a stop signal)

```bash
wc -l .meta/.omt/ledger.jsonl .meta/.omt/ledger-202608.jsonl .meta/.omt/ledger-202607.jsonl
# EXPECT: ~29-35 / 415 / 628 (hot churn from this session's omt_phase records; archive stable)
rg -o '"kind": ?"[^"]+"' .meta/.omt/ledger*.jsonl | sort | uniq -c | sort -rn
# EXPECT: think_consult ~517 / phase ~193 / skip ~164 / complete ~101 / tdd ~80 / tdd_testlist ~12; + new kind:"q" records ONCE omt_q.ts ships
jq '.gates | length' .meta/.omt/harness.ir.json    # EXPECT: 9 (7 before + 2 after)
git log --oneline -1                                # EXPECT: 8b384b1 (or later if user committed)
```

## Steps done by the pause workflow itself (track below)

- [x] Step 1 — Read WORK.md, identify in-progress task (feature_026 at line 27 — persisted stale pause_b copy; will be updated in Step 5).
- [x] Step 2 — List artifacts + remaining work (design docs written; TDD testlist + RED suite + GREEN src + REFACTOR + regression + phase exits + WORK.md close all pending — listed above in strict order).
- [x] Step 3 — Write `.sandbox/pause_2026-08-09_c.md` (this file).
- [x] Step 4 — User confirmation (received: "Yes, accurate — update WORK.md").
- [x] Step 5 — Update WORK.md (rewrote line 27 `[~] feature_026.omt_q_interrogative_first_ops` from the STALE pause_b "mid-Design-phase pre-design-doc" copy to the current "PAUSED 2026-08-09 (session C) mid-Programming-phase pre-TDD-testlist: design_001 + operation_spec WRITTEN; omt_complete Design→Programming advanced; omt_phase Programming declared (TDD auto-on, 0 cycles); omt_tdd{op:testlist} wrapper-bug investigated — not a bug, pass behaviors as JSON array string; next = omt_tdd{op:testlist, behaviors, feature:feature_026}. Resume by reading .sandbox/pause_2026-08-09_c.md"). WORK.md edit gated behind `omt_skip{scope:"all", reason:"pause_dev_for_resume_later workflow Step 5 …"}` per AGENTS.md protected-surface rule (reversible + idempotent per workflow Rule 5: the line 27 `[~] feature_026` row is uniquely identifiable — re-running this pause on the same state rewrites the same row, not duplicates).
- [x] Step 6 — WORK.md updated; session safe to close. Next resume reads: WORK.md `feature_026` row → this pause doc → parent PROJECT.md §Architecture + §Ready-to-start → design_001 + operation_spec → analysis_001 → mirror `omt_nav.ts` + sibling `gate_driver.ts` L241-270 (the refactor target — ADD `runBeforeGatesDry` without touching `runBeforeGates` body).
