# PAUSE — feature_036.studio_v3_graph (petri_net_studio roadmap #5)

> Date: 2026-08-29 · Session: resume project @.projects/meta/petri_net_studio/PROJECT.md → scaffolded feature_036 → Analysis + Design done → **Programming gate-blocked on operation spec**.
> Resume: read `WORK.md` `[~]` row → this file → PROJECT.md Quick Start → `analysis_001_graph_explorer.md` → `design_001_studio_v3_graph.md` → write `operation_spec_001_*.md` → `omt_phase` Programming.

---

## Active task

- **feature_036.studio_v3_graph** — roadmap feature #5 (project v2 completion): reachability-graph explorer (auto-layout), firing-sequence animation, liveness/SCC views, conformance-suite runner wiring, example gallery. Type: major_feature. Linked project: petri_net_studio (active). WORK.md task row NOT yet added (bookkeeping deferred to feature completion per prior iterations).

## What was completed this session

1. **Scaffold** — `uv run scripts/omt/new_feature.py "studio v3 graph" --type major_feature --project petri_net_studio` → auto-numbered **feature_036** (D9); FEATURE.md/PLAN.md stubs created; project manifest linked (`git status` shows `M .projects/meta/META.md` + untracked feature dirs).
2. **Analysis phase** — `omt_phase{tt:major_feature, ph:Analysis}` declared (21:55 ledger entry). Analysis doc written: `.meta/software_development_process/3.analysis/features/feature_036.studio_v3_graph/analysis_001_graph_explorer.md`.
   - **Headline finding C1:** the engine layer roadmap #5 describes is ALREADY SHIPPED by feature_035 — `analysis.ts` has `reachabilityGraph`/`firingSequenceTo`/`transitionLiveness`/`isLive`/`stronglyConnectedComponents`/`markingKey` (9-vector conformance suite green). Feature #5 is a **UI/UX-layer feature**; **no engine changes except one additive export** (`markingFromKey` — currently private).
   - Findings C1–C10 recorded (engine untouched · export markingFromKey · minimal store `graphVisible` · sibling view · new pure projection · gallery load-path reuse · independence allowlist extension · conformance disposition "runner exists, add regeneration script" · elkjs layout (D2) · TDD A11 manual red→green).
3. **Design phase** — `omt_phase{tt:major_feature, ph:Design, design_doc:…design_001…}` declared (artifact check ✅). Design doc written: `.meta/software_development_process/4.design/features/feature_036.studio_v3_graph/design_001_studio_v3_graph.md`.
   - Pins: `graphProjection.ts` pure projection (node id=markingKey, edge id `src::t::dst`) · `GraphExplorer.tsx` (elkjs layout, SCC colors, deadlock highlight, truncation banner D10, animation strip Play/Step/Reset via `fireMarking` fold, M0-rooted, never touches store marking) · store `graphVisible`/`toggleGraph` additive · `scripts/conformance.mjs` (`npm run conformance`: uv regenerate → `git status --porcelain shared/petri-net/conformance/` empty → vitest conformance) · gallery 8 entries (3 canonical examples + 5 unique fixture nets from vector corpus; `two_way_cycle_truncated` excluded) · independence allowlist regex `examples/|conformance/` · 6 programming cycles + evidence protocol.

## Blocked — the immediate next step

**Programming declaration was REJECTED by the gate:**

```
⛔ OMT++ gate: cannot leave Design phase — missing required artifacts (guide §12):
  • Operation specifications (operation_spec_*.md or operations.md)
```

- §12 requires an operation spec for major_feature before Programming. feature_034 precedent exists: `.meta/.../4.design/features/feature_034.studio_v1_editor/operation_spec_001_studio_v1_editor_ops.md` (op-table format: signature · pre · post/effects · errors). feature_035 has NO operation_spec (apparently not gated then — do not assume it's exempt now).
- **Next step: write `operation_spec_001_studio_v3_graph.md`** in `.meta/software_development_process/4.design/features/feature_036.studio_v3_graph/` covering the NEW public operations introduced by design_001:
  - `engine/analysis.ts`: `markingFromKey(key: string): number[]` (new export; empty-marking gotcha → `[]`).
  - `src/ui/graphProjection.ts`: `projectGraph(graph, sccs, deadlocks, positions, placeOrder) → {nodes, edges}` (node id/label/sccIndex/deadlock/initial; edge id `src::t::dst`; determinism).
  - `src/ui/animation.ts` (helpers the design implies): `firingSequenceTo`-driven `markingAt(net, m0, seq, step) → number[]` (fold fireMarking; clamp; null-seq → unreachable), maybe `sequenceSteps`.
  - `state/store.ts`: `graphVisible: boolean` (default false), `toggleGraph(): void` (NOT edit-locked; view-only over M0; setMode untouched).
  - `examples.ts`: `GALLERY_ENTRIES: GalleryEntry[]` (id/text/description; 8 entries; conformance-net extraction via JSON.parse(...).net re-stringified).
  - `scripts/conformance.mjs`: side-effect contract (uv regenerate → porcelain-empty assert → vitest conformance; exit 0 only all pass).
  - UI ops (GraphExplorer/Gallery/App toggles) if the op-table format covers components (feature_034 spec had a store/UI section — mirror it).
- Then re-run `omt_phase{tt:major_feature, ph:Programming, tdd:true, feature:"feature_036.studio_v3_graph"}` — declare the Vitest/omt_tdd mismatch (A11 precedent; manual red→green, evidence in test report).

## Files a future session must read first

1. `WORK.md` (this `[~]` row — added on pause; pointer here).
2. This file.
3. `.projects/meta/petri_net_studio/PROJECT.md` — Quick Start ("Next: scaffold feature #5 on user go" → now scaffolded) + iteration log (iter-8 entry is the predecessor).
4. `.meta/software_development_process/3.analysis/features/feature_036.studio_v3_graph/analysis_001_graph_explorer.md`
5. `.meta/software_development_process/4.design/features/feature_036.studio_v3_graph/design_001_studio_v3_graph.md`
6. Precedent docs: `feature_035` design_001 (B11/A11 TDD protocol) + `feature_034` operation_spec_001 (op-table format) + `feature_035` sentinel `tests/features/feature_035.studio_v2_analysis/test_studio_v2_analysis_sentinel.py` (env-skip + structural floor pattern for the feature_036 sentinel).
7. Code tree: `tools/petri-net-studio/src/{engine/analysis.ts,state/store.ts,ui/AnalysisPanel.tsx,ui/App.tsx,ui/flow.ts,examples.ts}` + `scripts/{check-independence.mjs,generate-vectors.py}` + `tests/engine/conformance.test.ts` + `tests/state/store.test.ts` (all read this session; TA thoughts on analysis.ts:31+481, store.ts:168, App.tsx:294, document.ts:96, check-independence.mjs:76, independence.test.ts:22).

## Blocker / notes

- **No blockers** other than the gate (operation spec — deterministic fix above).
- Key risk recorded (C1): do NOT touch `engine/*.ts` beyond the `markingFromKey` export.
- npm install of `elkjs` (design C9) needs network — prior iterations confirmed OK.
- WORK.md budget: 4659 B now (limit 5120). Adding the feature_036 `[~]` row + this pointer is ~150 B — fine; prefer WORK_ARCHIVE rotation if anything grows at bookkeeping.
- Phases already declared this session: Analysis (21:55) + Design. Do NOT re-declare; resume at Programming (after the op spec).

## Post-pause idempotence

Pause doc `pause_2026-08-29c.md` is the FIRST resume artifact for this feature. WORK.md updated to point here (step 5/6 of the workflow). No duplicate artifacts created; the feature dirs are untracked (new) and must NOT be git-committed (git commit is denied by harness) — git status at resume should show only the expected M/META.md + ?? feature dirs.

## Session-closed record (workflow step 6)

- ✅ WORK.md updated: `feature_036.studio_v3_graph` task row added and marked `[~]` (paused) with a one-line pointer to this file.
- ✅ Resume artifacts verified: FEATURE.md/PLAN.md stubs (scaffold) · analysis_001 · design_001 · this pause doc. All paths listed in §"Files a future session must read first".
- ✅ User confirmed the resumption document is accurate (2026-08-29).
- Session is safe to close. Resume entry point: `WORK.md` `[~]` row → `pause_2026-08-29c.md` → write `operation_spec_001_studio_v3_graph.md` → `omt_phase{tt:major_feature, ph:Programming, tdd:true}`.