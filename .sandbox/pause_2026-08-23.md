# PAUSE 2026-08-23 — petri_net_studio execution, mid-feature-#3

> Resumption document (pause_dev_for_resume_later workflow). Reversible/idempotent: re-running the pause on this state reproduces this file.
> **Status: session safe to close.** WORK.md updated (feature_034 row = `[~]` paused → pointer here).

---

## Active task

- **feature_034.studio_v1_editor** — petri_net_studio roadmap feature #3 (major_feature, scope LOCKED v1.1).
  State: **scaffolded only** (FEATURE.md + plan/PLAN.md stubs created by `new_feature.py`, linked origin: scaffold).
  No design doc, no `omt_phase`, no code, no npm scaffold. Nothing else in flight.

## Completed so far (this session, all green & omt_complete'd)

- **feature_032.petri_net_format** (roadmap #1, DONE): `shared/petri-net/` — FORMAT.md spec v1 (validation V1–V6, canonical serialization §8, versioning §9), JSON Schema Draft 2020-12, 3 canonical examples, `shared/META.md`. `shared` added to `@var root_allowlist` (META_HARNESS.omt, rebuilt).
- **feature_033.petri_net_io** (roadmap #2, DONE): `src/agentx/model/petri_net/io.py` (net_to_json/net_from_json/document_from_json; L1+V1–V6 typed-error validation; canonical bytes; layout verbatim; M0-only; stdlib-only) + `tests/model/petri_net/test_io.py` (59 tests). Full suite **1639 passed, 0 regressions**. Verified again at pause: petri_net subset 158 green.
- Project manifest: `petri_net_studio` = active; PROJECT.md at iter 4; WORK_ARCHIVE.md received feature_030/031 rotations (budget).

## Immediate next step (resume here)

1. Read `WORK.md` (task row `[~]` points here) → this file → `.projects/meta/petri_net_studio/PROJECT.md` (Quick Start) and its `CURRENT_STATE.md`.
2. Write the **design doc** for feature_034 at `.meta/software_development_process/4.design/features/feature_034.studio_v1_editor/design_001_studio_v1_editor.md` (major_feature §12 requirement), then `omt_phase{task_type:"major_feature", phase:"Design", design_doc:<path>, feature:"feature_034.studio_v1_editor"}`.
3. Scaffold the app: Vite + React + TS in `tools/petri-net-studio/`; deps: `@xyflow/react` (React Flow), `zustand`, `vitest` + jsdom (+ `@testing-library/react`). **Network confirmed working** 2026-08-23 (npm ping OK; npm 11.11.0, node v24.14.1).
4. Port order (engine first, UI second): `src/engine/model.ts` (port of `model.py` — add-only API, sorted canonical orders, pure `fire_marking`, typed errors) → `src/engine/io.ts` (port of `io.py` incl. L1+V1–V6, canonical §8 bytes, layout verbatim) → React Flow editor (palette add place/transition, drag, edge=arcs with weight, token/weight editing, click-to-fire + enabled highlighting, import/export JSON, load shared examples) → `vite build` static output → independence check (no imports outside `tools/petri-net-studio/` except reading `shared/petri-net/` data files).

## Open decision (was under investigation at pause)

- **TDD driver is pytest-shaped**: `omt_tdd` takes pytest node ids; feature_034's tests are Vitest. `omt_nav` has no guidance for non-pytest TDD. Likely resolution: keep red→green discipline manually under Vitest (engine behaviors first, watch them fail, then implement), record the evidence in feature_034 FEATURE.md; if `omt_tdd` auto-activates at Programming and blocks, use `omt_skip{scope:"tests"}` or declare the mismatch in the phase scope (feature's test runner is `npx vitest`, not pytest).

## Port reference knowledge (read before writing TS)

- **`src/agentx/model/petri_net/io.py` + `tests/model/petri_net/test_io.py`** = the executable reference matrix for the TS io port (59 tests: error precedence syntax→format→version→L1→L2, rule-id messages V1–V4, integral-float normalization, bool rejection, layout verbatim/extensions/V6, canonical byte rules).
- **`shared/petri-net/examples/*.json`** are golden canonical bytes: TS `serialize(load(example))` must reproduce the file bytes exactly (cross-impl test without Python: `fs.readFile` + compare).
- Canonical §8 equivalents: `JSON.stringify(doc, null, 2) + "\n"` ≡ Python `json.dumps(doc, indent=2, ensure_ascii=False) + "\n"`; pinned member order (format, version, places, transitions, arcs, layout / name, tokens / name / source, target, weight / x, y); arrays sorted code-point (JS `sort()` default = UTF-16 code units ≡ Python code points for well-formed strings); layout members sorted, extension values verbatim.
- **TA gotcha (PROJECT.md References)**: exact rational invariants need a fraction implementation in TS (feature #4, not #3); Python `_explore` truncation order (finish current state's edges, then stop) must match for conformance vectors (feature #5).
- Python `add_output(transition, place)` argument order is swapped vs `add_input(place, transition)` — keep the TS API keyword-like (object args) to avoid the trap.
- io.ts duplicate-key rejection: `JSON.parse` keeps last-wins silently — needs a reviver-free pre-scan or a small parser check if parity with Python's `FormatSyntaxError` is required (Python uses `object_pairs_hook`).

## Blockers

- None. (Network OK; harness state clean — harnessc check 0 errors at pause; budgets: work_md 4662/5120, scratchpad 3060/3072 — scratchpad nearly full, prefer WORK_ARCHIVE rotation over budget growth.)

## Artifacts to read first on resume (in order)

1. `WORK.md` (task row `[~]`) → this file
2. `.projects/meta/petri_net_studio/PROJECT.md` (§New Session Quick Start, roadmap table) + `CURRENT_STATE.md` (pause entry on top)
3. `.meta/software_development_process/2.requirements/features/feature_034.studio_v1_editor/FEATURE.md` (+ `plan/PLAN.md`) — state recorded there
4. `shared/petri-net/FORMAT.md` (the contract: §6 validation, §8 canonical serialization)
5. `src/agentx/model/petri_net/model.py` + `io.py` + `tests/model/petri_net/test_io.py` (port sources/reference matrix)

## Housekeeping done at pause (this session)

- petri_net test subset re-verified green (158 passed) — shipped parts (features #1–#2) intact.
- `CURRENT_STATE.md`: pause entry added on top (resume point); `PROJECT.md`: feature_034 scaffold ticked in Status.
- `feature_034` FEATURE.md/PLAN.md: state = scaffolded, next steps + open TDD decision recorded.
- TA thought added to `src/agentx/model/petri_net/io.py` (TS-port parity knowledge lives with the source).
- `WORK.md`: feature_034 row added as `[~]` paused → pointer to this file.
- Full suite was green at 1639 after feature_033; no code changes since (docs/housekeeping only).
