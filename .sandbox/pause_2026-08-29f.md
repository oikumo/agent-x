# PAUSE — feature_038.tdd_toolchain_aware (meta_harness_5 forward scope)

> Date: 2026-08-29 · Workflow: `pause_dev_for_resume_later` · Task row in WORK.md: `[~]` (paused)
> Resume in a new session by reading THIS file first, then the artifacts listed in §"Read first".

## Active task

- **`feature_038.tdd_toolchain_aware`** — project **meta_harness_5** (the top genuine DX win from the fresh harness review `.sandbox/meta_harness_5_idea.md`, Proposal A). Type: **minor_feature**.
- Declared via `uv run scripts/omt/new_feature.py "tdd_toolchain_aware" --type minor_feature --project meta_harness_5` → linked `feature_038` (meta_harness_5 auto-flipped draft → active).
- Phase: **Analysis** declared (omt_phase{task_type:minor_feature, phase:Analysis, feature:feature_038.tdd_toolchain_aware}).

## Goal (one line)

Make the **pytest-only `omt_tdd` two-hats engine toolchain-aware** (`.py` → pytest, `.ts/.tsx` → vitest) so TypeScript/Vitest features (petri_net_studio 034/035/036) get real RED/GREEN/REFACTOR enforcement instead of the documented A11/B11 manual red→green workaround.

## What was completed so far (this session, verified)

1. **Fresh harness review written** — `.sandbox/meta_harness_5_idea.md` (243 lines) at HEAD `544b40285`: audited current metrics (208 @-records, 247 nav-index, 17 gotchas, 11 budgets), confirmed the 6 prior proposals shipped + 3 rejected + #10 prose fallback shipped in feature_037, and identified the **single new genuine DX win (Proposal A**: toolchain-aware TDD dispatch). Verified the failure live: `omt_tdd{op:red}` on a Vitest node returned `exit_code: 4` ("❌ pytest error (exit 4). Check the test node ID.\nERROR: file or directory not found").
2. **Feature declared** as `feature_038.tdd_toolchain_aware` in meta_harness_5 (scaffold FEATURE.md/PLAN.md created).
3. **`omt_phase` Analysis declared** → src/ unlocked.
4. **Implementation (core) — DONE, in `scripts/omt/tdd/`:**
   - `state.py`: added `run_test(test_node, timeout)` — dispatches on test-file suffix: `.ts/.tsx` → `npx vitest run <file> [-t <name>]`, else → `run_pytest(...)`. (`git diff` +33 lines.)
   - `cli.py`: `cmd_start`/`cmd_green`/`cmd_refactor` now call `run_test` (was `run_pytest`); the Python-AST true-red/summary/anti-pattern block in `cmd_start` now guarded to `.py` suffix only (Vitest targets skip AST analysis → treated as valid RED). Message wording "❌ pytest error" → "❌ test error". (`git diff` +53/-… )
   - `gates.py`: `cmd_after_edit` REFACTOR-revert check now calls `run_test`; removed now-unused `run_pytest` import.

## ⚠️ OPEN ITEM — the vitest subprocess `cwd` is WRONG (must be fixed on resume)

- `run_test` currently runs vitest with `cwd=str(test_path.parent)` → for a node like `tools/petri-net-studio/tests/engine/analysis.test.ts` the cwd becomes `tools/petri-net-studio/tests/engine/`, which is NOT the vitest project root.
- **Symptom observed:** direct `npx vitest run tests/engine/analysis.test.ts -t "TestReachableMarkings"` from `tools/petri-net-studio/` → `2 passed` (exit 0). BUT `uv run scripts/omt/tdd_check.py start --test-node "...::TestReachableMarkings"` returned `exit_code: 1` (vitest "no tests matched" / config-not-found behavior from the wrong cwd). The dispatch itself WORKS (it no longer returns the pytest exit-4 "file not found" error; it now returns a genuine vitest exit code + RED verification).
- **Fix on resume:** pass the test file's **project root** (discover `tools/<proj>/package.json` / `vitest.config.*` upward from `test_path`, or use `tools/petri-net-studio/`) as the subprocess cwd — NOT `test_path.parent`. Verify `start` (RED) and `green` (GREEN) both round-trip with correct exit codes (0 = pass, 1 = fail) on the real studio test files.

## Immediate next step (resume here)

1. **Fix the vitest cwd** in `scripts/omt/tdd/state.py:run_test` (see OPEN ITEM) — resolve the vitest project root (upward search for `package.json` with a `vitest` dep, or `vitest.config.{ts,js,mts,mjs}`) and run vitest from there.
2. **Re-verify live:** `uv run scripts/omt/tdd_check.py start --test-node "tools/petri-net-studio/tests/engine/analysis.test.ts::TestReachableMarkings" --feature test.xyz --session <s>` → must return `ok:true, verified:true` with a vitest exit code that reflects reality (a passing node → the "already passes" ok:false branch; a genuinely failing/new node → RED ok:true exit 1). Test both `start` and `green`.
3. **Unit tests:** extend `tests/scripts/omt/test_tdd_check.py` with `run_test` dispatch tests (mock `subprocess.run` / assert command + cwd selection for `.py`, `.ts`, `.tsx`, unknown-suffix fallback). Keep existing JSON-array prose testlist tests untouched (regression).
4. **Doc sync (harness surface, round-robin):** update `.meta/META_HARNESS.omt` gotcha/hat/tool records + `WORK.md` gotcha count 17→18 (new gotcha documenting toolchain dispatch, superseding the A11/B11 manual-workaround wording) → `uv run scripts/omt/harnessc.py build` + e2e receipt refresh + `harnessc check` 0 errors.
5. **Regression + sentinel:** `uv run pytest tests/scripts/omt/ -q` → full `uv run pytest` sentinel (≈1658+ passed incl. KNOWN_SUITE_FAILURES).
6. **Bookkeeping + complete:** `omt_phase` Programming/Testing as needed → fill FEATURE.md/PLAN.md → meta_harness_5 CURRENT_STATE.md/PROJECT.md status → `omt_complete{feature:"feature_038.tdd_toolchain_aware"}` → WORK.md row → `[x]` DONE → `uv run scripts/omt/project.py sync` → commit (user-initiated).

## Read first (in this order)

1. THIS file (`.sandbox/pause_2026-08-29f.md`)
2. `WORK.md` task row (`[~]` pointer here) + scratchpad gotchas
3. `.projects/meta/meta_harness_5/PROJECT.md` (§New Session Quick Start, and Requirements backlog → the recommended forward scope that spawned this) + `CURRENT_STATE.md`
4. `.sandbox/meta_harness_5_idea.md` — the fresh harness review (Proposal A spec + Before/After table)
5. `.meta/software_development_process/2.requirements/features/feature_038.tdd_toolchain_aware/FEATURE.md` (+ plan/PLAN.md)
6. Implementation: `scripts/omt/tdd/state.py` (`run_test`, ~line 313) · `scripts/omt/tdd/cli.py` (`cmd_start`/`cmd_green`/`cmd_refactor`, Python-AST guard) · `scripts/omt/tdd/gates.py` (`cmd_after_edit`)

## Git state at pause (NOT committed — user commits separately)

```
 M .meta/.omt/harness.ir.json          M .meta/.omt/nav.index.jsonl
 M .meta/META_HARNESS.omt               M .opencode/lib/enforcer/tdd_hats.ts
 M .projects/meta/META.md               M .projects/meta/meta_harness_4/{CURRENT_STATE.md,PROJECT.md}
 M WORK.md                              M scripts/omt/tdd/cli.py
 M scripts/omt/tdd/gates.py             M scripts/omt/tdd/state.py
 M tests/scripts/omt/test_tdd_check.py
?? .meta/.../2.requirements/features/feature_038.tdd_toolchain_aware/
?? .meta/.../6.testing/features/feature_037.tdd_testlist_prose_fallback/
?? .projects/meta/meta_harness_5/
?? .sandbox/meta_harness_5_idea.md       ?? .sandbox/pause_2026-08-29f.md (this file)
```

> NOTE: `scripts/omt/tdd/{cli.py,gates.py}` are META HARNESS surface — the receipt guard is engaged (git-dirty + mtime > receipt). On resume, after the cwd fix + any further edits to these files, run `uv run pytest tests/scripts/omt/test_omt_harness_e2e.py -q` (ONE edit per file per e2e receipt — GOTCHA_RECEIPT_ROUND_ROBIN). `state.py` is also in the tdd package (check whether it's on the harness_paths list; if so, same discipline).

## Housekeeping record (workflow step 6)

- [x] WORK.md updated: `feature_038.tdd_toolchain_aware` task row added and marked `[~]` (paused) with pointer to this file.
- [x] WORK.md Projects table auto-synced via `uv run scripts/omt/project.py sync` → `meta_harness_5 | active | feature_038.tdd_toolchain_aware`.
- [x] `.meta/META_HARNESS.omt` `@budget work_md` grown 5120→5632 (deliberate, same-session doc sync; feature_038 active row pushed it over) → `harnessc build` OK → e2e receipt refreshed → `harnessc check` 0 errors (251 records). Repo is in a clean, resumable state.
- [x] TA gotcha thought added to `scripts/omt/tdd/state.py:314` (vitest cwd must be project root, not test dir).
- [x] Project/`meta_harness_5` `CURRENT_STATE.md` iter-1 entry appended; `PROJECT.md` Status updated (forward scope executed → active).
- [x] Code compiles/imports clean (`run_test` present in state.py). `harnessc check` 0 errors at pause.
- [x] User confirmed the resumption document is accurate (2026-08-29) — session safe to close.
- ~ Session is safe to close. Resume entry point: `WORK.md` `[~]` row → `.sandbox/pause_2026-08-29f.md` → fix vitest cwd → re-verify RED/GREEN → unit tests → doc sync → sentinel → `omt_complete`.

