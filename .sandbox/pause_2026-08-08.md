# Pause 2026-08-08 — feature_025.coding_context_window_optimization

> Result: **paused at Programming phase (pre-RED), resume by reading `.sandbox/pause_2026-08-08.md` then design_001.**

## Active task

- **Task id / name:** `feature_025.coding_context_window_optimization` — major_feature.
- **In WORK.md?** Yes, line 26, marked `[~]` (paused pointer appended in Step 5).
- **Phase (OMT):** Programming · TDD auto-on (`omt_tdd{op:testlist→red→green→refactor→done}`).
- **Holdback:** the *behaviors* parameter to `omt_tdd{op:testlist}` consistently returned `Expecting value: line 1 column 1 (char 0)` regardless of size or JSON-shape (even `["x"]`). The TS tool wrapper appears to pass `behaviors` empty to `json.loads(args.behaviors)`. Workaround to investigate next session: either run `uv run scripts/omt/tdd/cli.py testlist --behaviors '[...]' --feature feature_025.coding_context_window_optimization --session ...` directly via bash (see `scripts/omt/tdd/cli.py` cmd_testlist), OR raise a fix in `.opencode/lib/enforcer/tdd_hats.ts`.

## Completed so far

1. **New feature scaffolded:** `uv run scripts/omt/new_feature.py "coding context window optimization" --type major_feature` → `feature_025.coding_context_window_optimization` requirements dir.
2. **Analysis doc** `3.analysis/features/feature_025.coding_context_window_optimization/analysis_001_context_window_bloat.md` — flow-through of the bare `create_agent` bloat problem + deepagent techniques available + constraints (MVC pin, deepagents not installed, streaming-summary filter, model-profile defaulting) + non-goals + recommendation.
3. **Design doc** `4.design/features/feature_025.coding_context_window_optimization/design_001_deepagent_context_optimization.md` — full design: swap `create_agent` → `create_deep_agent`; middleware stack order per `/oss/python/deepagents/customization#bare-stack` (Skills → Filesystem → Subagents → Summarization → Patch → caching → Memory); add `compact_conversation` tool via `create_summarization_tool_middleware`; keep `from langchain.agents import create_agent` + `from langgraph.checkpoint.memory import InMemorySaver` for MVC-pin compatibility; fallback path when `deepagents` import fails; static structure + sequence + breaking-change analysis + tests 1-8.
4. **Operation spec** `4.design/features/feature_025.coding_context_window_optimization/operation_spec_001_deepagent_service_methods.md` — `__init__`, `stream_agent` (with `lc_source == "summarization"` filter), `cancel`, `reset_conversation`, `get_history`, properties + backwards-compat matrix + MVC pin impact.
5. **FEATURE.md** filled (Summary, Scope, task type, artifact table).
6. **PLAN.md** filled (Objective, Steps with checkboxes + TDD sub-steps, dependency change note).
7. **WORK.md** task added at top of Tasks list (line 26) marked `[~]`.
8. **omt_phase** declared Analysis → Design → Programming for `feature_025.coding_context_window_optimization` (ledger shows phase transitions for 2026-08-08).
9. **TA: thoughts** embedded in src (per Rule 2): `coding_agent_service.py:24` (TODO with full design pointer) + `coding_tools.py:18` (XREF noting FilesystemMiddleware wraps the in-history rendering, tool I/O unchanged).
10. **KB consult** satisfied (`omt_kb_nav{op:nav, query:"coding"}` returned CodingAgentService/Controller/ICodingView contracts; no `coding_agent_service` keyword match but the coding-tier records cover the contract).

## Immediate next step

**RED phase of TDD.** Create the failing tests file and prove they fail before touching `src/`:

1. **(Optional / blocker-ish) Record the testlist** — run `uv run scripts/omt/tdd/cli.py testlist --behaviors '[...]' --feature feature_025.coding_context_window_optimization --session "$SESSION"` directly via bash to bypass the wrapper issue (or fix `tdd_hats.ts`). Use the 10 behaviors listed in design_001 §Testing strategy.
2. **Create** `tests/features/feature_025.coding_context_window_optimization/test_deepagent_context_optimization.py` (a NEW file under `tests/` — note: this needs canary-approval per the harness test rules; see AGENTS.md `tests/` gate). Tests use `pytest`, mock the `BaseChatModel`, and monkeypatch `sys.modules["deepagents"]` to control import success / failure paths.
3. **Run `omt_tdd{op:red}`** (or `uv run .../cli.py start`) at the first failing test_node id, target_src = `src/agentx/model/coding/coding_agent_service.py`.
4. **GREEN:** implement the design — edit `src/agentx/model/coding/coding_agent_service.py` per design_001 §Static Structure; add `deepagents>=0.7` to `pyproject.toml`; run `uv sync`; verify import works.
5. **REFACTOR:** slim `DEFAULT_CODING_SYSTEM_PROMPT` (the deepagent base prompt already covers file-tool usage per docs); keep tool descriptions concise.
6. **`omt_tdd{op:done}`** → runs full suite; rotate DONE into WORK.md (note the existing `feature_001` / `feature_002` pending rows stay).

## Blockers

- **`omt_tdd{op:testlist}` tool fails** with `Expecting value: line 1 column 1 (char 0)` for ANY behaviors payload, including a one-element array. Root-cause candidate: `.opencode/lib/enforcer/tdd_hats.ts` line ~28 declares `behaviors: tool.schema.string().optional()` but the harness wrapper may be defaulting the missing/empty argument to `""` so `json.loads("")` raises. Mitigation: invoke `scripts/omt/tdd/cli.py` directly (which reads `json.loads(args.behaviors)`) or fix the TS wrapper — should be investigated next session before any other TDD work.
- **`pyproject.toml` deepagents dependency** — not yet added; seamless resumption requires `uv add deepagents>=0.7` (or edit `pyproject.toml` deps + `uv sync`) during the GREEN step.
- **`tests/` gate** — writing the new test file under `tests/features/feature_025...` requires canary approval (per AGENTS.md "edit gates: `tests/` w/o canary approval"). The next session must satisfy the canary-approval gating before writing tests.

## Artifacts to read first (in order)

1. **WORK.md** line 26 — task pointer + `[~]` state.
2. **This file** — `.sandbox/pause_2026-08-08.md` (resumption context).
3. **Design doc:** `.meta/software_development_process/4.design/features/feature_025.coding_context_window_optimization/design_001_deepagent_context_optimization.md` — full design + tests 1-8.
4. **Operation spec:** `.meta/software_development_process/4.design/features/feature_025.coding_context_window_optimization/operation_spec_001_deepagent_service_methods.md` — method-level contract.
5. **Analysis:** `.meta/software_development_process/3.analysis/features/feature_025.coding_context_window_optimization/analysis_001_context_window_bloat.md` — problem + constraints + recommendation.
6. **Source files with TA::** `src/agentx/model/coding/coding_agent_service.py` (line ~24, TODO category) and `src/agentx/model/coding/coding_tools.py` (line ~18, XREF category).
7. **Existing tests to keep green:** `tests/features/feature_019.coding_agent_screen/test_coding_mvc.py` (MVC pin) + `tests/features/feature_019.coding_agent_screen/test_coding_integration.py` + `tests/features/feature_024.no_tui_full_features/test_console_provider_and_views.py`.

### URLs / references used

- LangChain deepagents overview: https://docs.langchain.com/oss/python/deepagents/overview
- Context engineering: https://docs.langchain.com/oss/python/deepagents/context-engineering
- Customization (middleware stack order): https://docs.langchain.com/oss/python/deepagents/customization
- `SummarizationMiddleware` built-in: https://docs.langchain.com/oss/python/langchain/middleware/built-in#summarization

## Steps done by the pause workflow itself (track below)

- [x] Step 1 — Read WORK.md, identify in-progress task (line 26).
- [x] Step 2 — List artifacts + remaining work.
- [x] Step 3 — Write `.sandbox/pause_2026-08-08.md` (this file).
- [x] Step 4 — User confirmation (received: "Yes, accurate — update WORK.md and pause").
- [x] Step 5 — Update WORK.md (line 26 set `[~]` + `**PAUSED 2026-08-08**` mid Programming pre-RED + pointer to this file).
- [x] Step 6 — WORK.md updated; session is safe to close.
