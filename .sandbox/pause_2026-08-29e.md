# PAUSE — feature_036.studio_v3_graph (petri_net_studio roadmap #5)

> Date: 2026-08-29 (third pause, after resume) · Session: resumed from `pause_2026-08-29d.md` → **TDD Cycles 1–4 GREEN + C7 allowlist + Cycle 5 UI partially wired** → paused mid-**Cycle 5 (UI)** — `styles.css` §9 is the exact next step.
> Resume: read `WORK.md` `[~]` row → this file → PROJECT.md Quick Start → `design_001_studio_v3_graph.md` (§4–§11) → `operation_spec_001_studio_v3_graph.md` → begin **Cycle 5 remainder** (styles.css §9) per design §11.
> NOTE (git): the user made a WIP commit `80d7923` at 20:51 mid-session — files written before then (analysis.ts `markingFromKey` export, `graphProjection.ts` + test, `animation.ts` + test) are now in HEAD; the uncommitted delta = later work (examples.ts, store.ts, App.tsx, check-independence.mjs, package.json/lock, GraphExplorer.tsx, Gallery.tsx, gallery.test.ts, store.test.ts). `git status --porcelain` is the authoritative diff.

---

## Active task

- **feature_036.studio_v3_graph** — roadmap feature #5 (project v2 completion): reachability-graph explorer (auto-layout), firing-sequence animation, liveness/SCC views, conformance-suite runner wiring, example gallery. Type: major_feature. Linked project: petri_net_studio (active). WORK.md task row exists (`[~]`), pointer updated to this file.
- **Phase: Programming** — declared 2026-08-29 (operation_spec_001 accepted; `omt_phase` ledger). **TDD mode ACTIVE (TESTLIST), cycles: 0** (omt_tdd ledger count — the Vitest/omt_tdd mismatch is declared, A11 precedent; manual red→green evidence lives in this session and must be pasted into the test report at bookkeeping).

## What was completed this session (resume from d → now)

1. **Resume chain read**: WORK.md → `pause_2026-08-29d.md` → PROJECT.md → `analysis_001_graph_explorer.md` → `design_001_studio_v3_graph.md` → `operation_spec_001_studio_v3_graph.md` → verified tree at TDD cycle 0 (no ui/graphProjection.ts, no tests/ui/, no conformance.mjs).
2. **omt_tdd{op:testlist}** recorded — 6 behaviors (engine additive 10.5, projection 10.1, animation 10.2, store 10.4, gallery 10.3, conformance runner). State=TESTLIST.
3. **Cycle 1 (engine additive) — GREEN**: `tests/engine/analysis.test.ts` +2 (markingFromKey round-trip + `"" → []`) → RED (2 failed: not-a-function) → `export` on `markingFromKey` (analysis.ts:30, ONE-line; TA:31 gotcha unchanged) → GREEN 40/40. (omt_tdd red/green calls FAILED — pytest-shaped, A11 mismatch as declared; evidence pasted in-session.)
4. **Cycle 2 (projection) — GREEN**: `tests/ui/graphProjection.test.ts` (7 tests, design §10.1) + `src/ui/graphProjection.ts`. **OOM incident resolved**: the first stub value-imported `MarkerType` from `@xyflow/react` → node-env vitest worker OOM'd (~1.8GB heap at collect). Fix: **type-only imports** + `"arrowclosed"` string literal for the marker (pure/DOM-free module — design §4/C5). RED (6 failed) → pure implementation → GREEN 7/7. **Design-gap resolution (recorded in docblock + TA):** design §4 pinned the signature without the initial marking, but the M0-label rule needs `marking === initialMarkingTuple` → added `initialMarking` as the final `projectGraph` param.
5. **Cycle 3 (animation) — GREEN**: `tests/ui/animation.test.ts` (8 tests, §10.2) + `src/ui/animation.ts` (`markingAt` clamped fold, `sequenceSteps`). RED → GREEN 8/8. Null-sequence = caller case (UI renders "unreachable"; helpers never called with null).
6. **Cycle 4 (store + gallery) — GREEN**: store `graphVisible`/`toggleGraph` (default false; NOT edit-locked; `setMode` untouched; never in the doc/format) + store.test.ts +4 (10.4) → RED → GREEN 56/56. `examples.ts` → `GALLERY_ENTRIES` (8; 3 canonical `?raw` + 5 conformance fixture nets via `JSON.parse(raw).net` re-stringify) + `EXAMPLE_TEXTS` extended with the 5 fixture ids (loadExample works for all 8) + `EXAMPLE_NAMES` stays 3 + `gallery.test.ts` (8 tests, §10.3) → RED → GREEN 8/8.
7. **C7 independence allowlist**: `check-independence.mjs` `ALLOWED_OUTSIDE` → regex `shared\/petri-net\/(examples|conformance)\//` + docblock → `npm run check-independence` OK (17 files / 52 imports).
8. **Full suite checkpoint**: Vitest **274/274** (245 base + 2 + 7 + 8 + 4 + 8), tsc clean, independence OK — all GREEN at pause time.
9. **Cycle 5 (UI) — PARTIAL (paused mid-cycle)**: `npm install elkjs` done (package.json + package-lock.json modified). `src/ui/GraphExplorer.tsx` written (design §5: derived useMemo over `toNet(doc)`+`maxStates`, elkjs `layoutGraph` memoized async step — positions never stored, SCC palette 6 hues, deadlock border, truncation banner D10, component-local animation strip `markingAt`/`sequenceSteps` Play/Step/Reset ~700ms, liveness legend reusing StatusBadge). `src/ui/Gallery.tsx` written (dialog-panel over GALLERY_ENTRIES, Load → `loadExample`). `App.tsx` wired: "Graph" toggle (graphVisible/toggleGraph) + "Gallery" toggle (App-local `galleryOpen`) + `{graphVisible && <GraphExplorer />}` + `{galleryOpen && <Gallery />}` render. TA thoughts added (graphProjection design-gap; GraphExplorer layout-memoization + pause-state gotcha). **`styles.css` §9 NOT yet appended** — the next edit.

## Blocked — none. Immediate next step (Cycle 5 remainder, design §11-6)

1. **Append styles.css §9** (design §9 list): `.explorer-panel` (mirror `.analysis-panel`), `.explorer-legend`, `.scc-chip`, `.truncation-banner` (amber), `.sequence-strip` + `.sequence-cell.active`, `.explorer-canvas` (fixed height, e.g. 420px, border), `.gallery-grid`/`.gallery-card`/`.gallery-card button`. Reuse existing `status-ok/no/unknown` badges. Palette = the 6 HSL hues already in `SCC_PALETTE` (GraphExplorer.tsx).
2. `npx tsc --noEmit` clean → `npm run build` green → `vite preview` smoke (feature_034/035 precedent: 200×3, curl the dist server).
3. **Cycle 6 (conformance runner)**: `scripts/conformance.mjs` (design §7/C8): spawnSync `uv run python scripts/generate-vectors.py` → assert `git status --porcelain shared/petri-net/conformance/` EMPTY (byte-identical determinism; fail with diff hint) → `npx vitest run tests/engine/conformance.test.ts`; exit 0 only all pass. Wire `"conformance": "node scripts/conformance.mjs"` in package.json. Run it.
4. **Sentinel + suite**: `tests/features/feature_036.studio_v3_graph/test_studio_v3_graph_sentinel.py` (feature_035 precedent: executes `npx vitest run`, env-skip without node, structural floor always-on incl. NEW files — graphProjection.ts, GraphExplorer.tsx, Gallery.tsx, animation.ts, conformance.mjs, tests/ui/*; canary-approval skip logged) → `uv run pytest -q` full agentx suite green.
5. **Bookkeeping**: implementation_001 notes (incl. the OOM fix + design-gap resolution), test_report.md (paste the red→green evidence from THIS session: 1/2/3/4 cycles + suites; declare the Vitest/omt_tdd mismatch per A11; note the WIP commit), FEATURE.md/PLAN.md checkboxes, WORK.md task row, project CURRENT_STATE/PROJECT iter-9, `omt_complete` Programming → Testing → Done.

## Files a future session must read first

1. `WORK.md` (this `[~]` row — pointer here).
2. This file (`pause_2026-08-29e.md`).
3. `.projects/meta/petri_net_studio/PROJECT.md` — Quick Start + iteration log (iter-8 = predecessor; feature_036 scaffolded in iter-8).
4. `.meta/software_development_process/4.design/features/feature_036.studio_v3_graph/design_001_studio_v3_graph.md` (pins §4–§12) + `operation_spec_001_studio_v3_graph.md` (op contracts).
5. `.meta/software_development_process/3.analysis/features/feature_036.studio_v3_graph/analysis_001_graph_explorer.md` (C1–C10).
6. Code tree (current): `tools/petri-net-studio/src/ui/{GraphExplorer.tsx,Gallery.tsx,graphProjection.ts,animation.ts,App.tsx,AnalysisPanel.tsx}`, `src/state/store.ts`, `src/examples.ts`, `src/engine/analysis.ts` (markingFromKey exported), `scripts/check-independence.mjs` (C7 regex), `tests/ui/*` + `tests/engine/analysis.test.ts` + `tests/state/store.test.ts`.
7. Precedent: feature_035 sentinel `tests/features/feature_035.studio_v2_analysis/test_studio_v2_analysis_sentinel.py` + feature_034/035 test reports (A11/B11 manual TDD protocol, preview smoke pattern).

## Session-closed record (workflow step 6 — filled after user confirmation)

- ✅ TA thoughts added to new source files (graphProjection.ts design-gap; GraphExplorer.tsx layout-memoization + pause-state gotcha).
- ✅ `WORK.md` updated: `feature_036.studio_v3_graph` `[~]` row pointer → this file (paused mid-Cycle 5, styles.css §9 next).
- ✅ Resume artifacts verified: this doc + prior pause docs + design/op-spec/analysis; Vitest 274/274 + tsc clean verified at pause time.
- ✅ User confirmed the resumption document is accurate (2026-08-29, "Accurate, pause now").
- Session is safe to close. Resume entry point: `WORK.md` `[~]` row → `pause_2026-08-29e.md` → append styles.css §9 → build + preview smoke → Cycle 6 conformance runner → sentinel → bookkeeping.

## Result

paused at **Cycle 5 (UI) — styles.css §9 is the exact next step** (GraphExplorer/Gallery/App wired, elkjs installed, 274/274 + tsc clean); resume by reading `.sandbox/pause_2026-08-29e.md` → append styles.css §9 → build + preview smoke → Cycle 6 conformance runner → sentinel → bookkeeping.