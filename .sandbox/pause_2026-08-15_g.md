# Pause: 2026-08-15 (session G) — rag_v2 Design phase COMPLETE, Programming phase NOT started

> Active task: **feature_027.rag_v2** (major_feature, Programming phase now current).
> Paused at: **Design phase deliverable shipped, Programming phase TDD cycle NOT started.** The `design_001_retrieve_offload_delegate.md` + companion `operation_spec_001_rag_v2_service_and_tools.md` are written + in the worktree; the TDD cycle (`omt_tdd{op:testlist → red → green → refactor → done}`) has NOT been invoked. No `src/` edits were made this session (Design → docs only; Programming src/ gate unlocks at `omt_tdd{op:testlist}` declaration, not at `omt_complete`).
> Resume by: reading this file, then **invoking `omt_tdd{op:"testlist", behaviors:"[...]", feature:"feature_027.rag_v2"}`** with the 24-pytest-node-ID behavior array from the design doc's Test plan, then RED (tests/ only), then GREEN (src/ only), then REFACTOR (src/ only), then `omt_tdd{op:"done"}` with `checklist.suite_passes:true`.

---

## What this session is

The user asked to **resume feature_027 development**. The prior pause (`.sandbox/pause_2026-08-09_f.md`) had the feature at Design phase deliverable NOT started — `analysis_001_*.md` shipped, Design dir missing, `design_001_*.md` not written.

This session:
1. Read `WORK.md` → identified `feature_027.rag_v2` at `[~]` paused.
2. Read `.sandbox/pause_2026-08-09_f.md` (the iter-1 pause marker) + `analysis_001_v1_gaps_and_deepagents_grounding.md` (the 6-section Analysis artifact) + `.projects/meta/rag_v2/PROJECT.md` (locked v1.2) + `CURRENT_STATE.md` iter-1 entry's "Next" section — understood the 5 design-doc ownership areas (architecture, subagents contract, persistence decision, cutover decision record, test plan).
3. Consulted the 5 TA: thoughts at `PROJECT.md:272-276` (think-gate satisfied before writing the design doc): :272 gotcha D5 pattern locked + coding_agent_service.py:159 grounding; :273 todo v1-cutover deferred to Design; :274 xref ANALYSIS-PARTIAL deepagents stack verified (graph.py:268-893); :275 gotcha G1-G3 STALE vs closure matrix lines 178-187; :276 risk D5 rubric-composition boundary-vs-content.
4. Re-verified the substrate against the working tree (HEAD commit `90b6bd5`): `coding_agent_service.py:159-172` `create_deep_agent(...)` call site; `coding_tools.py:18` `@tool`-pattern; `providers.py:103` `create_rag_view()` factory; `interfaces.py:53,274` `IRagView`/`IRagViewPartner` ABC pair; `main_controller.py:106` v1's buggy `.view =` vs `:244,266` fixed `set_view()` (the Constraint d gate); `rag.py`/`rag_db.py`/`rag_controller.py` (the v2 model aggregates mirror).
5. Re-declared `omt_phase{phase:"Design", task_type:"major_feature", scope:"rag v2 — Design: write design_001_*.md ...", feature:"feature_027.rag_v2"}` (the 2026-08-09 Design activation had expired at the 8h marker) → `mkdir -p .meta/software_development_process/4.design/features/feature_027.rag_v2/` (the dir was MISSING at session start).
6. Wrote `design_001_retrieve_offload_delegate.md` (49 KB) — 9-section design doc (Summary → Problem Analysis → Components → Architecture decision → Static Structure → Functional Flow → Persistence-strategy decision → Cutover decision record → Breaking-change risk → Backwards compatibility → Testing strategy → Open questions/risks → Links). Companion `operation_spec_001_rag_v2_service_and_tools.md` (10 KB) mirrors feature_025's design-dir convention (both design_001 + operation_spec_001 ship together).
7. `omt_complete{feature:"feature_027.rag_v2", advance_to:"Programming"}` passed cleanly (artifact check found the design doc under the auto-detected `4.design/features/feature_027.rag_v2/`). Feature health now 80% (R:1 A:1 D:1 I:1 T:0); Programming phase now current.
8. Updated `CURRENT_STATE.md` iter-2 entry (above iter-1) + `WORK.md` task row.

Then the user invoked the pause_dev_for_resume_later workflow → this session pause.

## What was completed (this session)

- **`design_001_retrieve_offload_delegate.md`** (49 KB) shipped to `.meta/software_development_process/4.design/features/feature_027.rag_v2/`. Five design-doc ownership areas, all closed:
  1. **`RagV2AgentService` architecture** — decision taken: **PARALLEL service** (sibling of `CodingAgentService`, NOT tools-on-coding). 4 reasons documented (separation of concern + v1-is-already-a-separate-screen + subagent isolation + both-consume-the-stack). Same guarded-import + fallback pattern as `coding_agent_service.py:39-50`. NEW kwarg vs coding = `subagents=[chunk_analyst]` explicit.
  2. **`subagents=[chunk-analyst]` contract** — the `SubAgent` dict spec `{name, description, system_prompt, tools?, middleware?, skills?, response_format?, permissions?}`. Decision: keep `general-purpose` default auto-added alongside (Constraint b — less surface-area change). `CHUNK_ANALYST` + `RAG_V2_SUBAGENTS` documented in `rag_v2_subagents.py`.
  3. **Persistence-strategy decision** (Constraint c) — **KEEP `StateBackend`** (ephemeral, per-turn). Chunks are scratch, NOT state. The `backend=` is about agent-side chunk files for subagent `read_file`/`grep` access; the vector store itself stays ChromaDB. `_rag_search_impl` uses explicit `backend.upload_files()` for deterministic chunk filenames (`chunk_0.txt`, …) so the chunk-analyst's `task(description="summarize chunk_0.txt")` references a stable path. Swap-to-FilesystemBackend is a future `meta_harness_2`-style reserved move, NOT v2 scope.
  4. **Cutover decision record** (D3) — **DECISION: DEFER**. v1 stays untouched in this iteration. Gate (locked): v2 surface MUST be proven against G1–G6 BEFORE any cutover. Audit requirement: grep downstream-consumers of v1's RAG (`from agentx.model.rag` / `from agentx.ui.screens.rag`, `RagShowCommand`, `IRagView`/`IRagViewPartner`, `create_rag_view()`) before remove-vs-fallback choice. Two acceptable post-proof choices documented (A remove / B keep as opt-in fallback); taken in a separate post-Testing iteration, NOT here.
  5. **Test plan** — G1–G6 closure matrix sharpened to **24 pytest node IDs** across 5 test files: `test_rag_v2_mvc_contract.py` (~630 lines, mirrors feature_024 `test_console_provider_and_views.py`); `test_rag_v2_commands_and_views.py` (~250 lines, mirrors `test_console_commands_and_views.py`); `test_rag_v2_agent_service.py` (~264 lines, mirrors feature_025 `test_deepagent_context_optimization.py`); `test_rag_v2_retrieval_tool.py` (rag_search + backend.upload_files + chunk-analyst); `test_rag_v2_gaps_closure_matrix.py` (the G1–G6 rows).
- **`operation_spec_001_rag_v2_service_and_tools.md`** (10 KB) companion shipped — 7 operation specs (`RagV2AgentService.__init__`/`stream_agent`/`cancel`/`reset_conversation`/`get_history`, `rag_search` `@tool`, `rag_ingest_status` `@tool`, `chunk-analyst` dispatch, `show_rag_v2` set_view gate, G5 switch_repository, the 3 ingestion sub-screens). Mirrors feature_025's design-dir convention (design_001 + operation_spec_001 paired).
- **Critical `set_view()` pin carried forward** (Constraint d + feature_024 TA: gotcha @ `main_controller.py:251,273`): v1's `show_rag` at `main_controller.py:106` still uses the buggy `rag_controller.view = rag_view` — v2's `show_rag_v2()` uses `set_view()` (the fixed pattern at `show_react`/`show_coding` lines `244,266`). The two coexist; v2 does NOT touch v1's line.
- **Process**: `omt_phase` Design re-declared (expired at 8h) → `omt_complete{advance_to:"Programming"}` passed. Phase ledger updated. Feature health 40% → 80% (R:1 A:1 D:0 I:0 T:0 → R:1 A:1 D:1 I:1 T:0).
- **`CURRENT_STATE.md` iter-2 entry** written (above iter-1). **`WORK.md` task row** updated to reflect `[~]` with "Design phase COMPLETE 2026-08-15; advanced to Programming (TDD auto-activates D7)" pointer.

## What is the immediate next step

**Start the Programming-phase TDD cycle.** Per design_001's Testing strategy + D7 lock:

1. **`omt_tdd{op:"testlist", behaviors:"[...]", feature:"feature_027.rag_v2"}`** — declare the behaviors (the 24 test nodes from the matrix; optional grouping). **Behaviors MUST be a JSON array** (gotcha: prose fails `json.loads` — WORK.md Agent Scratchpad top-3). The behavior array should encode the 24 node IDs from the design doc's "G1–G6 closure matrix — sharpened to pytest node IDs" table.
2. **RED (tests/ only)** — write the 5 test files under `tests/features/feature_027.rag_v2/` with the pytest node IDs from the matrix; all RED (no v2 src yet). Two-hats: RED touches `tests/` only.
   - `test_rag_v2_mvc_contract.py` (~630 lines, mirrors feature_024 `test_console_provider_and_views.py` — 10 TestCase classes)
   - `test_rag_v2_commands_and_views.py` (~250 lines, mirrors feature_024 `test_console_commands_and_views.py`)
   - `test_rag_v2_agent_service.py` (~264 lines, mirrors feature_025 `test_deepagent_context_optimization.py`)
   - `test_rag_v2_retrieval_tool.py` (rag_search + backend.upload_files + chunk-analyst)
   - `test_rag_v2_gaps_closure_matrix.py` (the G1–G6 rows)
3. **GREEN (src only)** — implement `src/agentx/model/rag_v2/` (11 files: `rag_v2.py`/`rag_v2_db.py`/`rag_v2_repository.py`/`rag_v2_agent_service.py`/`rag_v2_tools.py`/`rag_v2_subagents.py`/`rag_v2_provider.py` + `pdf_ingestion/`/`md_ingestion/`/`web_ingestion/`/`query/`) + `src/agentx/ui/screens/rag_v2/` (15 files: controller + view for outer + 4 ingestion sub-screens + create-repo + select-repo + constants) + extend `interfaces.py` (new ABC pairs) + `providers.py` (new factories) + `main_controller.py` (`show_rag_v2()` using `set_view()`). Two-hats: GREEN touches `src/` only.
4. **REFACTOR (src only)** — slim `DEFAULT_RAG_V2_SYSTEM_PROMPT`; ensure `rag_v2_tools.py` `@tool` descriptions are concise (the docstring = the tool description the model sees).
5. **`omt_tdd{op:"done", feature:"feature_027.rag_v2"}` with `checklist.suite_passes:true`**. NOT via `omt_skip` (D7 lock).

After the TDD cycle closes:
- Then **Testing phase** — the `test_report.md` artifact (Feature Health 100% R:1 A:1 D:1 I:1 T:1).
- Then **post-Testing cutover decision** — the remove-vs-fallback choice taken after v2 passes G1–G6 in Testing + the downstream-consumer audit is run against the proven surface.

## Blockers

- **None material.** The Design-phase deliverable shipped cleanly; the Programming-phase decision set is named (the Architecture decision + Persistence-strategy + Cutover-decision + 24-pytest-node-ID test plan are all in `design_001_*.md`).
- One process note: the Programming phase has an 8h expiry (started 2026-08-15 ~now). If the next session resumes >8h after this phase declaration, **re-declare `omt_phase{phase:"Programming", task_type:"major_feature", scope:"rag v2 — Programming: TDD cycle (red → green → refactor → done) per design_001 test plan", feature:"feature_027.rag_v2"}` before `omt_tdd` invocations** — the re-declaration refreshes the expiry. Then start the TDD cycle.
- One TDD-cycle gotcha (WORK.md Agent Scratchpad top-3): **declare red/green/refactor at the SAME test_node** — red at `f.py::C::t` + green at `f.py` strands latest=red → `omt_tdd{op:done}` blocked (recovery: `omt_tdd{op:green}` at the exact red node).

## Paths a future session needs to read first

1. **`.sandbox/pause_2026-08-15_g.md`** (this file) — the resume anchor; documents the Design-complete / Programming-not-started handoff point.
2. **`.meta/software_development_process/4.design/features/feature_027.rag_v2/design_001_retrieve_offload_delegate.md`** — the artifact this session shipped; the source-of-truth for the Programming-phase Static Structure + the 24-pytest-node-ID test plan. READ this before writing any test/src.
3. **`.meta/software_development_process/4.design/features/feature_027.rag_v2/operation_spec_001_rag_v2_service_and_tools.md`** — the companion; the 7 operation specs (Pre/Post/Exceptions) the GREEN-phase implementation follows.
4. **`.projects/meta/rag_v2/CURRENT_STATE.md` iter-2 entry** — the session log for this round; the "Next" section names the 5-step TDD cycle (testlist → red → green → refactor → done). iter-1 (Analysis) and iter-0 (definition) entries stay below as historical anchors (don't rotation-delete).
5. **`.projects/meta/rag_v2/PROJECT.md`** — re-read §New Session Quick Start + the v1-gap→v2 closure matrix at lines 178–187 (the G1–G6 rows the test plan sharpens). The 5 TA: thoughts at lines 272–276 are still anchor references (they carry the substrate findings).
6. **`.meta/software_development_process/3.analysis/features/feature_027.rag_v2/analysis_001_v1_gaps_and_deepagents_grounding.md`** — the 6-section Analysis artifact; source-of-truth for the G1–G3 RESOLVED-in-v1 finding (v2 mirrors for parity, NOT net-new) + the eight v1 surface surprises carried to Design.
7. **Sibling test shapes to mirror at RED time** — `tests/features/feature_024.no_tui_full_features/test_console_provider_and_views.py` (630 lines / 10 TestCase) + `test_console_commands_and_views.py` (250 lines) + `tests/features/feature_025.coding_context_window_optimization/test_deepagent_context_optimization.py` (264 lines).
8. **Sibling src to mirror at GREEN time** — `src/agentx/model/coding/coding_agent_service.py` (the `create_deep_agent` parallel) + `coding_tools.py:18` (the `@tool`-wrapper pattern) + `src/agentx/ui/providers.py:103` (the factory shape) + `src/agentx/ui/interfaces.py:53,274` (the ABC pair) + `src/agentx/ui/screens/main/main_controller.py:244,266` (the `set_view()` fixed pattern — NOT line 106, which is v1's buggy `.view =`).

## What is safe to close

- **Conversation context is SESSION-BOUND** — the substrate-verification reads + the design-doc writing details won't carry to a new session. They are **distilled into** (a) `design_001_retrieve_offload_delegate.md` + `operation_spec_001_rag_v2_service_and_tools.md` (the 9-section + 7-spec artifacts), (b) `CURRENT_STATE.md` iter-2 entry (the session log), (c) the 5 TA: thoughts at `PROJECT.md:272-276` (the substrate findings — still accurate), and (d) this pause file. The design doc + operation spec together are the canonical source-of-truth; don't reconstruct the raw reads.
- **Git**: the scaffolding is the same as iter 1's + iter 0's adds:
  - `.meta/software_development_process/4.design/features/feature_027.rag_v2/` — UNTRACKED (uncommitted per AGENTS.md NEVER rule on `git commit`). Safe to leave untracked across sessions.
  - `.meta/software_development_process/3.analysis/features/feature_027.rag_v2/` — UNTRACKED (from iter 1).
  - `.projects/meta/rag_v2/CURRENT_STATE.md` — MODIFIED (iter-2 entry added this session).
  - `WORK.md` — MODIFIED (task row pointer updated this session).
  - The design doc + operation spec are UNTRACKED. If the user wants them staged: `git add .meta/software_development_process/4.design/features/feature_027.rag_v2/` (NOT committing per AGENTS.md NEVER on `git commit`).
- **No `src/` edits were made this session** (Design phase → docs-only; Programming phase has started via `omt_complete` but the TDD cycle isn't begun → src/ remains gated until `omt_tdd{op:testlist}` declares the behaviors; `omt_complete` to Programming unlocks src/ for the feature, but feature_016 TDD requires RED first — the safe sequence is testlist → red (tests only) → green (src)).

## Idempotency verification (workflow Rule 5)

- Re-running this pause on the same session state would NOT duplicate `design_001_*.md` or `operation_spec_001_*.md` (the files exist; the workflow step 3 is "write a resumption doc", not "rewrite the design doc"). It would NOT duplicate the CURRENT_STATE.md iter-2 block (it's already above iter-1; a re-pause would only refresh the iter-2 prose, not add an iter-3). It would NOT duplicate the WORK.md task row (the row exists at `[~]`; a re-pause would only refresh the pointer text). The pause doc itself is named `pause_2026-08-15_g.md` with a per-session `_g` suffix (iter-1's pause was `_f`, iter-0's was `_e` — this is the next letter) — re-runs on the same date are collision-protected.

## Resume protocol (one paragraph)

Open the next session, read this file, then read `design_001_retrieve_offload_delegate.md` (the 9-section Design artifact this session shipped — it's the Programming-phase Static Structure + 24-pytest-node-ID test plan source-of-truth) + `operation_spec_001_rag_v2_service_and_tools.md` (the companion Pre/Post/Exceptions specs the GREEN-phase implementation follows), then read `.projects/meta/rag_v2/CURRENT_STATE.md` iter-2 entry's "Next" section (names the 5-step TDD cycle). Re-declare `omt_phase{phase:"Programming", task_type:"major_feature", scope:"rag v2 — Programming: TDD cycle (red → green → refactor → done) per design_001 test plan", feature:"feature_027.rag_v2"}` if the phase has expired (8h marker), then invoke `omt_tdd{op:"testlist", behaviors:"[<the 24 node IDs from the matrix>]", feature:"feature_027.rag_v2"}` (behaviors MUST be a JSON array — prose fails `json.loads`), then RED (tests/ only) → GREEN (src/ only) → REFACTOR (src/ only) → `omt_tdd{op:"done"}` with `checklist.suite_passes:true`. **TDD node-granularity gotcha**: declare red/green/refactor at the SAME test_node — red at `f.py::C::t` + green at `f.py` strands latest=red → `omt_tdd{op:done}` blocked. **Think gate for src/**: before any `src/` edit at GREEN, consult `omt_think{op:list}` on the file under edit; deposit new thoughts via `omt_think{op:add}` for forward-going gotchas (e.g., the `set_view()` consequence on `show_rag_v2()`, the `backend.upload_files()` deterministic-naming concern, the `subagents=` SubAgent dict-vs-dataclass shape re-verified against the installed `deepagents>=0.7` in GREEN).

---

## Result (filled in after step 6 completion)

- 2026-08-15 ~now — paused at Design-phase COMPLETE / Programming-phase TDD cycle NOT started. The handoff point is the `design_001_retrieve_offload_delegate.md` + `operation_spec_001_rag_v2_service_and_tools.md` artifacts + the 24-pytest-node-ID test plan + the architecture decision (parallel service) + the persistence decision (keep StateBackend) + the cutover decision (defer to post-Testing with G1–G6 proof gate). Resume by reading this file, then `design_001_*.md`, then invoking `omt_tdd{op:"testlist", behaviors:"[...]", feature:"feature_027.rag_v2"}`.
- WORK.md updated (2026-08-15): `feature_027.rag_v2` row stays `[~]` (paused) with pointer text refreshed to "Design phase COMPLETE 2026-08-15; advanced to Programming (TDD auto-activates D7)" + feature health 80% + the Programming-next pointer.
- CURRENT_STATE.md updated (2026-08-15): iter-2 entry added above iter-1 (the session-log layer). Step done before the pause workflow was invoked.
- Session safe to close. Idempotency verified (Rule 5) — re-running this pause would only refresh pointer text, not duplicate artifacts.
- Phase ledger: the Programming-phase activation is recorded (Design→Programming advance succeeded this session). The next session opens in Programming phase; src/ stays TDD-gated until `omt_tdd{op:testlist}` + RED (tests only) ships; GREEN (src) ships only after red is stranded at a test_node.
