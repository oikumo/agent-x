# Pause 2026-08-09 — feature_026.omt_q_interrogative_first_ops

> Result: **paused at Design phase (pre-design-doc), resume by reading `.sandbox/pause_2026-08-09_b.md` then design_001 + operation_spec (to be written next session).**

## Active task

- **Task id / name:** `feature_026.omt_q_interrogative_first_ops` — major_feature (TDD auto-on).
- **In WORK.md?** Yes — Step 5 added line 27 `[~] feature_026.omt_q_interrogative_first_ops — **PAUSED 2026-08-09** mid-Design-phase pre-design-doc` (between the feature_025 DONE row and the feature_001 pending row) with a pointer to this file.
- **OMT phase (current):** Design (recorded via `omt_phase` this session).
- **Phase progression this session:** Analysis (declared, analysis_001 written, `omt_complete` advanced) → Design (declared, but no design_001 / operation_spec written yet — pause hit mid-Design).

## What this feature is (carry the model in one paragraph)

Ship a new read-only `omt_q` TS plugin at `.opencode/plugins/omt_q.ts` with three ops on top of v1 substrates: `op:state{feature?, session?, as_of?}` (U1 + U6/U7/U8/U10/U13 folds + U9 `live_smoke_count` named field), `op:plan{path, tool?, session?, as_of?}` (U2 pre-flight + U11 receipt-detail fold), `op:drift{as_of?}` (U3 AKB/source drift + count-drift direction-b). Every response envelope carries `as_of_commit:"<HEAD-sha>"` (v1.5 characteristic stub — parsed live via `git rev-parse HEAD`). Single mechanical touch: `gate_driver.runBeforeGates` gains a synthetic-`GateCtx` + `dryRun=true` return-decisions variant (catches `OmtBlock`, captures `{gate_id, blocked, msg, skip_ok}` instead of propagating; real edits still throw on the existing path). No new gates, no ledger semantics change, no enforcer relocate — v1 lock preserved. Parent design source-of-truth: `.projects/meta/meta_harness_2/PROJECT.md` (v1.5, 513 lines).

## Completed so far

1. **Pre-flight sanity checks** — all pass:
   - ledgers: `wc -l .meta/.omt/ledger*.jsonl` = 23 / 415 / 628 (= 1066) — matches the v1.5 fingerprint within churn tolerance (one new ledger.jsonl record since v1.5);
   - kind totals cross-ledger: think_consult 517 / phase 192 / skip 164 / complete 101 / tdd 80 / tdd_testlist 12 — matches v1.5-corrected totals;
   - `task(`/`subagent_type` ledger hits = 0 (subagent surface never used — U16 evidence intact);
   - `.agents/agents/` directory does NOT exist (subagent surface unused); `.agents/skills/` exists with the mermaid skill only;
   - 63 of 123 skip records in 202608 are `"reason":"live smoke"` uniformly scoped `nav` — U9 evidence intact;
   - substrates present: `gate_driver.ts`, `omt_nav.ts` (mirror template), `test_omt_enforcer_guard_source_pins.py`, `harness.ir.json`, `kb.ir.json`, `omt_harness_e2e_last_run.json`;
   - `KNOWN_SUITE_FAILURES = frozenset(` literal at `scripts/omt/tdd/state.py:132` (the U10 parse target);
   - git tree clean, last commit `8b384b1 [WIP] Project META HARNESS v2`.
2. **Feature scaffold:** `uv run scripts/omt/new_feature.py "omt_q interrogative first ops" --type major_feature` → `feature_026.omt_q_interrogative_first_ops` requirements dir (FEATURE.md + plan/PLAN.md template).
3. **`omt_phase` Analysis** declared (major_feature, tdd=true, scope=omt_q 3 ops + 7 folds + envelope + gate_driver dryRun refactor + golden suite).
4. **Analysis doc** `.meta/software_development_process/3.analysis/features/feature_026.omt_q_interrogative_first_ops/analysis_001_substrate_rederivation_costs.md` written (~150 lines): problem statement, current implementation, 10 observed re-derivation costs (mapped 1:1 to op/fold that collapses them), substrate inventory (with live-helper index), **live IR fingerprint correction** (parent doc says "9 before-gates" — the live IR has 7 before-gates + 2 after-gates today), the TA: self-trigger recursion (edge case #2), constraints, Phase-A v1.4 telemetry additions, non-goals, recommendation.
5. **`omt_complete Analysis → Design` advanced** — Analysis artifacts verified (FEATURE.md + analysis_001_*.md both pass).
6. **`omt_phase` Design** declared (major_feature, tdd=true, scope=design_001 + operation_spec outputs).
7. **One TA: thought added to analysis_001 doc** recording the "9-→-7 before-gate correction" so the next session consults the corrected count.

## NOT completed so far (the immediate next steps)

- **Design doc `design_001_omt_q_first_ops.md`** under `4.design/features/feature_026.omt_q_interrogative_first_ops/` — NOT WRITTEN. This is the §12 artifact gate for Phase-A `major_feature`; Programming phase edits stay blocked until it exists. (I had just started drafting this when the pause request came in.)
- **Operation spec `operation_spec_001_omt_q_ops.md`** under the same dir — NOT WRITTEN. Required by §12 (PHASE_EXIT_REQUIREMENTS.Design: `design_001_*.md` + `operation_spec_*.md`/`operations.md`).
- **`omt_complete Design → Programming`** — gated on the two artifacts above existing.
- **`omt_phase` Programming** (TDD auto-on) — gated on the Design → Programming advance.
- **TDD testlist** (`omt_tdd{op:testlist}` with the golden behaviors for U1/U2/U3 + U6–U11 + U13).
- **Build-sequence bootstrap:** first `omt_q{op:plan, path:".opencode/lib/enforcer/gate_driver.ts"}` predicting `g.think` BEFORE the refactor edit (the v1.3 thesis demonstration).
- **`gate_driver.runBeforeGates` refactor** — synthetic `GateCtx` + `dryRun:true` return-decisions variant.
- **`.opencode/plugins/omt_q.ts`** — 3 ops + 7 folds + `as_of_commit:"HEAD"` envelope + `kind:"q"` telemetry (incl. v1.4 `op_set`/`fold_used`/`latency_ms` + v1.5 `as_of:"HEAD"`).
- **`KNOWN_SUITE_FAILURES` regex extractor** — one regex over `scripts/omt/tdd/state.py;132`, fail-open with `KNOWN_SUITE_FAILURES_parse_failed` flag.
- **Golden test suite** — new file under `tests/scripts/omt/test_omt_q.py` (mirrors `test_omt_enforcer_guard_source_pins.py` location convention for harness-level TS plugins). NOTE: harness-paths edits trigger `g.receipt`; each edit round needs a fresh `tests/scripts/omt/test_omt_harness_e2e.py` run.
- **Full regression suite green** — 1196+ baseline; `KNOWN_SUITE_FAILURES` allowlist of 6 IDs unchanged.
- **Implementation notes** under `5.implementation/features/feature_026.omt_q_interrogative_first_ops/` (required by §12 Programming → Testing exit).
- **Test report** `6.testing/features/feature_026.omt_q_interrogative_first_ops/test_report.md` (required by §12 Testing → Done exit).
- **WORK.md + CURRENT_STATE.md updates** at feature close (rotate DONE per CONV_WORK_ROTATE).
- **`.projects/meta/meta_harness_2/CURRENT_STATE.md`** — append a 2026-08-09 iter 12 session-log entry recording this build-start + pause (currently the file stops at iter 10 v1.4 round).

## Blockers

- _(none)_ — the previous session's `omt_tdd{op:testlist}` wrapper bug (per pause_2026-08-08.md holdback: `Expecting value: line 1 column 1 (char 0)`) was not investigated this session and may still be present. Mitigation in the pause_2026-08-08.md "Immediate next step" applies: invoke `scripts/omt/tdd/cli.py` directly via bash, OR fix `tdd_hats.ts` if the wrapper alone is broken. **Investigate before the first `omt_tdd{op:testlist}` call next session.**

## Substrate-level design decisions already locked (pre-design-doc — write into design_001 next session)

These were settled by the Analysis doc + live IR inspection and should be carried directly into the Design doc:

1. **Before-gate count is 7, not 9.** Parent doc's "9 before-gates" is stale; live `harness.ir.json` today has `g.nav, g.protect, g.receipt, g.tests, g.phase, g.think, g.kb` = 7 before-gates. `op:plan` filters dynamically via `gates.filter(on==="before")` so the count comes from the IR, not a hardcoded literal.
2. **Plugin mirrors `omt_nav.ts` structure.** Imports from `../lib/omt_shared` (`repoRoot`, `initOmtShared`, IR/kb/ledger loaders + `loadIr`, `readLedger`, `readThoughtsIndex`, `loadKbIr`, `omtHarnessE2eStatus`, `appendLedger`, `gateMsg`). `tool()` built inside `createQTools()` AFTER `initOmtShared` (F2/F17 cwd-fix pattern). Export default `({directory, worktree}) => { initOmtShared(worktree ?? directory); const {omt_q} = createQTools(); return {tool:{omt_q}} }`.
3. **`gate_driver.runBeforeGates` refactor shape.** Add an orthogonal `dryRun=true` flag (or a sibling `runBeforeGatesDry(ctx)` entrypoint that takes a pre-built `GateCtx` directly). The loop already exists at L261-269 (`for (const gate of gates)` … `await impl(gate, ctx)`). The dryRun path wraps `impl(gate, ctx)` in `try { await impl(gate, ctx) } catch (e) { if (e instanceof OmtBlock) capture else throw }` and returns `[{gate_id, blocked, msg, skip_ok} ...]` instead of `void`. **Real edits still throw** on the existing `await impl(gate, ctx)` path — pinned by `test_omt_enforcer_guard_source_pins.py` (BUG-A before-hook edit path reads `output?.args?.filePath`) + `test_omt_harness_e2e.py` (the F14 real-binary tests).
4. **`op:state` field set (locked v1.5 surface):** `phase` (pipe-through of `session_state.getActiveFeaturePhase(feature, session)`); `tdd_position` `{state, test_node, target_src[], verified, exit_code}` from latest `kind:"tdd"`; `stranded_red` (per-`test_node` latest-`state==="red"` with no later `green` at same `test_node`); `closed_via_skip` (U7 — latest `done` with any false in `checklist.{suite_passes, refactor_recorded, naming_ok}` + later `skip` with reason containing "Override permits" or "pre-existing baseline", scoped to the same `feature` + 1h window cross-feature FP guard); `decree_health` (U8 — `slug_variants[]`, `empty_slug_records[{ts,scope}]`, `invalid_phase_records[{ts,phase}]` validated against `fsm.phase.states="Analysis,Design,Programming,Testing,Done"`, `phase_cycle_count`); `known_suite_failures` (U10 — 6 node IDs parsed from `state.py:132` `frozenset({...})` via regex `/KNOWN_SUITE_FAILURES\s*=\s*frozenset\(\{([^}]+)\}\)/` then split on quotes; fail-open flag `KNOWN_SUITE_FAILURES_parse_failed`); `recent_consults[{files[], ts, session}]` + `consult_needed[]` (U13 — within `UNLOCK_WINDOW_MS=28800000ms`=8h); `skip_reason_tally` (U9 — top-3 reason stems + counts, **with `live_smoke_count` as a named field** surfaced separately from the generic top-3); `last_activity_ts`; risky thoughts on files touched under this feature with `line_drift` flag per edge case #3 (live re-scan of source for the `TA:` marker via `@var thought_pattern`).
5. **`op:plan` field set.** Ordered before-gate chain (7 gates from the IR, filtered on==="before" + sorted by `order`); each gate's `when=` and `requires=` evaluated through the existing `evalPred`/`evalPredExpr` against a synthetic `GateCtx` (built from `path`/`tool`/`session` inputs + live `env.state`). `receipt_detail` (U11) — when `path` matches `@var.harness_paths` (`g.receipt` would fire), return `{receipt_required:true, file_mtime, receipt_passed_at, stale:bool, refresh_tests:["tests/scripts/omt/test_omt_harness_e2e.py"], refresh_cmd:"uv run pytest tests/scripts/omt/test_omt_harness_e2e.py -q"}`.
6. **`op:drift` field set.** (a) KB records whose `src`+`line` don't match the source skeleton at the same symbol id (gone/moved/untracked); (b) `count_drift` = `len(kb.ir.json records)` vs `len(source skeleton)`, **direction-b only** (KB > skeleton IS drift; KB < skeleton is NOT drift, just un-curated — per edge case #5 + the v1 "new = not-yet-tracked is NOT drift" rule).
7. **`as_of_commit` envelope.** Every `omt_q` response wraps in `envelope: {as_of_commit:"<HEAD-sha>", op, ...}` — Phase-A stub returns `HEAD` for every call (parsed live via `git rev-parse HEAD`). Phase-B U18 (`as_of:"<commit>"` parameter + `git show` reconstruction) is out of scope.
8. **`kind:"q"` ledger record.** Per `omt_q` invocation: `op`, `feature?`, `session`, `ts` (v1.3 schema) + `op_set:[<folds used>]`, `fold_used:"<comma-separated U#>"` (v1.4 additions), `latency_ms:<int>` (v1.4 outcome-acceptance metric) + `as_of:"<HEAD-sha>"` (v1.5 stub — always `"HEAD"` in Phase-A).
9. **Golden tests.** New file `tests/scripts/omt/test_omt_q.py` following the harness-level pin-test convention (alongside `test_omt_enforcer_guard_source_pins.py` + `test_omt_harness_e2e.py`). Hand-crafted scenarios per use case:
   - **U1** (op:state resume — 5-read → 1-call): hand-craft a feature with phase + tdd + skip + think_consult + thoughts + fsm state, assert the snapshot matches the documented expected answer.
   - **U2** (op:plan pre-flight): **the highest-risk canary** — assert `op:plan{path:".opencode/lib/enforcer/gate_driver.ts"}` predicted chain == real `tool.execute.before` run on the same path/session/tool; includes the `gate_driver.ts` self-trigger scenario (predicts `g.think`).
   - **U3** (op:drift): assert `count_drift > 0` (direction-b); the known `feature_kb count drift` pre-existing failure must surface.
   - **U6**: hand-craft a stranded red (`state:"red"` at `test_node=A` with no later `green`) → assert `op:state` returns `stranded_red: ["A"]`.
   - **U7**: hand-craft a `done` with `checklist.suite_passes:false` followed by a `skip` with reason `"Override permits..."` → assert `op:state{feature:X}` returns `closed_via_skip:true`. **Cross-feature FP guard**: hand-craft a same-window skip for feature_Y → assert `op:state{feature:X}` does NOT mark X as `closed_via_skip`.
   - **U8**: hand-craft (i) a `phase` record with `feature:""` whose `scope` text contains "feature_024", (ii) a `phase:""` literal record, (iii) 3 `phase` records under 3 distinct slug variants of feature_024 → assert `slug_variants:[...]` length 3+, `empty_slug_records` length 1, `invalid_phase_records` length 1. **Near-collision guard**: hand-craft `feature_004` vs `feature_04` → assert they do NOT match.
   - **U10**: assert `op:state` returns exactly the 6 node IDs in `state.py:132` `KNOWN_SUITE_FAILURES` (parsed from the constant, not hardcoded).
   - **U11**: hand-craft a path in `@var.harness_paths` whose mtime > `omt_harness_e2e_last_run.json`'s `passed_at` → assert `op:plan.receipt_detail.stale === true` + `refresh_tests === ["tests/scripts/omt/test_omt_harness_e2e.py"]`.
   - **U13**: hand-craft a think_consult within the 8h window for file X → assert X in `recent_consults` and NOT in `consult_needed`.
   - **U9**: hand-craft 4 skips with reason stems `live smoke`, `live smoke`, `TDD bootstrap`, `Override permits pre-existing baseline` → assert `op:state` returns `live_smoke_count:2` as a named field PLUS the generic top-3 stem tally (`live smoke: 2`, `TDD bootstrap: 1`, `Override permits…: 1`).
   - **v1.5 envelope**: every golden asserts `as_of_commit === <HEAD-sha>` (parsed live via `git rev-parse HEAD` at call time); two consecutive calls against an unchanged commit return byte-identical `as_of_commit` values.

## Test placement note (decided)

Golden tests land under `tests/scripts/omt/test_omt_q.py` (the harness-level pin-test convention — `tests/scripts/omt/` already houses `test_omt_enforcer_guard_source_pins.py`, `test_omt_harness_e2e.py`, `test_omt_live_opencode_guards.py`, `test_opencode_sdk_contract.py`). This is NOT under `tests/features/feature_026.../` because the harness-level TS-plugin pin tests live with the other harness pin tests (not per-feature directory). The `omt_complete Programming → Testing` exit check looks for `tests/features/<feature>/test_*.py` — this means I'll ALSO need a sentinel `tests/features/feature_026.omt_q_interrogative_first_ops/test_omt_q_golden_smoke.py` (or a single re-export) to satisfy the §12 phase-exit pattern matcher. **Investigate at Programming→Testing transition.**

## Receipt round-robin note (g.receipt guards omt_q.ts edits)

`.opencode/plugins/omt_` is in `@var.harness_paths` → editing `omt_q.ts` triggers `g.receipt`. Each multiedit round needs a fresh `tests/scripts/omt/test_omt_harness_e2e.py` run **before** the second edit to the same harness file (per GOTCHA_RECEIPT_ROUND_ROBIN). The build-sequence bootstrap step (`omt_q{op:plan, gate_driver.ts}`) is the first *interrogation* of this gate by the very tool it predicts. After the plugin lands, the next harness edit to it should pre-flight via `omt_q{op:plan, path:".opencode/plugins/omt_q.ts"}` to see the receipt requirement *before* `g.receipt` blocks.

## Artifacts to read first (in order, for resumption)

1. **WORK.md** (the new line 27 — `[~] feature_026.omt_q_interrogative_first_ops` + pointer to this file).
2. **This file** — `.sandbox/pause_2026-08-09_b.md` (resumption context).
3. **Parent design source-of-truth:** `.projects/meta/meta_harness_2/PROJECT.md` (v1.5, 513 lines — scoped at the section the next-step work covers: §Architecture "Phase-A surface", §Edge cases #1–#11, §Risks #1–#8, §Tasks → Phase-A build, §Ready to start next session, §Success criteria).
4. **Analysis doc:** `.meta/software_development_process/3.analysis/features/feature_026.omt_q_interrogative_first_ops/analysis_001_substrate_rederivation_costs.md` (the substrate inventory + 10 re-derivation costs + live IR fingerprint correction — the corrected 7-before-gate count + the tempplate for design_001).
5. **Mirror template:** `.opencode/plugins/omt_nav.ts` (the exact structure `omt_q.ts` mirrors — `createNavTools()` → `createQTools()`, the four-impl-one-registered-tool pattern, the default export with `initOmtShared(worktree ?? directory)`).
6. **Refactor target:** `.opencode/lib/enforcer/gate_driver.ts` L241-270 (`runBeforeGates`), L39-48 (`GateCtx` interface), L175-207 (`IMPLS` registry), L261-269 (the gate loop), L282-300 (`runAfterGates` — do NOT change in Phase-A).
7. **Behaviour-preservation pins:** `tests/scripts/omt/test_omt_enforcer_guard_source_pins.py` (BUG-A before-hook `output?.args?.filePath` pin — must stay green through the dryRun refactor).
8. **Phase-exit gate source:** `.opencode/lib/enforcer/phase_gate.ts` L31-51 (`PHASE_EXIT_REQUIREMENTS` — Design → Programming needs `design_001_*.md` AND `operation_spec_*.md`); L99-123 (`detectDesignArtifact` — auto-detect for the artifact-check).
9. **Existing major-feature template (format reference):** `.meta/software_development_process/4.design/features/feature_025.coding_context_window_optimization/design_001_deepagent_context_optimization.md` (the design doc shape to mirror — Summary, Problem Analysis, Components, Static Structure, Functional Flow, Operation Specs, Breaking-change risk, Backwards compat, Testing strategy, Open questions/risks, Links).
10. **Existing operation-spec template:** `.meta/software_development_process/4.design/features/feature_025.coding_context_window_optimization/operation_spec_001_deepagent_service_methods.md` (the per-op Pre/Post/Exc contract shape).
11. **Substrate helpers:** `.opencode/lib/omt_shared.ts` — `loadIr`, `readLedger`, `appendLedger`, `readThoughtsIndex`, `loadKbIr`, `omtHarnessE2eStatus`, `gateMsg`, `THOUGHT_PATTERN`, `UNLOCK_WINDOW_MS`, `OMT_HARNESS_E2E_TEST`, `OMT_HARNESS_E2E_COMMAND`. `.opencode/lib/enforcer/session_state.ts` — `getActiveFeaturePhase`, `getActiveUnlock`, `hasNavUnlock`.

### Live IR fingerprint (re-verify at resume — drift = a stop signal)

```bash
wc -l .meta/.omt/ledger.jsonl .meta/.omt/ledger-202608.jsonl .meta/.omt/ledger-202607.jsonl
# EXPECT: ~23 / 415 / 628 (cumulative +ledger churn — keep within ~10 of v1.5)
rg -o '"kind": ?"[^"]+"' .meta/.omt/ledger*.jsonl | sort | uniq -c | sort -rn
# EXPECT: ~517 think_consult / ~192 phase / ~164 skip / ~101 complete / ~80 tdd / ~12 tdd_testlist
jq '.gates | length' .meta/.omt/harness.ir.json    # EXPECT: 9 (7 before + 2 after)
git status                                          # EXPECT: clean tree at resume start
git log --oneline -1                                # EXPECT: 8b384b1 (or later if user committed)
```

## Steps done by the pause workflow itself (track below)

- [x] Step 1 — Read WORK.md, identify in-progress task (feature_025 done line 26; feature_026 was started this session but not yet in WORK.md — Step 5 added it on line 27 below).
- [x] Step 2 — List artifacts + remaining work (Analysis written; Design + operation_spec + Programming build pending — all listed above).
- [x] Step 3 — Write `.sandbox/pause_2026-08-09_b.md` (this file).
- [x] Step 4 — User confirmation (received: "Yes, accurate — update WORK.md and pause").
- [x] Step 5 — Update WORK.md (new line 27 `[~] feature_026.omt_q_interrogative_first_ops — **PAUSED 2026-08-09** mid-Design-phase pre-design-doc` + pointer to this file + Design-status summary + parent-PROJECT.md pointer; inserted between the feature_025 DONE row and the feature_001 pending row). WORK.md edit gated behind `omt_skip{scope:"all", reason:"pause_dev_for_resume_later workflow Step 5 …"}` per AGENTS.md protected-surface rule (reversible + idempotent per workflow Rule 5 — re-running the pause on the same session state matches the same insert-point: the `[~] feature_026` row + pause_2026-08-09_b.md pointer, no duplicates).
- [x] Step 6 — WORK.md updated; session is safe to close. Next resume reads: WORK.md `feature_026` row → this pause doc → `.projects/meta/meta_harness_2/PROJECT.md` §Architecture + §Ready-to-start-next-session → Analysis doc → mirror `omt_nav.ts` + refactor `gate_driver.ts` L241-270.
