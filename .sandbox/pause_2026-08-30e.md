# PAUSE 2026-08-30e — feature_041.resource_places_concurrency (RED complete, GREEN NOT started)

> Resume point for `meta_harness_concurrent` core 3/3. Reversible/idempotent: re-running the pause on this state produces this same file.
> Read FIRST: this file → `.sandbox/pause_2026-08-30d.md` (design R1–R8) → `.projects/meta/meta_harness_concurrent/PROJECT.md` (D1–D18) → `CURRENT_STATE.md` → FEATURE.md (`2.requirements/features/feature_041.resource_places_concurrency/`).
> Supersedes the resume sequence in pause_2026-08-30d.md steps 2+ (step 1 already done THIS session — re-do it: phase + skip are session-scoped).

## Active task

- **feature_041.resource_places_concurrency** — minor_feature, project `meta_harness_concurrent` (core 3/3, the last core feature).
- Ledger state at pause: phases **Analysis ✅ → Design ✅ → Programming active**.
- tests canary skip issued THIS session (expires with it — **re-issue at resume, phase FIRST then skip** per GOTCHA_TESTS_CANARY_SHADOW):
  `omt_phase{task_type:"minor_feature", phase:"Programming", feature:"feature_041.resource_places_concurrency", scope:"implement R1-R8"}` → `omt_skip{scope:"tests"}`.

## Completed so far (this session)

1. Resumed per pause_2026-08-30d.md: phase declared (Programming) + tests canary re-issued; TA consults recorded for all 4 TA-carrying source files (state.py:58, cli.py:36, project.py:69, new_feature.py:70).
2. **RED ✅ — 21 intended failures, runnable RED** (assertion failures, not collection errors):
   - `tests/scripts/omt/test_net_resources.py` (NEW, 18 tests): TestBootstrapResourceCatalog (R1: 5 catalog places M0=1, sorted overlay supervisor places) · TestSubnetAgentAttentionWiring ×7 (R2 template arcs; 2-feature serial-mirror conflict fire cycle; conservation law agent_attention+Σf{N}_active=1; R4 invariant envelope holder/capacity_ok/conflicts + legacy-empty + seeded-active violation; §2.3.2 multi-resource pair conservation) · TestResyncResourceProposal ×4 (R5 ONE add_resource_places entry, retrofit arcs, no-entry-when-present, apply-order+R3 ports.resources refinement) · TestLifecycleSyncHook ×6 (R6 spy/empty-silent/fail-open/unbootstrapped-skip + project.py cmd_new + new_feature.py link integration).
   - `tests/scripts/omt/test_net_sync.py` — 3 evolved pins (R1/R2/R3 blast radius, per pause_d §4): bootstrap place-set+live_marking+overlay supervisor (:91 test), subnet-template arc set +9 arcs (:160 test), ports entry/exit+resources (:230 test). The other 6 sync tests UNTOUCHED and green; splice `resources: []` pins (:184,:400) SURVIVE as designed (p2/p7 not resource places).
   - `tests/features/feature_041.resource_places_concurrency/test_feature_041_sentinel.py` (NEW — subprocess bridge, 039/040 precedent; CANONICAL_SUITE = test_net_resources.py + test_net_sync.py).
   - TA xref added @ test_net_resources.py:54 (R1–R8 → test-class map).
3. Green-at-pause verified: `pytest tests/scripts/omt tests/features/feature_041...` = **322 passed, 22 failed** — failures EXACTLY the 21 RED + the 041 sentinel bridge (expected red until feature completion); test_project_lifecycle.py green; harnessc check 0 err (253 records); **e2e receipt refreshed post-TA (clean for resume)**.

## Immediate next step (resume sequence)

1. Re-issue phase + tests canary (order mandatory — see above).
2. **GREEN** — implement per pause_2026-08-30d.md R1–R8; the tests ARE the spec (envelope shapes, ledger keys, print format all pinned there). Implementation decisions locked THIS session (mechanical apply):
   - `state.py`: `RESOURCE_PLACES = ("agent_attention","src_edit_capacity","tests_capacity","harness_surface_round","e2e_receipt")` after the TA line (:58) · bootstrap adds all 5 M0=1 · `_subnet_mutation` appends claim `("agent_attention",f{N}_start)` + release `(f{N}_complete,"agent_attention")` arcs at END of add_arcs (9 total) · `derive_overlay` ports `resources = sorted((entry | exit_) & set(RESOURCE_PLACES))` · R5 in `sync()`: `missing_resources` (catalog order) + `retrofit_arcs` per `sorted(existing)` subnet (claim if start lacks agent_attention input, release if complete lacks output) → ONE `add_resource_places` proposal entry when either non-empty; net_sync record gains `add_resource_places: <list>` + `retrofit_arcs: <count>` · NEW `resource_report(st)` (holders = subnets with f{N}_active>0 for agent_attention only, else []; capacity_ok = live+held==capacity; conflicts = pending subnets with f{N}_start not enabled, blocked_by = empty UNPREFIXED inputs of start) · NEW `lifecycle_sync_hook(event)` (is_bootstrapped guard → `sync(base, reasoning=f"lifecycle auto-sync ({event})")` inside try/except Exception → print ONE line `omt_net auto-sync ({event}): {n} proposal(s) pending — apply via splice (D4)` ONLY when proposal count > 0).
   - `cli.py` `_invariant`: envelope gains `resources`/`conflicts` keys from `state.resource_report(st)` (additive, after `drift`).
   - `project.py`: `_net_auto_sync(event)` helper (lazy `from net import state`, try/except pass) called after `_sync_all()` in cmd_new("create")/cmd_link("link")/cmd_close("close")/cmd_archive("archive")/cmd_reopen("reopen") — NOT cmd_sync/cmd_backfill (not lifecycle events).
   - `new_feature.py`: after successful `--project` link (rc==0), same lazy-import fail-open hook with event `"new_feature_link"`.
3. **Round discipline (GOTCHA receipt round-robin):** the GREEN edits span 4 harness-surface files × multiple regions → do them via ONE uv-run python transform script in bash (exact-match replace + count asserts), then ONE e2e refresh: `uv run pytest tests/scripts/omt/test_omt_harness_e2e.py -q`.
4. `uv run pytest tests/scripts/omt/test_net_resources.py tests/scripts/omt/test_net_sync.py -q` → 21/21 green → full omt dirs → harnessc build+check (0 err) → drift pins 12/12 → full sentinel (baseline 1756: 1736 + 18 new + 2 sentinel; all green incl. the 041 bridge).
5. Dogfood: `omt_net{op:sync}` on the REAL bundle → proposal gains ONE `add_resource_places` entry (5 places, no retrofit — real net has no subnets at rev 0) → apply via `splice{mode:add}` → **rev 1**; `omt_net{op:invariant}` green (resources[] 5× capacity_ok, conflicts[] empty). Subnet proposals for feature_001/002/041 stay UNAPPLIED (user decision).
6. `6.testing/features/feature_041.../test_report.md` → CURRENT_STATE.md log → WORK.md row → `omt_complete` Testing → Done.

## Blockers / open decisions

- None blocking. **D17 (USER decision at the feature_041 exit review):** promote feature_045 (WORK.md net projection) into the core, or keep phase-2. Surface it at completion — do NOT self-decide.
- Rule-3 note (pause workflow): the RED suite (21 runnable failures) IS the automated verification of what remains; repo verified green-at-pause around it (322 passed, harnessc 0 err, e2e 1/1).

## Artifacts a future session reads first

| Path | Why |
|---|---|
| `.sandbox/pause_2026-08-30e.md` | this file — RED-complete state + locked implementation decisions |
| `.sandbox/pause_2026-08-30d.md` | design R1–R8 + analysis findings (source line refs) |
| `.projects/meta/meta_harness_concurrent/PROJECT.md` | D1–D18 locked decisions, scope guardrails (D1 no src/, D2 no library import, D4 proposals never auto-applied) |
| `.projects/meta/meta_harness_concurrent/CURRENT_STATE.md` | session log |
| `tests/scripts/omt/test_net_resources.py` | **the RED spec — tests pin every GREEN decision** (TA xref :54) |
| `tests/scripts/omt/test_net_sync.py` | the 3 evolved pins (failing until GREEN) |
| `scripts/omt/net/state.py` | GREEN target — TA xref :58 |
| `scripts/omt/net/cli.py` | GREEN target `_invariant` — TA xref :36 |
| `scripts/omt/project.py` / `new_feature.py` | GREEN hook sites — TA xrefs :69/:70 |
| `ideas/idea-002-...md` §2 | resource catalog + serial-mirror semantics (canonical capacities) |

## Pause bookkeeping

- Green-at-pause: 322 passed / 22 failed (21 intended RED + 041 bridge) · harnessc 0 err (253) · e2e 1/1 refreshed post-TA · budgets OK (work_md 6595/7168).
- WORK.md: updated — Tasks row pointer switched pause_2026-08-30d → **pause_2026-08-30e** (RED-complete state; user confirmed the resumption doc before the update, per workflow).
- Uncommitted at pause (by design — no commits requested): test_net_resources.py (new), test_feature_041_sentinel.py (new), test_net_sync.py (3 evolved pins), TA xref in test_net_resources.py, WORK.md row, this pause file.
- **Session is safe to close.**

## Result

Paused at GREEN gate (RED 21/21 failing as designed); resume by reading `.sandbox/pause_2026-08-30e.md` → re-issue phase+skip → implement R1–R8 per the locked decisions above.
