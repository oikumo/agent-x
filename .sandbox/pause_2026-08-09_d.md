# Pause 2026-08-09 (session D) — feature_026.omt_q_interrogative_first_ops

> Result: **paused mid-Step-3 GREEN-fix, 13/14 golden tests green, 1 red remaining (U10 hermetic-root fix). Resume by reading `.sandbox/pause_2026-08-09_d.md` then fix U10 → cleanup debug `catch (e)` block → REFACTOR → `omt_tdd{op:done}` → regression + phase exits.**

## Active task

- **Task id / name:** `feature_026.omt_q_interrogative_first_ops` — major_feature (TDD auto-on).
- **In WORK.md?** Yes — line 27 `[~] feature_026...`. Currently carries the STALE "PAUSED 2026-08-09 (session C) mid-Programming-phase pre-TDD-testlist" copy from pause_c; Step 5 of THIS pause will update it to reflect Step-3-GREEN-fix state + pointer to `.sandbox/pause_2026-08-09_d.md`.
- **OMT phase (current):** Programming (TDD Mode ACTIVE, cycles partially run — 13/14 reds greened).
- **Phase progression this session:** Resumed from pause_c (mid-Programming-phase pre-TDD-testlist) → planted TDD testlist (12 behaviors via direct CLI; the MCP `omt_tdd` wrapper had a quote-escaping issue with embedded parentheses/commas — the CLI path planted the real record, the TDD state machine reads the latest record per feature) → wrote RED golden suite (12 test nodes + sentinel) → wrote GREEN `runBeforeGatesDry` in `gate_driver.ts` (additive, 31/31 behaviour-preservation pins stay green) → wrote GREEN `.opencode/plugins/omt_q.ts` (682 lines) → fixed `buildCtxFromInputs` `state` field bug (was spreading `createSessionState()` at top-level instead of `state: createSessionState()`) → 10→13 tests green → ONE red remaining (U10).

## What this feature is (carry the model in one paragraph)

Ship a read-only `omt_q` TS plugin at `.opencode/plugins/omt_q.ts` with three ops: `op:state{feature?, session?, as_of?}`, `op:plan{path, tool?, session?, as_of?}`, `op:drift{as_of?}`. Every response envelope carries `as_of_commit:"<HEAD-sha>"` (parsed live via `git rev-parse HEAD`, falls back to literal `"HEAD"` when git fails). Single mechanical touch: `gate_driver.runBeforeGates` gained a sibling `runBeforeGatesDry(ctx): Promise<GateDecision[]>` export (synthetic `GateCtx` + catch-and-capture instead of throw; real edits still throw on the existing path — v1 lock preserved, 31/31 pins green). A `kind:"q"` ledger record per call. Parent design source-of-truth: `.projects/meta/meta_harness_2/PROJECT.md` (v1.5, 513 lines).

## Completed so far (this session — adds to the pause_c baseline)

1. **Resumed from pause_c** — IR fingerprint re-verified: ledgers 34/415/628 (1077 total) ✓ · `jq '.gates|length'` = 9 (7 before + 2 after) ✓ · 7 before-gates: g.nav(0), g.protect(10), g.receipt(20), g.tests(30), g.phase(40), g.think(50), g.kb(55) ✓ · KNOWN_SUITE_FAILURES = 6 IDs at `state.py:132` ✓.
2. **TDD testlist planted** — `omt_tdd{op:testlist}` MCP call returned `Expecting value: line 1 column 1 (char 0)` on the 12-behavior JSON array (quoting issue with embedded parentheses/commas at the MCP JSON-RPC layer — the wrapper passes `--behaviors <value>` but the long string with `(),` and double-quotes inside isn't arriving intact). Mitigation: ran the equivalent `uv run scripts/omt/tdd_check.py testlist --behaviors '<JSON array>' --feature feature_026... --session ''` directly via bash — the CLI path planted the real `tdd_testlist` ledger record with all 12 behaviors verbatim (the TDD state machine reads the latest record per feature). A 2-behavior probe record was planted first by accident (testing the CLI mechanics) — it's harmless (the latest record wins) but visible in `tail -3 .meta/.omt/ledger.jsonl`.
3. **RED golden suite written** — `tests/scripts/omt/test_omt_q.py` (681 lines, 14 test nodes across 12 classes: `TestOpStateResumeSnapshot` U1, `TestOpPlanPredictsBeforeChain` U2, `TestOpDriftCountDriftDirectionB` U3, `TestOpStateStrandedRed` U6, `TestOpStateClosedViaSkip` U7 (2 tests: same-feature + cross-feature FP guard), `TestOpStateDecreeHealth` U8, `TestOpStateSkipReasonTally` U9, `TestOpStateKnownSuiteFailuresParse` U10, `TestOpPlanReceiptDetail` U11, `TestOpStateConsultDedup` U13, `TestEnvelopeAsOfCommit` v1.5 (2 tests: matches-HEAD-sha + byte-identical-2-calls), `TestRunBeforeGatesDryDoesNotBreakRealPath` behaviour-preserving). RED state confirmed: 14 skipped (all guarded by `not OMT_Q_PLUGIN.exists()`).
4. **Sentinel re-export written** — `tests/features/feature_026.omt_q_interrogative_first_ops/test_omt_q_golden_smoke.py` (thin `from tests.scripts.omt.test_omt_q import *`-style re-export of constants/helpers/test-classes) — satisfies the `omt_complete{Programming→Testing}` pattern matcher `tests/features/<feature>/test_*.py`. Collection confirmed at BOTH paths (28 tests total with the sentinel duplication).
5. **GREEN `runBeforeGatesDry` written** in `.opencode/lib/enforcer/gate_driver.ts` (additive — added after `runBeforeGates` ends at L270, before the after-gates `// --- after-gates` section). Added `export type GateDecision = {gate_id, blocked, msg, skip_ok}` + `export async function runBeforeGatesDry(ctx: GateCtx): Promise<GateDecision[]>`. The function mirrors `runBeforeGates` exactly (same gate source `(ir?.gates.length ? ir.gates : FALLBACK_GATES).filter(on==="before").sort(order)`, same tools= skip, same when= pre-filter, same `IMPLS[gate.id] ?? genericImpl` dispatch) but wraps `await impl(gate, ctx)` in `try { ... } catch (e) { if (e instanceof OmtBlock) { capture + continue } else { throw e } }` and returns the captured `decisions[]`. `runBeforeGates` body is byte-identical (real edits still throw). All 31 behaviour-preservation pin tests (`test_omt_enforcer_guard_source_pins.py`) stayed green through this edit; the receipt e2e (`test_omt_harness_e2e.py`) stayed green.
6. **GREEN `.opencode/plugins/omt_q.ts` written** (682 lines). Mirrors `omt_nav.ts` factory exactly: `default async ({directory, worktree}) => { initOmtShared(worktree ?? directory); const {omt_q} = createQTools(); return {tool:{omt_q}} }`. Imports `irToolDescription` + `relOf` from `../lib/omt_shared` (NOT from `@opencode-ai/plugin` — that was an initial mistake, fixed on the second edit). The `createQTools()` builds 3 unregistered impls (`omt_state`, `omt_plan`, `omt_drift`) + the ONE registered dispatcher (`omt_q` — `op` switch). Fold helpers inlined: `headSha()`, `parseKnownSuiteFailures(root)`, `tsOf(r)`, `foldStrandedReds`, `foldClosedViaSkip` (cross-feature FP guard via `skip.feature === feature` OR `skip.scope.includes(feature)`), `foldDecreeHealth` (health checks GLOBAL across all phase records; only `phase_cycle_count` narrows to feature — FIX applied this session after U8 first red), `foldSkipReasonTally` (top-3 stems + separate `live_smoke_count`), `foldRecentConsults`, `foldReceiptDetail` (U11 — when path ∈ harness_paths, returns `{receipt_required, file_mtime, receipt_passed_at, stale, refresh_tests, refresh_cmd}`), `foldDrift` (KB-vs-source classification + count_drift direction_b_only:true), `buildCtxFromInputs(args)` (synthetic `GateCtx` + `EnforcerEnv` with `state: createSessionState()` — FIX applied this session after the `env.state.nav` TypeError; was `...createSessionState()` (spread at top-level) instead of `state: createSessionState()`). Every op wraps its return in `envelope: {as_of_commit, op, ...}` and appends a `kind:"q"` ledger record (v1.3 schema + v1.4 `op_set`/`fold_used`/`latency_ms` + v1.5 `as_of:"HEAD"`). All ops fail-open: the outer `catch` returns a minimal envelope so a malformed substrate never blocks a session resume. The plugin's own description contains the literal `"TA:"` — `omt_q{op:plan, path:".opencode/plugins/omt_q.ts"}` predicts BOTH `g.think` AND `g.receipt` on itself (the v1.3 thesis demonstration).
7. **Bug fix `buildCtxFromInputs` state field** — initial spread `...createSessionState()` populated `env` with the SessionState fields at top level instead of nesting them under `env.state`. Caused `TypeError: undefined is not an object (evaluating 'env.state.nav')` inside `runBeforeGatesDry`'s first impl call. Fixed: `state: createSessionState()`. U2 + U11 went green immediately after.
8. **Bug fix `foldDecreeHealth` global vs feature-scoped** — initial filter `records.filter(r => r.kind === "phase" && (!feature || r.feature === feature))` made the empty-slug record (`feature: ""`) invisible when the U8 test queried with `feature:"feature_024.tui_framework"`. Fixed: health-scan pools (slug_variants / empty_slug_records / invalid_phase_records) are GLOBAL across all `kind:"phase"` records; only `phase_cycle_count` narrows to the queried feature (per design §Functional Flow U8). U8 went green after.

## NOT completed so far (the immediate next steps — STRICT ORDER)

### Step 3 GREEN-fix (resume here — ONE red remaining)

**U10 `test_u10_known_suite_failures_from_state_py` failing:**

```
AssertionError: U10 known_suite_failures must equal state.py parse:
  plugin=[]  py=['tests/features/feature_018.../test_react_screen_mounts...',
                 ... 6 IDs total ...]
```

Root cause: the test sets up a hermetic `tmp_path` via `_copy_real_ir(tmp_path)` + `_q_probe(..., use_real_ir=True, tmp_path=tmp_path)`. The probe runs `bun probe.ts <tmp_path>`, and the plugin calls `parseKnownSuiteFailures(repoRoot())` where `repoRoot()` resolves to `tmp_path` (the injected root). `tmp_path/scripts/omt/tdd/state.py` doesn't exist → `parse_failed:true, nodeIds:[]`. The Python cross-check `_parse_ksf_from_state_py()` reads the REAL repo's `STATE_PY = REPO_ROOT / "scripts/omt/tdd/state.py"` → 6 IDs. Mismatch.

Fix (decided, NOT yet applied): U10's contract is "parse the LIVE state.py" — the test should use the REAL repo root for the probe (NOT a hermetic tmp_path). Change the U10 test to call `_q_probe(..., session="ses_u10", use_real_ir=False, tmp_path=REPO_ROOT)` (or add a `use_real_root: bool` parameter to `_q_probe` that passes `REPO_ROOT` instead of `tmp_path` to the bun probe). This is consistent with U10 being a "parse the live state.py" probe — it's NOT a substrate-fold test (U1/U6/U7/U8/U9/U13 use hermetic ledgers; U2/U11 use hermetic IR + copied source files; U10 must use the real repo for state.py). NOTE: the ledger `kind:"q"` records the U10 probe writes will land in `.meta/.omt/ledger.jsonl` (the real repo's ledger) — that's expected churn; the golden does NOT assert against the ledger.

**Cleanup of debug `catch (e)` block in `omt_q.ts` plan op** — the L590 `} catch (e: any) {` currently includes `stack: e?.stack` in the returned envelope (added for the `env.state.nav` debug). Revert to the clean fail-open: `} catch { const envelope = { as_of_commit, op: "plan", path, error: "plan op failed (fail-open)" }; return JSON.stringify(envelope) }`. Same cleanup for the `omt_state` and `omt_drift` catch blocks (they never had the stack — only plan did).

After the fix: `uv run pytest tests/scripts/omt/test_omt_q.py -q` should show `14 passed`.

### Step 4 — REFACTOR

Verify `omt_q.ts` stays < 600 lines (currently 682 — over by 82). Extract fold primitives (`foldStrandedReds`, `foldClosedViaSkip`, `foldDecreeHealth`, `foldSkipReasonTally`, `foldRecentConsults`, `foldReceiptDetail`, `foldDrift`, `buildCtxFromInputs`, `tsOf`, `headSha`, `parseKnownSuiteFailures`) into a local-helper block at the top of `createQTools` (they're already module-level functions — the line count is mostly the 3 op impl bodies). Options: (a) compress comments, (b) move the 3 op impls' envelope-construction + ledger-append into a shared `emitEnvelope()` helper, (c) lift the fold helpers into a sibling `.opencode/lib/omt_q_folds.ts` module (NOT in `@var.harness_paths` so it doesn't trip `g.receipt` on its own — but it would still trip `g.think` if it contains `"TA:"`, which it won't). Recommend (b) — keeps the plugin self-contained. The design doc says "< 600 lines" as a soft target; if compression is hard, document the 82-line overrun in the implementation notes (Step 6) and ship at 682 — the contract is the golden suite + behaviour-preservation, NOT a line count.

`uv run pytest tests/scripts/omt/test_omt_q.py -q` → 14 passed after refactor.

### Step 5 — `omt_tdd{op:done, feature:"feature_026.omt_q_interrogative_first_ops"}`

Once all 14 reds are green AND the refactor pass keeps them green. (NOTE: the MCP `omt_tdd` wrapper had the quoting issue this session — if `omt_tdd{op:done}` returns the same `Expecting value` error, fall back to the direct CLI: `uv run scripts/omt/tdd_check.py done --feature feature_026.omt_q_interrogative_first_ops --session "<this session>"`. The `done` subcommand has no `behaviors` arg so the quoting issue is moot.)

### Step 6 — Regression sweep + exit phases

- `uv run pytest` — full suite (1196+ baseline; KNOWN_SUITE_FAILURES allowlist of 6 IDs unchanged — the U10 test now parses them). NOTE: running the full suite will hit `g.receipt` on `test_omt_harness_e2e.py` edits — run `uv run pytest tests/scripts/omt/test_omt_harness_e2e.py -q` first to refresh the receipt, then the full suite.
- Write implementation notes under `5.implementation/features/feature_026.omt_q_interrogative_first_ops/` (mirror feature_025's structure — a single `implementation_001_*.md` with: what shipped, the 3 ops surface, the runBeforeGatesDry additive refactor, the fold helpers, the golden suite, any line-count overrun, the MCP-omt_tdd-quoting workaround).
- `omt_complete Programming → Testing`.
- `omt_phase Testing` (feature_026).
- Write test report `6.testing/features/feature_026.omt_q_interrogative_first_ops/test_report.md` (mirror feature_025's: suite totals, green count, any known-baseline failures, the receipt-round-robin log).
- `omt_complete Testing → Done`.
- Update WORK.md: rotate `feature_026` from `[~]` to `[x] DONE (2026-08-09)` with a one-line summary + pointer to the test_report (per CONV_WORK_ROTATE).
- Update `.projects/meta/meta_harness_2/CURRENT_STATE.md` (append iter 13 session-log entry — mirror the iter 12 / pause_c entry shape).
- Add `.meta/.omt/kb.ir.json` records for `omt_q` (the new plugin + the new `runBeforeGatesDry` export — feature_kb_akb's AST extractor runs on next commit; no manual action unless the KB drift golden U3 needs them pre-populated).

## Blockers

- _(none)_ — the MCP `omt_tdd{op:testlist}` quoting issue is NOT a blocker (the CLI path planted the real record; the `done` subcommand has no `behaviors` arg). The U10 red is a 2-line test fix (use `REPO_ROOT` instead of hermetic `tmp_path` for the U10 probe). The `stack` debug block is a 3-line revert.

## Substrate-level decisions locked (carried forward from pause_b/pause_c — UNCHANGED this session)

1. **Before-gate count = 7, not 9.** Live `harness.ir.json` has `g.nav, g.protect, g.receipt, g.tests, g.phase, g.think, g.kb` = 7 before-gates. `op:plan` filters dynamically via `gates.filter(on==="before")`.
2. **Plugin mirrors `omt_nav.ts` structure.** Factory + `createQTools()` + default export with `initOmtShared(worktree ?? directory)`.
3. **`gate_driver.runBeforeGates` refactor shape — FINAL.** Sibling `runBeforeGatesDry(ctx): Promise<GateDecision[]>`; the existing `runBeforeGates(env, session, input, output, rawEditPath): Promise<void>` body is byte-identical and throws `OmtBlock`. The dryRun catches `OmtBlock` per-gate, captures, continues. ✅ SHIPPED + 31/31 pins green.
4. **`op:state` field set (locked v1.5 surface):** `phase`, `tdd_position`, `stranded_red`, `closed_via_skip`, `decree_health`, `skip_reason_tally` + `live_smoke_count` (named), `known_suite_failures` + `known_suite_failures_parse_failed`, `recent_consults` + `consult_needed`, `last_activity_ts`, `risky_thoughts[]`. ✅ SHIPPED.
5. **`op:plan` field set:** `predicted_chain[]`, `receipt_detail` (U11), `first_blocker`. ✅ SHIPPED.
6. **`op:drift` field set:** `drift_records[]`, `count_drift` {kb, skeleton, direction_b_only:true}. ✅ SHIPPED.
7. **`as_of_commit` envelope.** Every `omt_q` response wraps in `envelope: {as_of_commit:"<HEAD-sha>", op, ...}` — Phase-A stub parses `git rev-parse HEAD` live per call; falls back to `"HEAD"` on git failure (e.g. hermetic tmp root with no .git). ✅ SHIPPED (U2/U11 golden under tmp_path returns `"HEAD"`; direct probe against real repo returns the 40-char sha).
8. **`kind:"q"` ledger record.** Per `omt_q` invocation: `op`, `feature?`, `session`, `ts` + `op_set`, `fold_used`, `latency_ms` + `as_of:"HEAD"`. ✅ SHIPPED.
9. **Golden tests under `tests/scripts/omt/test_omt_q.py`** + sentinel re-export under `tests/features/feature_026.omt_q_interrogative_first_ops/test_omt_q_golden_smoke.py`. ✅ SHIPPED.

## Test placement note (carried from pause_c — confirmed this session)

`tests/scripts/omt/test_omt_q.py` is the canonical golden-suite location (14 tests, 12 classes). The sentinel is a thin re-export. Both paths collect (28 tests total with duplication) — `omt_complete{Programming→Testing}` will match on the per-feature dir sentinel.

## Receipt round-robin log (this session)

- Edited `omt_q.ts` (NEW) once — no receipt needed for the first edit (file didn't exist).
- Edited `gate_driver.ts` (in `@var.harness_paths`) once — ran `uv run pytest tests/scripts/omt/test_omt_harness_e2e.py -q` BEFORE the (potential) second edit; only made ONE edit so no second-edit guard fired. 31/31 pins stayed green.
- Edited `omt_q.ts` (now in `@var.harness_paths` via the `.opencode/plugins/omt_` prefix) many times (imports fix, state-fix, decree-health fix, debug catch, regex fix) — EACH multi-edit round needed a fresh `uv run pytest tests/scripts/omt/test_omt_harness_e2e.py -q` run BEFORE the second edit to the same file. The receipt was refreshed ~7 times this session. The e2e test itself is receipt-EXEMPT.
- Edited `tests/scripts/omt/test_omt_q.py` (in `@var.harness_paths` via the `tests/scripts/omt/` prefix) twice — same receipt-round-robin rule.

## Artifacts to read first (in order, for resumption)

1. **WORK.md** (the `[~] feature_026` row — line 27; pointer will point to `.sandbox/pause_2026-08-09_d.md` after Step 5).
2. **This file** — `.sandbox/pause_2026-08-09_d.md` (resumption context — you are here).
3. **Parent design source-of truth:** `.projects/meta/meta_harness_2/PROJECT.md` (v1.5, 513 lines).
4. **Design doc:** `.meta/software_development_process/4.design/features/feature_026.omt_q_interrogative_first_ops/design_001_omt_q_first_ops.md` (~190 lines — §Static Structure + §Functional Flow + §Testing strategy are the build instructions).
5. **Operation spec:** `.meta/software_development_process/4.design/features/feature_026.omt_q_interrogative_first_ops/operation_spec_001_omt_q_ops.md` (~180 lines — per-op Pre/Post/Exc).
6. **Pause_c (prior session context):** `.sandbox/pause_2026-08-09_c.md` (the pause that this session resumed from — has the 12-behavior JSON array verbatim + the wrapper-bug investigation).
7. **GREEN source (shipped this session):**
   - `.opencode/plugins/omt_q.ts` (682 lines — the 3-op plugin + fold helpers + envelope + ledger).
   - `.opencode/lib/enforcer/gate_driver.ts` (385 lines — the additive `runBeforeGatesDry` + `GateDecision` export at L272-309; `runBeforeGates` L241-270 byte-identical, `IMPLS` L175-207 untouched, `FALLBACK_GATES` L227-235 untouched).
8. **RED source (shipped this session):**
   - `tests/scripts/omt/test_omt_q.py` (681 lines — 14 golden tests across 12 classes).
   - `tests/features/feature_026.omt_q_interrogative_first_ops/test_omt_q_golden_smoke.py` (sentinel re-export).
9. **Mirror template:** `.opencode/plugins/omt_nav.ts` (the exact structure `omt_q.ts` mirrors).
10. **Behaviour-preservation pins:** `tests/scripts/omt/test_omt_enforcer_guard_source_pins.py` (31/31 stayed green through the additive refactor).
11. **The failing U10 test:** `tests/scripts/omt/test_omt_q.py::TestOpStateKnownSuiteFailuresParse::test_u10_known_suite_failures_from_state_py` at line 448. The hermetic `tmp_path` needs to become `REPO_ROOT` for this one test (or add a `use_real_root` parameter to `_q_probe`).

### Live IR fingerprint (re-verify at resume — drift = a stop signal)

```bash
wc -l .meta/.omt/ledger.jsonl .meta/.omt/ledger-202608.jsonl .meta/.omt/ledger-202607.jsonl
# EXPECT: ~50-60 / 415 / 628 (hot churn from this session's testlist + 2 probe + multiple kind:q debug records; archive stable)
rg -o '"kind": ?"[^"]+"' .meta/.omt/ledger*.jsonl | sort | uniq -c | sort -rn
# EXPECT: think_consult ~517 / phase ~193 / skip ~164 / complete ~101 / tdd ~80 / tdd_testlist ~14 / q ~10-20 (NEW kind from this session's probes)
jq '.gates | length' .meta/.omt/harness.ir.json    # EXPECT: 9 (7 before + 2 after)
git log --oneline -1                                # EXPECT: 1d68b14 [WIP] feature_026 (or later if user committed)
```

### Current git state (snapshot for resumption sanity)

```
M .opencode/lib/enforcer/gate_driver.ts   (additive runBeforeGatesDry + GateDecision)
?? .opencode/plugins/omt_q.ts              (new, 682 lines)
?? tests/scripts/omt/test_omt_q.py         (new, 681 lines, 14 tests)
?? tests/features/feature_026.omt_q_interrogative_first_ops/  (sentinel re-export)
M .workflows/agentx/loops/feature_deep_review.md  (an unrelated staged change — NOT this feature; leave alone)
```

Last commit: `1d68b14 [WIP] feature_026` (the user committed session C's design docs; this session's GREEN source is uncommitted).

## Steps done by the pause workflow itself (track below)

- [x] Step 1 — Read WORK.md, identify in-progress task (feature_026 at line 27 — persisted stale pause_c copy; will be updated in Step 5).
- [x] Step 2 — List artifacts + remaining work (GREEN source shipped this session captured above; remaining work = U10 fix + debug-catch cleanup + REFACTOR + `omt_tdd{op:done}` + regression + phase exits + WORK.md close).
- [x] Step 3 — Write `.sandbox/pause_2026-08-09_d.md` (this file).
- [x] Step 4 — User confirmation (received: "continue" after the pause-workflow call — interpreted as: pause now, this is the pause workflow invocation).
- [x] Step 5 — Update WORK.md (rewrote line 27 `[~] feature_026.omt_q_interrogative_first_ops` from the STALE pause_c "mid-Programming-phase pre-TDD-testlist" copy to the current "PAUSED 2026-08-09 (session D) mid-Step-3-GREEN-fix: 13/14 golden tests green, 1 red remaining (U10 hermetic-root fix); GREEN source shipped: gate_driver.ts runBeforeGatesDry (additive, 31/31 pins green) + omt_q.ts (682 lines, 3 ops + envelope + ledger); bugs fixed this session: buildCtxFromInputs state field + foldDecreeHealth global-vs-feature-scope; next = fix U10 (use REPO_ROOT not tmp_path) + cleanup debug catch + REFACTOR + omt_tdd done + regression. Resume by reading .sandbox/pause_2026-08-09_d.md"). WORK.md edit gated behind `omt_skip{scope:"all", reason:"pause_dev_for_resume_later workflow Step 5 …"}` per AGENTS.md protected-surface rule (reversible + idempotent per workflow Rule 5: the line 27 `[~] feature_026` row is uniquely identifiable — re-running this pause on the same state rewrites the same row, not duplicates).
- [x] Step 6 — WORK.md updated; session safe to close. Next resume reads: WORK.md `feature_026` row → this pause doc → pause_c (prior session, has the 12-behavior JSON) → parent PROJECT.md §Architecture → design_001 + operation_spec → GREEN source (omt_q.ts + gate_driver.ts runBeforeGatesDry) → RED source (test_omt_q.py U10 line 448).
