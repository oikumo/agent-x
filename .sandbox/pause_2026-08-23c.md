# PAUSE 2026-08-23 (c) — feature_034.studio_v1_editor, late-Programming (state+UI done, build+report remaining)

> Resumption document (pause_dev_for_resume_later workflow). Reversible/idempotent: re-running the pause on this state reproduces this file.
> Supersedes `.sandbox/pause_2026-08-23b.md` (consumed at resume) — third pause of the same feature, further along.
> **Status: session safe to close** (WORK.md updated 2026-08-23 — see §Housekeeping).

---

## Active task

- **feature_034.studio_v1_editor** — petri_net_studio roadmap feature #3 (major_feature, scope LOCKED v1.1).
  State: **Programming phase, state layer + UI COMPLETE and verified; only `npm run build` + dist verification + test report + `omt_complete` remain.**
  Phases: Analysis ✅ (analysis_001) · Design ✅ (design_001 + operation_spec_001) · Programming ⏸ (declared; Vitest/omt_tdd mismatch declared in scope per design A11 — manual red→green, evidence below).

## Completed so far (this session, since resume)

1. **Un-paused** — pause_2026-08-23b consumed; all resume artifacts re-read (WORK.md, CURRENT_STATE.md, design_001 §7/§9.3/§10, operation_spec_001, engine exports, examples).
2. **State layer COMPLETE** — `src/state/document.ts` + `src/state/store.ts` + `src/examples.ts`:
   - **Cycle 3 (store)**: RED `npx vitest run tests/state/store.test.ts` → `Test Files 1 failed (1) · Tests 46 failed | 1 passed (47)` (stub "not implemented (RED stub)") → GREEN full suite **`Test Files 4 passed (4) · Tests 169 passed (169)`** (122 engine + 47 store).
   - document.ts: NetDocument (format-shaped, A8) + pure ops — addPlace/addTransition (first-free auto-name across P∪T, V1-safe), removeNode (cascade), addArc (V2/V3/V4-checked, weight 1), removeArc, renameNode (V1 collision-checked, arcs rewired), setTokens (int ≥0), setWeight (int ≥1), `toNet` memoized WeakMap-by-identity. Fallible ops return `{doc, ok, reason?}`; rejection = input identity unchanged.
   - store.ts (zustand): doc/positions/mode/marking/selection/importError + all op-spec actions; **simulate lock is store-level** (A9, all mutating actions guard); setMode simulate snapshots `initialMarkingTuple()`, edit discards; fireTransition = pure `fireMarking` via memoized net, disabled/out-of-mode = no-op; importJson → docFromNet + positionsFromLayout (layout.nodes verbatim; missing → deterministic circle r=120 `AUTO_LAYOUT_RADIUS`), error → `importError = "<Class>: <message>"` state untouched, success → edit mode + cleared error; exportJson = `netToJson(toNet(doc), {nodes: snapped positions})`; loadExample via `src/examples.ts` (?raw imports — works under Vitest, no fs.allow issue).
3. **UI layer COMPLETE** — `src/ui/{flow,PlaceNode,TransitionNode,Inspector,App}.tsx/ts` + `src/main.tsx` + `src/styles.css`: pure `toFlowGraph` projection (live tokens edit=M0/simulate=marking; enabled flag; edge id = arcId JSON; weight labels always), circle PlaceNode, TransitionNode (enabled=green glow/disabled=muted), Inspector (rename/tokens/weight/delete, commit-on-blur/Enter, edit-mode only), App (toolbar: mode toggle, palette +Place/+Transition arm-then-click, Examples select, Import/Export dialogs, Reset simulate-only; controlled React Flow THROUGH the store; V3 connect rejection → transient hint). **No component tests — design §10 step 6: UI covered by store tests + build.**
4. **Independence tooling COMPLETE** — `scripts/check-independence.mjs` (design §8: specifier scan; BANNED agentx|scripts/|.meta/|.projects/|tests/; relative-escape check; allowlist substring `shared/petri-net/examples/`) + `tests/independence.test.ts` (spawns it so `npm test` covers it; node-env docblock):
   - **Cycle 4 (independence)**: RED `Tests 1 failed (1)` (script missing) → script written → GREEN.
   - `npm run check-independence` → `independence OK: 12 files scanned, 36 imports checked`.
5. **Build breakers found + fixed** (first-ever `tsc --noEmit`, was pending as part of `npm run build`): io.ts `validateLayoutShape` return widened to JsonObject (TA-consulted edit); store.ts Point→JsonObject fresh-literal in exportJson; `@types/node` added (devDep) + tsconfig `types: ["vite/client", "node"]` (engine tests used node:fs since cycle 2 — tsc had never run); check-independence.mjs docstring contained `src/**/*.ts` whose `**/` closed the block comment → SyntaxError (TA-pinned).
6. **TA thoughts pinned (5)**: check-independence.mjs (`**/`-in-comment gotcha), document.ts (addArc design-gap why), store.ts (store-level lock + partial-merge reset why), tests/independence.test.ts (node-env gotcha), App.tsx (controlled-through-store why).
7. **Final tree verification after all edits + TA inserts**: `npx vitest run` → **170/170 passed (5 files)** · `npx tsc --noEmit` → clean · `node scripts/check-independence.mjs` → OK (12 files, 36 imports).

## Test-run evidence (design §9.4 protocol — copy into the test report)

- **Cycle 3 RED**: `npx vitest run tests/state/store.test.ts` → `Test Files 1 failed (1) · Tests 46 failed | 1 passed (47)` (stub "not implemented (RED stub)"; passer = initial-state test).
- **Cycle 3 GREEN (full suite)**: `npx vitest run` → `Test Files 4 passed (4) · Tests 169 passed (169)` (Duration ~1.1s).
- **Cycle 4 RED**: `npx vitest run tests/independence.test.ts` → `Test Files 1 failed (1) · Tests 1 failed (1)` (spawn ENOENT — script not yet written).
- **Cycle 4 GREEN (full suite)**: `npx vitest run` → `Test Files 5 passed (5) · Tests 170 passed (170)` (Duration ~1.2s).
- **Independence CLI**: `node scripts/check-independence.mjs` → `independence OK: 12 files scanned, 36 imports checked`, exit 0.
- **Typecheck**: `npx tsc --noEmit` → clean (exit 0) after the 3 fixes in §Completed #5.
- (Cycles 1–2 engine evidence: see pause_2026-08-23b.md §Test-run evidence — paste both sets into the report.)

## Immediate next step (resume here)

Continue design_001 §10 from step 7 — **build + report only** (everything else is done and verified):

1. `npm run build` (= `tsc --noEmit && vite build`) → static `dist/`; verify `dist/index.html` + assets exist; smoke-serve (`npx vite preview` + curl 200 on `/` and a hashed asset; shared examples are bundled via ?raw, no runtime fs needed).
2. Test report `.meta/software_development_process/6.testing/features/feature_034.studio_v1_editor/test_report.md` — paste ALL RED/GREEN evidence (cycles 1–2 from pause_2026-08-23b.md + cycles 3–4 above) + independence + tsc + build outputs; record the two design-gap resolutions (below).
3. FEATURE.md/PLAN.md checkboxes → `omt_complete{feature:"feature_034.studio_v1_editor", advance_to:"Testing"}` → Done. (Budgets: work_md + scratchpad were FULL at last pause — rotate WORK_ARCHIVE, don't grow; scratchpad: replace the feature_034 scratchpad line only at final Done rotation per convention.)

## Design-gap resolutions (Programming-time, consistent with pins — record in the test report)

- **addArc op**: operation_spec_001's document.ts table omits an arc-creation op, but design §7 pins the connect-drag gesture → `addArc(doc, source, target)` added to document.ts (V2/V3/V4-checked, weight 1) so ALL doc mutation stays in pure ops (A8). TA-pinned in document.ts.
- **Import ⇒ edit mode**: op spec silent on mode after import → import lands in `edit` with marking cleared (fresh import = editing); store-level simulate lock (A9 "structure locked") implemented in every mutating action (toolbar hint alone insufficient).
- Minor: positions snap to integers on WRITE (add/move; design §7 "drag moves snapped to integers"); selection cleared on mode switch + node/arc removal; `loadExample` unknown name → `importError`, returns false.

## Blockers

- None. (Suite 170/170 green; tsc clean; independence OK; npm install works — @types/node added this session.)

## Known caveats recorded (no action needed; already TA-pinned / docstringed)

- io.ts: A2 integer-like-key reorder caveat (layout extension values); integral-float acceptance diverges from Python strict-int (schema-correct; outside pinned matrix). FORMAT.md §8.5 UTF-16≡code-point claim = erratum candidate (spec LOCKED; surfaced, no edit).
- jsdom rewrites `import.meta.url` → node-env docblock required in engine tests + tests/independence.test.ts (TA-pinned both places).

## Artifacts to read first on resume (in order)

1. `WORK.md` (task row `[~]` paused → this file)
2. `.projects/meta/petri_net_studio/CURRENT_STATE.md` (pause entry on top)
3. This file's §Immediate next step + §Test-run evidence (report content) + `.sandbox/pause_2026-08-23b.md` §Test-run evidence (cycles 1–2)
4. `tools/petri-net-studio/` — complete app (engine + state + ui + scripts); design §10 step 7–8 is the only remaining sequence
5. `.meta/software_development_process/2.requirements/features/feature_034.studio_v1_editor/FEATURE.md` (status row) + PLAN.md — fill at the end

## Housekeeping done at pause (this session)

- Tree left verified-working: **170/170 Vitest, tsc clean, independence OK** (re-run after TA inserts — no stub, no /tmp dependency).
- TA: 5 thoughts pinned (see §Completed #6).
- `WORK.md`: feature_034 row updated to `[~]` PAUSED → pointer to this file (budget-tight: in-place row rewrite, scratchpad untouched).
- `.projects/meta/petri_net_studio/CURRENT_STATE.md`: pause entry added on top; FEATURE.md status line → paused-late-Programming (build+report remaining).
- No `src/` (agentx) edits at any point in this feature; agentx suite untouched (1639 green at feature_033 completion).
