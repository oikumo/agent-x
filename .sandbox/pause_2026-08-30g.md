# PAUSE 2026-08-30g — feature_046.omt_net_session_arg_whitelist (RED confirmed; fix NOT applied)

> Resume point for the meta_harness_concurrent exit-review batch. Reversible/idempotent: re-running the pause on this state produces this same file.
> Read FIRST: this file → `.projects/meta/meta_harness_concurrent/PROJECT.md` (D1–D18) → `CURRENT_STATE.md` → `.sandbox/pause_2026-08-30f.md` (feature_041 history, now DONE).

## Active task

- **feature_046.omt_net_session_arg_whitelist** — bug_fix, project `meta_harness_concurrent`.
- Bug (TA gotcha @ `.opencode/plugins/omt_net.ts:43`): the proxy appends `--session <context.sessionID>` to EVERY op's argv, but the CLI `probe`/`invariant`/`synthesize` subparsers declare no `--session` → argparse exit 2 → `omt_net{op:probe|invariant}` via the plugin ALWAYS fails (latent since feature_039; CLI path `net_check.py` is green).
- Ledger state at pause: phase **Programming** declared + tests canary issued (**both expire with this session — re-issue at resume, phase FIRST then skip**, GOTCHA_TESTS_CANARY_SHADOW).

## Completed so far (this session)

1. **feature_041.resource_places_concurrency wrap-up → DONE ✅** (test_report.md, FEATURE.md [x], CURRENT_STATE log ×2, WORK.md DONE row + feature_036 rotated to WORK_ARCHIVE.md, `omt_complete` Testing→Done, D7 hook silent=green). Battery re-run fresh: sentinel **1756 passed**, drift pins 12/12, e2e 1/1, harnessc 0 err (253 records).
2. **User decisions (exit review, all approved):** (a) **D17 RESOLVED — promote feature_045 into the core** (core = 039/040/041/045; 045 is the next feature); (b) **fix the omt_net.ts bug now** as feature_046 bug_fix; (c) **apply pending subnet proposals** to the real net.
3. **Real net: 41 subnets applied ✅** — sync → merged-mutation `splice{mode:add}` (123 places / 82 transitions / 369 arcs, one atomic transaction) → **rev 2**, drift-free (net=ledger=2), invariants hold, 5/5 resources capacity_ok, conflicts []; probe shows done features 037–041 materialized `f{N}_done`=1, 36 pending starts enabled competing for `agent_attention`=1 (conflict trap as designed).
4. **Scaffolded 042–046 ✅** (number reservation for the locked roadmap — auto-numbering, no --number flag): `feature_042.goal_net_synthesis` (minor), `feature_043.meta_net_dashboard` (major), `feature_044.mined_behavioral_net` (minor), `feature_045.work_md_net_driven` (minor, promoted core 4/4), `feature_046.omt_net_session_arg_whitelist` (bug_fix) — all `--project meta_harness_concurrent` (lifecycle hook fired proposal-only lines, nothing applied).
5. **feature_046 RED ✅** — `tests/scripts/omt/test_omt_net_plugin_args.py` written (cross-source pin: parses `OP_ARGS` whitelist out of the .ts, asserts SUBSET of the per-op flags parsed from `cli.py` subparsers + explicit no-session for probe/invariant/synthesize + session kept for fire/splice/sync + loop-over-whitelist construct pin). **RED confirmed: 5/5 fail** for the right reason (`OP_ARGS` absent). TA/think consult for omt_net.ts recorded.

## Immediate next step (resume sequence)

1. `omt_phase{task_type:"bug_fix", phase:"Programming", feature:"feature_046.omt_net_session_arg_whitelist", scope:"per-op argv whitelist in omt_net.ts"}` → THEN `omt_skip{scope:"tests"}` (order mandatory).
2. **Establish the post-scaffold baseline FIRST** (5 new feature dirs + `.projects/meta/META.md` changed AFTER the last green battery): `uv run pytest tests/scripts/omt/test_omt_docs_drift_pins.py tests/scripts/omt/test_omt_harness_e2e.py -q` (also refreshes the STALE e2e receipt — the new pin-test file is an unverified harness-surface creation; its 2nd edit was gate-blocked at pause) + `uv run scripts/omt/harnessc.py check`. If a drift pin trips on the new scaffolds, fix the pin/doc per its message BEFORE proceeding.
3. Fix the LSP narrowing nit in `tests/scripts/omt/test_omt_net_plugin_args.py` (`_cli_flags_by_op`): replace `current: str | None = None` + `flags.setdefault(current, set())` + `assert current is not None` with `current = ""` + `flags[current] = set()` + `assert current, ...` (exact diff was gate-blocked at pause — apply after step 2's receipt).
4. Re-run the pin file → still 5/5 RED (proves the fix, not the test, changes state).
5. **GREEN — ONE edit to `.opencode/plugins/omt_net.ts`** (harness surface; think-gate consult already recorded, re-list at resume): introduce `const OP_ARGS: Record<string, readonly string[]>` = probe:[] · fire:[transition,reasoning,session] · splice:[mode,mutation,subnet,reasoning,session,feature] · sync:[reasoning,session] · invariant:[] · synthesize:[]; argv loop becomes `for (const k of OP_ARGS[op])` (session fallback stays INSIDE the loop); scope max_states: `if (op === "probe" && args?.max_states ...)`. Remove/refresh the TA gotcha @ :43 (bug fixed → convert to a why-note pointing at this feature). Update the pin file in the same round: +1 test pinning `op === "probe"` on the max_states line (regex) → 6 pins total.
6. Battery: pin file 6/6 → `pytest tests/scripts/omt tests/features/feature_046.omt_net_session_arg_whitelist` (no sentinel bridge needed for bug_fix — skip creating one unless a pin says otherwise) → e2e refresh (omt_net.ts edit) → drift pins 12/12 (schema/budgets untouched) → harnessc build+check → **full sentinel (expect 1756+6=1762)**.
7. Wrap 046: FEATURE.md `[x] Done (2026-08-30)`; WORK.md DONE row (**5-DONE rotation: rotate feature_037 to WORK_ARCHIVE.md**); `omt_complete` → Testing → Done; `project.py log meta_harness_concurrent`.
8. **D17 bookkeeping in PROJECT.md** (not done at pause): append resolution note to the D17 decisions-log entry (user-approved 2026-08-30: feature_045 PROMOTED, core = 039/040/041/045); roadmap table row 7 → core/promoted; "Core = features 1–3" line → 1–3 + 7; Status checklist + Quick Start refresh (net rev 2; 045 next).
9. **WORK.md pending rows** (not done at pause): `[ ] feature_045.work_md_net_driven` (promoted core 4/4 — NEXT feature, scope per IDEA-005); `[ ]` rows for 042/043/044 (optional phase-2, scaffolded, unscoped). Remove the 046 `[~]` row when writing its DONE row (step 7).
10. **Apply 042–046 subnets** (covered by the user's apply-approved decision): sync → merge the 5 `add_subnets` mutations (same recipe as step 3 of "Completed") → one `splice{mode:add}` → invariant green (expect rev 3).
11. Final: full sentinel + harnessc check; update PROJECT.md Quick Start "Next:" (feature_045 implementation is the next session's work).

## Blockers / open decisions

- None blocking. Receipt gate: the pin-test file's 2nd edit was blocked at pause (stale e2e receipt) — step 2 unblocks it.
- feature_045 scope/design: IDEA-005 doc + `ideas/WORK.md.net-driven-example` are the design basis; D4 (proposal-only) applies to md→net direction.

## Artifacts a future session reads first

| Path | Why |
|---|---|
| `.sandbox/pause_2026-08-30g.md` | this file |
| `tests/scripts/omt/test_omt_net_plugin_args.py` | the RED spec (5 pins, cross-source) |
| `.opencode/plugins/omt_net.ts` | fix target; TA gotcha :43 |
| `scripts/omt/net/cli.py` | subparser flag authority the whitelist mirrors |
| `.projects/meta/meta_harness_concurrent/PROJECT.md` | D1–D18; D17 resolution bookkeeping pending |
| `.projects/meta/meta_harness_concurrent/ideas/idea-005-work-md-net-driven-concurrency.md` | feature_045 design basis |
| `.meta/.omt/META_NET.petri.json` + sidecar + overlay | REAL SSOT at **rev 2** (41 subnets + resource catalog), drift-free |

## Pause bookkeeping

- Green-at-pause: full sentinel **1756 passed** + pin file **5 failed (INTENDED RED, not regressions)** · drift pins 12/12 + harnessc 0 err **pre-scaffold** (re-verify at resume step 2) · e2e receipt STALE (pin-test file creation unverified).
- Real net: **rev 2** (41 subnets applied; 042–046 subnets pending proposal — approved to apply at resume).
- Uncommitted at pause (by design — no commits requested): feature_041 wrap-up files (test_report.md new, FEATURE.md, WORK.md, WORK_ARCHIVE.md, CURRENT_STATE.md, PROJECT.md), 5 scaffold dirs (042–046), `.projects/meta/META.md` (auto-manifest), `tests/scripts/omt/test_omt_net_plugin_args.py`, this pause file.
- WORK.md: **updated** — feature_046 `[~]` PAUSED row added pointing here (user confirmed this resumption doc before the update, per workflow).
- **Session is safe to close.**

## Result

Paused at feature_046 RED-confirmed (fix spec locked, receipt-gate documented); resume by reading `.sandbox/pause_2026-08-30g.md` → re-issue phase+canary → post-scaffold baseline + e2e receipt → narrowing fix → omt_net.ts whitelist (one edit) → battery → 046 DONE → D17/WORK.md bookkeeping → apply 042–046 subnets → feature_045 next.
