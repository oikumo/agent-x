# PAUSE 2026-08-23 (b) — feature_034.studio_v1_editor, mid-Programming (engine GREEN, UI not started)

> Resumption document (pause_dev_for_resume_later workflow). Reversible/idempotent: re-running the pause on this state reproduces this file.
> Supersedes nothing — the earlier `.sandbox/pause_2026-08-23.md` (scaffold-state pause) was consumed at resume; this is the second pause of the same feature, further along.
> **Status: session safe to close** (WORK.md updated 2026-08-23 — see §Housekeeping).

---

## Active task

- **feature_034.studio_v1_editor** — petri_net_studio roadmap feature #3 (major_feature, scope LOCKED v1.1).
  State: **Programming phase, engine port COMPLETE and GREEN; UI/independence/build/report remaining.**
  Phases: Analysis ✅ (analysis_001) · Design ✅ (design_001 + operation_spec_001; `omt_phase` Design recorded) · Programming ⏸ (declared; Vitest/omt_tdd mismatch declared in scope per design A11 — manual red→green, evidence below).

## Completed so far (this session, since resume)

1. **Un-paused** — resume doc consumed; WORK.md/FEATURE.md/CURRENT_STATE.md flipped to active.
2. **Analysis doc** — `.meta/software_development_process/3.analysis/features/feature_034.studio_v1_editor/analysis_001_port_sources.md`: port matrices (60 model + 59 io behaviors + 3 golden examples), findings A1–A12.
3. **Design doc + op spec** — `.meta/.../4.design/features/feature_034.studio_v1_editor/design_001_studio_v1_editor.md` (A-findings → PINs, repo layout, API pins, store/UI design, test plan, programming sequence) + `operation_spec_001_studio_v1_editor_ops.md` (required by the Design→Programming gate).
4. **`omt_phase` Design + Programming recorded** (major_feature; Programming scope declares the Vitest runner mismatch — `omt_tdd` NOT used by design; manual red→green with pasted evidence instead).
5. **Harness**: `tools` added to `@var root_allowlist` (META_HARNESS.omt + `harnessc.py build`; check 0 errors) — was design finding A10.
6. **Scaffold** — `tools/petri-net-studio/`: package.json (react 18, @xyflow/react 12, zustand 5, vite 5, vitest 2, jsdom, testing-library, **ajv 8** dev-only for schema cross-checks), tsconfig (strict), vite.config.ts (`server.fs.allow: ["../.."]` for shared/ ?raw imports; test env jsdom default), index.html, .gitignore. `npm install` done (197+5 pkgs; network fine).
7. **ENGINE PORT COMPLETE** — `src/engine/{errors,model,io}.ts`:
   - **Cycle 1 (model)**: RED 60/60 failed (stub) → GREEN **60/60 passed**.
   - **Cycle 2 (io)**: RED 58 failed | 4 passed of 62 (stub; 4 passers = 3 schema-example validations + subclass test) → GREEN: full suite **122/122 passed** (60 model + 59 io + 3 examples-golden).
   - Golden bytes verified: TS `netToJson(documentFromJson(example))` === file bytes for all 3 `shared/petri-net/examples/` (cross-impl byte-identity WITHOUT Python).
   - ajv Draft-2020-12 cross-checks pass (3 examples schema-valid; 5 bad docs rejected by both schema and io).
8. **TA thoughts** pinned in `io.ts` (unary-`**` + integral-float caveats), `model.ts` (code-point sort rationale), `tests/engine/io.test.ts` (node-env requirement).

## Test-run evidence (design §9.4 protocol — copy into the test report)

- **Cycle 1 RED**: `npx vitest run tests/engine/model.test.ts` → `Test Files 1 failed (1) · Tests 60 failed (60)` (stub "not implemented (RED stub)").
- **Cycle 1 GREEN**: same cmd → `Test Files 1 passed (1) · Tests 60 passed (60)` (Duration ~0.9s).
- **Cycle 2 RED**: `npx vitest run tests/engine/io.test.ts tests/engine/examples.test.ts` → `Test Files 2 failed (2) · Tests 58 failed | 4 passed (62)`.
- **Cycle 2 GREEN (full suite)**: `npx vitest run` → `Test Files 3 passed (3) · Tests 122 passed (122)` (Duration ~0.7s).
- **Environment fix during cycle 2 RED**: jsdom env rewrites `import.meta.url` to http://… → `fs.readFileSync(new URL(...))` fails "URL must be of scheme file"; fixed by `// @vitest-environment node` docblock atop all 3 engine test files (TA-pinned).
- **First GREEN attempt failed collection**: `-10 ** 15` (Python-ism; JS forbids unary minus before `**`) → fixed to `-1e15` (TA-pinned in io.ts). Never skip the GREEN run.

## Immediate next step (resume here)

Continue design_001 §10 sequence from step 6 — **the UI layer** (engine is done):

1. `src/state/document.ts` — `NetDocument` (format-shaped editable model) + pure ops: addPlace/addTransition (auto-name p1../t1.. first-free), removeNode (cascade arcs), removeArc, renameNode (V1 collision-checked), setTokens (≥0), setWeight (≥1), `toNet(doc)` memoized via io.ts `buildNet` (design §7 + operation_spec; document-model-first = design A8).
2. `src/state/store.ts` — zustand: doc/positions/mode(edit|simulate)/marking/selection/importError + actions (setMode snapshots M0 into marking; fireTransition via engine fireMarking; resetMarking; importJson → documentFromJson → doc+positions from layout.nodes, missing → circle auto-layout r=120, error → importError string state unchanged; exportJson → netToJson(toNet(doc), {nodes: integer positions}); loadExample).
3. `tests/state/store.test.ts` — smoke per design §9.3 (ops validity, mode transitions, fire/reset, import-error integrity, export→import round-trip). Same manual red→green (RED output → test report).
4. UI: `src/ui/flow.ts` (doc+marking+positions → React Flow nodes/edges, pure), `PlaceNode` (circle, name+tokens), `TransitionNode` (enabled highlight in simulate), `Inspector` (rename/tokens/weight/delete; edit-mode only), `App` (toolbar: mode toggle, palette +place/+transition, import/export dialogs, examples menu, reset), `examples.ts` (?raw imports of the 3 shared examples), `main.tsx`, `styles.css`. React Flow: connect drag → arc weight 1, reject P→P/T→T (V3); edge label = weight (always shown); tokens shown live (edit=M0, simulate=marking).
5. `scripts/check-independence.mjs` (design §8) + `npm run check-independence`.
6. `npm run build` → static `dist/` verified (`tsc --noEmit` is wired into the build script).
7. Test report `6.testing/features/feature_034.studio_v1_editor/test_report.md` (paste the RED/GREEN evidence above + store-cycle evidence + build + independence outputs) → FEATURE.md/PLAN.md checkboxes → `omt_complete{feature:"feature_034.studio_v1_editor", advance_to:"Testing"}` → Done.

## Blockers

- None. (Engine green; deps installed; harness check 0 errors at pause. Budgets: work_md ~5015/5120 — tight, prefer WORK_ARCHIVE rotation on next growth; scratchpad 3060/3072 — full, rotate rather than grow.)

## Known caveats recorded (no action needed; already TA-pinned / docstringed)

- io.ts docstring: A2 integer-like-key reorder caveat (layout extension values); integral-float `version` acceptance diverges from Python strict-int (schema-correct; outside pinned matrix).
- FORMAT.md §8.5 UTF-16≡code-point claim is an **erratum candidate** (fails for astral-plane names; TS pins code-point comparator = spec primary rule). Spec LOCKED — surfaced to user, no edit without re-lock.

## Artifacts to read first on resume (in order)

1. `WORK.md` (task row `[~]` paused → this file)
2. `.projects/meta/petri_net_studio/CURRENT_STATE.md` (pause entry on top) + `PROJECT.md` (roadmap #3 status)
3. `.meta/software_development_process/4.design/features/feature_034.studio_v1_editor/design_001_studio_v1_editor.md` §7/§9.3/§10 + `operation_spec_001_studio_v1_editor_ops.md` (the UI contracts to implement)
4. `tools/petri-net-studio/src/engine/io.ts` exports (`buildNet`, `documentFromJson`, `netToJson`) + `src/engine/model.ts` — the finished engine the UI builds on
5. `.meta/software_development_process/2.requirements/features/feature_034.studio_v1_editor/FEATURE.md` (status row) — fill test-report row at the end

## Housekeeping done at pause (this session)

- io.ts restored from RED stub to the GREEN implementation and **verified: full suite 122/122 green** (tree left in a working state, no stub, no /tmp dependency — `/tmp/opencode/io.ts.full` + `model.ts.full` are redundant copies only).
- TA: 3 thoughts pinned (io.ts, model.ts, io.test.ts) — see §Completed #8.
- `WORK.md`: feature_034 row updated to `[~]` PAUSED → pointer to this file.
- FEATURE.md status line → paused-at-Programming (engine GREEN); CURRENT_STATE.md pause entry added on top.
- No `src/` (agentx) edits at any point in this feature; agentx suite untouched (1639 green at feature_033 completion; petri_net subset unaffected — engine lives in tools/).
