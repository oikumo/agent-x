# PAUSE 2026-08-30f — feature_041.resource_places_concurrency (GREEN complete + dogfooded rev 1; wrap-up bookkeeping NOT done)

> Resume point for `meta_harness_concurrent` core 3/3. Reversible/idempotent: re-running the pause on this state produces this same file.
> Read FIRST: this file → `.sandbox/pause_2026-08-30d.md` (design R1–R8) → `.projects/meta/meta_harness_concurrent/PROJECT.md` (D1–D18) → `CURRENT_STATE.md` → FEATURE.md (`2.requirements/features/feature_041.resource_places_concurrency/`).
> Supersedes `.sandbox/pause_2026-08-30e.md` (RED-complete state — RED is now GREEN).

## Active task

- **feature_041.resource_places_concurrency** — minor_feature, project `meta_harness_concurrent` (core 3/3, the LAST core feature).
- Ledger state at pause: phases **Analysis ✅ → Design ✅ → Programming active**.
- tests canary skip was issued THIS session (expires with it — **re-issue at resume only if `tests/` edits are needed**, phase FIRST then skip per GOTCHA_TESTS_CANARY_SHADOW). The remaining wrap-up (test_report.md, CURRENT_STATE.md, WORK.md, omt_complete) needs **no `tests/` edits** — only re-declare the phase if the enforcer asks.

## Completed so far (this session)

1. Resumed per pause_2026-08-30e.md: phase re-declared (Programming) + tests canary re-issued; TA consults recorded for the 4 TA-carrying GREEN targets (state.py, cli.py, project.py, new_feature.py).
2. **GREEN ✅ — R1–R8 implemented** (one bash transform round, 17 exact-match replacements, receipt round-robin honored — ONE e2e refresh after ALL edits):
   - `scripts/omt/net/state.py`: `RESOURCE_PLACES` catalog (5 places, all cap=1 per IDEA-002 v4 §2.2) · R1 bootstrap skeleton (+5 M0=1 places, live_marking + overlay) · R2 `_subnet_mutation` agent_attention claim/release arcs (9 arcs, appended last) · R3 `derive_overlay` `ports.resources = sorted((entry ∪ exit) ∩ RESOURCE_PLACES)` (P10 pure) · R5 `sync()` resync → ONE `add_resource_places` proposal entry (missing catalog places + agent_attention retrofit arcs per unwired subnet; `net_sync` record gains `add_resource_places` + `retrofit_arcs`) · R4 NEW `resource_report(st)` (resources[] capacity/live/capacity_ok/holders + conflicts[] pending-blocked subnets with `blocked_by` = empty unprefixed start inputs) · R6 NEW `lifecycle_sync_hook(event)` (fail-open, silent when unbootstrapped, proposal-only D4, one-line stdout).
   - `scripts/omt/net/cli.py`: `_invariant` envelope gains ADDITIVE `resources`/`conflicts` keys (after `drift`).
   - `scripts/omt/project.py`: `_net_auto_sync(event)` helper (lazy `from net import state`, try/except fail-open) wired after `_sync_all()` in cmd_new("create")/cmd_link("link")/cmd_close("close")/cmd_archive("archive")/cmd_reopen("reopen") — NOT cmd_sync/cmd_backfill.
   - `scripts/omt/new_feature.py`: `new_feature_link` hook after successful `--project` link (rc==0, AFTER the link ledger append) + **spec-driven fix**: created-paths print now survives a hermetic `FEATURES_DIR` outside the repo (`relative_to` ValueError → fallback to absolute path; pinned by `test_new_feature_link_triggers_hook` rc==0).
3. **Verification battery — ALL GREEN:**
   - `pytest tests/scripts/omt/test_net_resources.py tests/scripts/omt/test_net_sync.py` → **27/27** (the 21 RED + 6 untouched pins).
   - `pytest tests/scripts/omt tests/features/feature_041.resource_places_concurrency` → **344 passed** (041 sentinel bridge green).
   - e2e `test_omt_harness_e2e.py` → **1/1** (receipt refreshed ONCE, post-transform).
   - `harnessc build` + `check` → **0 errors** (253 records).
   - drift pins `test_omt_docs_drift_pins.py` → **12/12**.
   - **FULL SENTINEL: 1756 passed** (exactly the predicted baseline: 1736 + 18 new + 2 sentinel).
4. **Dogfood on the REAL SSOT ✅ (rev 0 → rev 1):**
   - `omt_net{op:sync}` → proposal = 41 subnet entries (UNAPPLIED, user decision) + **ONE `add_resource_places` entry** (5 catalog places, no retrofit — no subnets at rev 0) exactly as designed.
   - `omt_net{op:splice, mode:add}` applied ONLY the resource-catalog entry → **rev 1**, conformance 9/9, marking = 3 boundary + 5 resources (all 1 except goal_satisfied=0).
   - `invariant` **via CLI** (`uv run scripts/omt/net_check.py invariant`) → green: resources[] 5× `{capacity:1, live:1, capacity_ok:true, holders:[]}`, conflicts[] `[]`, drift `net=ledger=1`, `live_marking_invariants_hold: true`.

## NEW FINDING — omt_net.ts proxy bug (pre-existing, NOT feature_041 scope)

- `omt_net{op:invariant}` (and `op:probe`) **via the MCP plugin ALWAYS fails**: `.opencode/plugins/omt_net.ts:42` appends `--session <context.sessionID>` to EVERY op's argv, but the CLI `probe`/`invariant` subparsers declare no `--session` → argparse `unrecognized arguments` (exit 2 → `engine_error` envelope). Latent since feature_039 (dogfoods used the CLI or --session-accepting ops; the D7 omt_complete hook calls the CLI in-process, so nothing surfaced it).
- **The Python engine is correct** — this is a proxy arg-whitelist bug. TA gotcha recorded @ `.opencode/plugins/omt_net.ts:43`.
- **Decision needed (user):** scaffold a bug_fix (next slot feature_046: per-op arg whitelist in omt_net.ts + pin test) or defer. Do NOT fold into feature_041 (R8: no TS churn in scope).

## Immediate next step (resume sequence — wrap-up only, NO code)

1. `6.testing/features/feature_041.resource_places_concurrency/test_report.md` — write it (battery numbers above; 27-test canonical suite + sentinel bridge; dogfood rev 0→1 record; note the plugin-bug finding + CLI workaround).
2. `uv run scripts/omt/project.py log meta_harness_concurrent "<one-line session entry>"` (CURRENT_STATE.md; cmd_log fires NO net hook).
3. WORK.md row → `[x] **feature_041.resource_places_concurrency** — DONE 2026-08-30: ...` (DONE-style one-liner + pointer; watch the 7168 B budget — replace the PAUSED row, similar length).
4. `omt_complete{feature:"feature_041.resource_places_concurrency"}` → verify Testing artifacts → `advance_to:"Testing"` → then `advance_to:"Done"` (header flip → project stays active; D7 drift hook runs at the exit — expect green, rev 1 = ledger 1).
5. **Surface D17 to the USER (do NOT self-decide):** promote feature_045 (WORK.md net projection) into the core, or keep phase-2.
6. **Surface the plugin-bug decision (user):** scaffold feature_046 bug_fix (omt_net.ts per-op arg whitelist + pin test) or defer.
7. Subnet proposals for feature_001..041 stay UNAPPLIED in the real net (user decision — sync will keep re-proposing them; that is by design, D4).

## Blockers / open decisions

- None blocking the wrap-up. **D17** (feature_045 promotion) + **plugin-bug handling** (feature_046 or defer) are USER decisions at the exit review.
- Rule-3 note (pause workflow): the 27-test canonical suite + 041 sentinel bridge (all green) ARE the automated verification of the implementation; the plugin bug has NO test yet — its fix feature must add one (RED-first).

## Artifacts a future session reads first

| Path | Why |
|---|---|
| `.sandbox/pause_2026-08-30f.md` | this file — GREEN-complete + dogfood state, wrap-up sequence |
| `.sandbox/pause_2026-08-30d.md` | design R1–R8 (locked) |
| `.projects/meta/meta_harness_concurrent/PROJECT.md` | D1–D18; D17 open decision |
| `.projects/meta/meta_harness_concurrent/CURRENT_STATE.md` | session log |
| `tests/scripts/omt/test_net_resources.py` | the spec that pinned GREEN (TA xref :54) — all green |
| `scripts/omt/net/state.py` | GREEN delivered (TA xrefs :58, :283) |
| `.opencode/plugins/omt_net.ts` | plugin --session bug + TA gotcha :43 |
| `.meta/.omt/META_NET.petri.json` + sidecar + overlay | REAL SSOT now at **rev 1** (8 places, drift-free) |

## Pause bookkeeping

- Green-at-pause: full sentinel **1756 passed** · omt dirs 344 · drift pins 12/12 · harnessc build+check 0 err (253) · e2e 1/1 (receipt fresh — no harness-surface edits since).
- Real net: **rev 1** (resource catalog applied; 41 subnet proposals pending by design).
- Uncommitted at pause (by design — no commits requested): the 4 GREEN files (state.py, cli.py, project.py, new_feature.py), this pause file, TA gotcha in omt_net.ts, plus everything uncommitted from the RED session (test_net_resources.py + sentinel bridge new, test_net_sync.py evolved pins, WORK.md row, pause_2026-08-30e.md).
- WORK.md: **updated** — Tasks row pointer switched pause_2026-08-30e → **pause_2026-08-30f** (GREEN-complete + dogfood state; user confirmed this resumption doc before the update, per workflow).
- **Session is safe to close.**

## Result

Paused at the feature_041 exit review (GREEN + dogfood complete, wrap-up bookkeeping pending); resume by reading `.sandbox/pause_2026-08-30f.md` → write test_report.md → log → WORK.md DONE row → `omt_complete` Testing → Done → surface D17 + the plugin-bug decision to the user.
