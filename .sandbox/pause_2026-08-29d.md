# PAUSE — feature_036.studio_v3_graph (petri_net_studio roadmap #5)

> Date: 2026-08-29 (second pause, after resume) · Session: resumed from `pause_2026-08-29c.md` → **unblocked the Programming gate** (wrote `operation_spec_001` → `omt_phase` Programming accepted) → paused at **Programming-start (TDD cycle 0)** before any code change.
> Resume: read `WORK.md` `[~]` row → this file → PROJECT.md Quick Start → `analysis_001_graph_explorer.md` → `design_001_studio_v3_graph.md` → `operation_spec_001_studio_v3_graph.md` → begin **TDD Cycle 1** per design §11.

---

## Active task

- **feature_036.studio_v3_graph** — roadmap feature #5 (project v2 completion): reachability-graph explorer (auto-layout), firing-sequence animation, liveness/SCC views, conformance-suite runner wiring, example gallery. Type: major_feature. Linked project: petri_net_studio (active). WORK.md task row exists (`[~]`), pointer updated to this file.
- **Phase: Programming** — declared 2026-08-29 after the operation spec was accepted (`omt_phase` ledger). **TDD mode ACTIVE (TESTLIST), cycles: 0.** No `src/` (studio) code changed yet; no tests written yet.

## What was completed this session (resume from c → now)

1. **Resume chain read**: WORK.md → `pause_2026-08-29c.md` → `.projects/meta/petri_net_studio/PROJECT.md` → `analysis_001_graph_explorer.md` → `design_001_studio_v3_graph.md` → precedent `operation_spec_001_studio_v1_editor_ops.md` (feature_034 op-table format).
2. **Think-gate consulted** (TA records): `analysis.ts:31` (markingFromKey empty-marking gotcha) + `:481` (-0 canonicalization), `store.ts:168` (simulate-lock store-level), `App.tsx:294` (React Flow controlled through store), `check-independence.mjs:76` (`**/` inside JS block comment gotcha), PROJECT.md:156-157.
3. **Code verified** (signatures pinned for the op spec): `PetriNetAnalyzer` class (`analysis.ts:96`) with `reachableMarkings/reachabilityGraph/deadlocks/firingSequenceTo/transitionLiveness/isLive/stronglyConnectedComponents`; module fns `markingKey` (exported), `markingFromKey` (private, line 30), `compareMarkings`. Store `initialDataState`/`toggleAnalysis` pattern. `examples.ts` `EXAMPLE_TEXTS`/`EXAMPLE_NAMES` (3 canonical). App.tsx `analysisVisible && <AnalysisPanel />` toggle pattern. package.json scripts. Vector corpus: 9 files in `shared/petri-net/conformance/analysis-v1/`.
4. **WROTE `.meta/software_development_process/4.design/features/feature_036.studio_v3_graph/operation_spec_001_studio_v3_graph.md`** — op-table format mirroring feature_034, covering the NEW public operations:
   - `analysis.ts`: `markingFromKey(key) → number[]` export only (`"" → []`); consumed engine APIs listed read-only.
   - `src/ui/graphProjection.ts`: `projectGraph(graph, sccs, deadlocks, positions, placeOrder) → {nodes, edges}` (node id=markingKey, label `M0` vs `(m.join(","))`, sccIndex/deadlock/initial flags, edge id `src::t::dst`, dangling edges emitted, pure/deterministic).
   - `src/ui/animation.ts`: `markingAt(net, m0, seq, step) → number[]` (fold fireMarking, clamp) + `sequenceSteps(net, m0, seq) → number[][]`; null-seq = caller/UI case ("unreachable").
   - `store.ts`: `graphVisible: boolean` (default false) + `toggleGraph()` — NOT edit-locked, `setMode` untouched.
   - `examples.ts`: `GalleryEntry` + `GALLERY_ENTRIES` (8 entries; conformance-net `.net` extraction) + **`EXAMPLE_TEXTS` extension** (5 fixture-net ids so `loadExample(id)` succeeds for ALL 8 — key consistency pin for test 10.3); `EXAMPLE_NAMES` stays 3.
   - `scripts/conformance.mjs`: sequential side-effect contract (uv regenerate → porcelain-empty assert → vitest conformance; exit 0 only all pass).
   - UI ops: App Graph/Gallery toggles, GraphExplorer (derived memo, elkjs layout, view-only ReactFlow, truncation banner, component-local animation), Gallery cards → `loadExample`.
   - Global invariants (engine untouched, explorer read-only over M0, pure layer first, independence allowlist examples/+conformance/, code-point sorts).
5. **`omt_phase{task_type:"major_feature", phase:"Programming", tdd:true, design_doc:…design_001…}` ACCEPTED** — the §12 gate that blocked pause-c now passes (operation_spec present). Ledger: `[..] phase major_feature Programming feature_036.studio_v3_graph`. `omt_status` now shows Programming, TDD ACTIVE (TESTLIST), cycles: 0, valid next: Testing/Design/Analysis.

## Blocked — none. Immediate next step (TDD Cycle 1, design §11)

Programming is declared; resume = begin the **manual red→green TDD cycles under Vitest** (A11 precedent — `omt_tdd` is pytest-shaped; declare the mismatch in the test report; evidence pasted):

1. `omt_tdd{op:testlist, behaviors: <JSON array of behaviors>, feature:"feature_036.studio_v3_graph"}` — enumerate the 6 test-group behaviors (engine additive 10.5, projection 10.1, animation 10.2, store 10.4, gallery 10.3, plus conformance script).
2. **Cycle 1 (engine additive)**: RED — extend `tests/engine/analysis.test.ts` with `markingFromKey` round-trip + `markingFromKey("") === []` → `npx vitest run tests/engine/analysis.test.ts` RED → GREEN — add `export` to `markingFromKey` in `src/engine/analysis.ts` (ONE-line change; TA:31 gotcha stays in the fn).
3. **Cycle 2 (projection)**: `src/ui/graphProjection.ts` stub + `tests/ui/graphProjection.test.ts` (design §10.1) → RED → implement pure → GREEN.
4. **Cycle 3 (animation)**: `tests/ui/animation.test.ts` (§10.2) → RED → implement `markingAt`/`sequenceSteps` in `src/ui/animation.ts` → GREEN.
5. **Cycle 4 (store + gallery data)**: store `graphVisible`/`toggleGraph` + store test additions (§10.4) → RED → GREEN; `examples.ts` `GALLERY_ENTRIES` + `EXAMPLE_TEXTS` extension + `tests/ui/gallery.test.ts` (§10.3) → RED → GREEN.
6. **Cycle 5 (UI)**: `npm install elkjs` → `GraphExplorer.tsx` + `graphProjection` wiring + `App.tsx` Graph/Gallery toggles + `styles.css` §9 → `tsc` clean + `npm run build` green + `vite preview` smoke; independence allowlist edit (C7: `ALLOWED_OUTSIDE` regex `examples/|conformance/` + docblock) + `npm run check-independence` green.
7. **Cycle 6 (conformance runner)**: `scripts/conformance.mjs` + `package.json` script → run → byte-identical + suite green.
8. **Sentinel + suite**: `tests/features/feature_036.studio_v3_graph/test_studio_v3_graph_sentinel.py` (feature_035 precedent: executes `npx vitest run`, env-skip without node, structural floor always-on incl. new files; canary-approval skip logged) → `uv run pytest -q` full agentx suite green.
9. **Bookkeeping**: implementation notes, test report, FEATURE.md/PLAN.md checkboxes, WORK.md, project CURRENT_STATE/PROJECT iter-9, `omt_complete` Programming → Testing → Done.

## Files a future session must read first

1. `WORK.md` (this `[~]` row — pointer here).
2. This file (`pause_2026-08-29d.md`).
3. `pause_2026-08-29c.md` (pre-resume state — optional context; the c-doc's "Files a future session must read first" list still applies).
4. `.projects/meta/petri_net_studio/PROJECT.md` — Quick Start + iteration log (iter-8 = predecessor).
5. `.meta/software_development_process/3.analysis/features/feature_036.studio_v3_graph/analysis_001_graph_explorer.md` (C1–C10).
6. `.meta/software_development_process/4.design/features/feature_036.studio_v3_graph/design_001_studio_v3_graph.md` (pins §1–§12).
7. `.meta/software_development_process/4.design/features/feature_036.studio_v3_graph/operation_spec_001_studio_v3_graph.md` (**NEW — the gate artifact; contracts for all new ops**).
8. Precedent docs: `feature_034` `operation_spec_001_studio_v1_editor_ops.md` (op-table format) + `feature_035` sentinel `tests/features/feature_035.studio_v2_analysis/test_studio_v2_analysis_sentinel.py` (env-skip + structural floor pattern) + feature_035 design_001 (B11/A11 TDD protocol).
9. Code tree: `tools/petri-net-studio/src/engine/{analysis.ts,model.ts}`, `src/state/{store.ts,document.ts}`, `src/ui/{App.tsx,AnalysisPanel.tsx,flow.ts}`, `src/examples.ts`, `scripts/{check-independence.mjs,generate-vectors.py}`, `tests/{engine/conformance.test.ts,state/store.test.ts}`.

## Session-closed record (workflow step 6 — filled after user confirmation)

- ✅ TA thought added to `.projects/meta/petri_net_studio/PROJECT.md` (rule 2 — resume state recorded in the project home).
- ✅ WORK.md updated: `feature_036.studio_v3_graph` `[~]` row pointer → this file (paused at Programming-start, TDD cycle 0).
- ✅ Resume artifacts verified: FEATURE.md/PLAN.md stubs · analysis_001 · design_001 · **operation_spec_001 (NEW)** · this pause doc.
- ✅ User confirmed the resumption document is accurate (2026-08-29).
- Session is safe to close. Resume entry point: `WORK.md` `[~]` row → `pause_2026-08-29d.md` → begin TDD Cycle 1 (`omt_tdd{op:testlist}` → markingFromKey RED → GREEN).

## Result

paused at **Programming-start (TDD cycle 0)** — op spec written, gate unblocked; resume by reading `.sandbox/pause_2026-08-29d.md` → begin TDD Cycle 1 (markingFromKey RED→GREEN).