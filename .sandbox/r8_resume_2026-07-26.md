# R8 (OMT-HDL-1) — resume spec v4 (2026-07-26, session-5 pause)

Plan: `.sandbox/meta_harness_refactor_plan.md` Appendix D. Phase: refactor/Programming
(redeclare `omt_phase{task_type:"refactor", phase:"Programming", feature:"meta_harness_refactor"}`
if the 8h window lapsed; canary `omt_skip{scope:"tests"}` likewise).
**Supersedes v3** — v3 steps 1–6 + A + B + C are DONE and verified. Only §2.D remains.

## 0. Session-5 PAUSE (clean checkpoint — resume at §2.D)

DONE this session (**C**, tests + state.py):
- C.1 e2e HARNESS_FILES += `.meta/META_HARNESS.omt` (before guide entry) +
  `scripts/omt/harnessc.py` (scripts/omt block).
- C.2 NEW `tests/scripts/omt/test_harnessc.py` — 8 test fns covering the 7 spec
  behaviors: L161 parse regression (`@var x : 123` → payload "123", attrs {}),
  repo .omt check 0 errors, verify-projections green, ref closure catches
  `@doc.bogus`, pred vocab closed (@pred + @gate when= legs), unknown @budget
  id = error, splice_config idempotent round-trip + missing-end-marker →
  SystemExit. Imports via `sys.path.insert(scripts/omt)` + `import harnessc`.
- C.3 drift pins shrunk 9→8: #3 startup → build.md leg ONLY (AGENTS leg owned
  by verify-projections), #4 tool-set → registered-tools ↔ IR `tools` keys
  (dropped AGENTS/CMD_/config legs + `_md_section` helper + META_HARNESS const;
  added `json` import + IR const), #5 COMP_* DELETED (compiler
  `check_comp_paths` owns it). #1/#2/#6 unchanged.
- C.4 state.py: `_ir_var_int()` reads `vars.unlock_window_ms`/`ledger_cap_bytes`
  from the IR with the literal defs KEPT as fallback (thought-pattern pin
  regexes still match the literals first; IR values currently == literals).

Verified at pause: **static suite 104/104** (~65s; incl. e2e → receipt FRESH),
`harnessc check --verify-projections` **0 errors** (226 records; budgets:
work_md 10366/14336, scratchpad 5786/6144, tool_schemas 1484/2560,
agents_md in budget). Working tree DIRTY, deliberately UNCOMMITTED (no user
request): M .gitignore, .meta/META_HARNESS.omt, TS ×6 (omt_shared, phase_gate,
tdd_hats, omt_status/think/nav), AGENTS.md, WORK.md, opencode.jsonc,
scripts/omt/harnessc.py, scripts/omt/tdd/state.py, tests ×2 (e2e, drift pins);
?? tests/scripts/omt/test_harnessc.py, .meta/.omt/ (IR+nav.index git-visible
per step A — add on commit).

RESUME: **§2.D exactly** (below). Nothing half-spliced; jsonc blocks FILLED.

## 1. Cumulative verified state (sessions 1–5)

Steps 1–6 (v2): .omt v3 spec records, harnessc.py compiler (parse/check/
projections/budgets/splice), IR + nav.index + AGENTS.md + jsonc projections,
L161 parser fix, L129 see= fix + L134 reword.
A: .gitignore un-ignores IR + nav.index (harness.report stays ignored).
B: TS ×6 — omt_shared += irPath/loadIr/irToolDescription/navIndexPath/
loadNavIndex + isOmtHarness += `.meta/META_HARNESS.omt`; phase_gate/tdd_hats
descs wrapped in irToolDescription; omt_status/omt_think/omt_nav tools in
post-init create*Tools() factories; omt_nav answers from the compiled index
w/ full grep fallback + runGrep `-H` fix (R1 open finding FIXED).
C: see §0.

## 2. Remaining work — D. live-verify B6 + retirement (in order)

1. `uv run pytest tests/scripts/omt/test_omt_live_opencode_guards.py -q` → 17 green.
2. Probe 18 tools register (WORK.md scratchpad R6 recipe: `bun /tmp/probe.ts` +
   `await import("<abs>/.opencode/plugins/omt_<x>.ts")` + `await m.default()` from repo root;
   imports resolve from `.opencode/node_modules`). Assert union of the 4 plugins' tool maps
   == IR tools keys (18). Nav-from-index answers: call `omt_nav`/`omt_list_sections` execute
   directly in the probe and assert results carry `.meta/META_HARNESS.omt:` src lines.
3. AGENTS.md diff-review WITH THE USER: `git diff HEAD -- AGENTS.md` (hand version
   retrievable via git — tracked). Adoption = the generated file staying in the tree.
4. META_HARNESS.md → generated stub (unguarded: NOT in isOmtHarness, NOT protected).
   FIRST update e2e check 9 (canary): replace `assert "SECTION:THINK" in _read(".meta/META_HARNESS.md")`
   with a stub-aware assert (e.g. "GENERATED" marker + pointer to .omt), then e2e.
5. harness.report budget summary → WORK.md; mark R8 `[x]` in WORK.md with the outcome;
   delete this resume file's IN PROGRESS pointer in WORK.md.
6. `omt_complete{feature:"meta_harness_refactor", advance_to:"Testing"}` (or per phase state).

## 3. Gotchas carried forward (v3 §3 + session 5)

- **NEVER pause mid-splice**: e2e is RED whenever jsonc harnessc blocks are EMPTY.
  Currently FILLED — safe. Recovery: `git show HEAD:opencode.jsonc > opencode.jsonc` → e2e.
- **main() masks check errors** behind projection-stage SystemExit — run full `check`
  after ANY projection-stage change.
- Parse errors still silently dropped (main L569 throwaway list) — cycle-2 fix candidate
  (route into `c.errors`), NOT this cycle.
- REF_RE principled prefix-class fix (harnessc.py L71) deferred — separate receipt cycle.
- Receipt guard: ONE edit per guarded file per fresh receipt; parallel edits to DIFFERENT
  files OK; e2e ≈0.6s makes cycles cheap. tests/ edits need the canary skip ONCE per 8h.
- **NEW (session 5): ALL of `tests/scripts/omt/` is receipt-guarded** (isOmtHarness
  path-prefix via IR `harness_paths`), even files NOT in HARNESS_FILES — the
  drift-pins shrink took 3 receipt cycles (docstring → big block → imports; the
  imports edit was gate-blocked once because it landed after a sibling edit).
  Plan multi-hunk test refactors as ONE edit, or round-robin via the 0.6 s e2e.
- omt_think.ts + think_gate.ts are think-gated — `omt_think_list{path}` consult FIRST
  (think_gate.ts only if actually edited; currently no edit planned).
- Plugin loader contract: plugins keep function-only named exports + default factory —
  `create*Tools()` must NOT be named-exported from plugin files (define inside the factory
  or as a non-exported module function).
- `python3 -c` probing is DENY-gated — use `jq` on the IR or `uv run python`.
