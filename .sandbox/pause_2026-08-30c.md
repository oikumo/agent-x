# PAUSE 2026-08-30c — feature_040.net_composition_supervisor (meta_harness_concurrent core 2/3)

> Resumption document — written at pause time, idempotent (re-run produces the same artifacts).
> **One line:** implementation NOT started; scaffold + full consolidated design DONE; resume by declaring Programming phase and writing the RED tests.

---

## 1. Active task

- **Task:** `feature_040.net_composition_supervisor` — WORK.md row `[~]` (paused pointer there).
- **Project:** meta_harness_concurrent (core roadmap 2/3; PROJECT.md D1–D18; feature_039 engine DONE).
- **Process state:** phases Analysis ✅ → Design ✅ → **Programming (active)** · minor_feature · tdd_mode **false** → manual red→green per feature_039 precedent (cycles recorded in ledger).

## 2. Completed so far (this session)

1. Scaffold: `new_feature.py "net composition supervisor" --type minor_feature --project meta_harness_concurrent` → `2.requirements/features/feature_040.net_composition_supervisor/{FEATURE.md, plan/PLAN.md}` + project_link ledger record.
2. **FEATURE.md filled** — Summary / Scope / Task type / Deferred (synthesize→042, resources→041, lifecycle auto-sync→041+, WORK.md render→045) / artifacts table.
3. WORK.md task row added (`[~]`, 6116 bytes ≤ 6144 WORK_BUDGET pin).
4. **Analysis complete** (grounds: IDEA-002 v4, feature_039 shipped code) → the consolidated design in §4 lives ONLY here + 4 TA tags (see §6) — it was never written to a phase doc (minor_feature = declaration only).
5. 4 thought-tags embedded in the exact edit targets (§6); e2e receipt refreshed after them.

## 3. Immediate next step (do this first on resume)

1. `omt_phase{task_type: minor_feature, phase: Programming, feature: feature_040.net_composition_supervisor, scope: ...}` (session unlock) — then **`omt_skip{scope:"tests"}`** (tests/ canary; feature_039 precedent in ledger 2026-08-30T20:38 "GOTCHA_TESTS_CANARY_SHADOW": phase first, skip second).
2. RED: write `tests/scripts/omt/test_net_splice.py` + `tests/scripts/omt/test_net_sync.py`; update `test_net_cli.py::TestReservedOps` (splice/sync leave the reserved set; **synthesize stays reserved → message now feature_042**).
3. GREEN: implement §4 in `scripts/omt/net/state.py` (splice engine + derived overlay + sync scan) and `scripts/omt/net/cli.py` (dispatch/args).
4. TS/docs round: `.opencode/plugins/omt_net.ts` (args mode/mutation/subnet + description), `.opencode/lib/enforcer/phase_gate.ts` (drift hook, §4 P9), `.meta/META_HARNESS.omt` @tool omt_net description → `uv run scripts/omt/harnessc.py build` (regens AGENTS.md/IR; budgets tool_schemas 1536 / tool_args 2048 — bump if `harnessc check` complains) → e2e refresh.
5. Sentinel `tests/features/feature_040.net_composition_supervisor/test_feature_040_sentinel.py` (subprocess bridge — copy feature_039 pattern; module-local fixtures block re-export).
6. Test report `6.testing/features/feature_040.net_composition_supervisor/test_report.md` → `omt_complete{advance_to: Testing}` → full sentinel → `omt_complete{advance_to: Done}`; update PROJECT.md Status + CURRENT_STATE.md (auto-logged by omt_complete; expand).

## 4. Consolidated design (THE artifact — decisions made, not yet on disk elsewhere)

**splice CLI:** `--mode add|remove|disable|undo|repair` (req) · `--mutation '<json>'` · `--subnet <slug>` · `--reasoning` (req) · `--session`.

- **P1 add** — mutation `{add_places:[{name,tokens}], add_transitions:[{name}], add_arcs:[{source,target,weight}]}`; arc direction by node kinds (place→transition=`add_input`; transition→place=`add_output` by KEYWORD — §9 arg-order gotcha); validate-all-then-apply; `state.rebase_marking`; revision+1; atomic save; ledger `kind:"net_splice"` {mode, mutation, reasoning, revision, session, feature}; conformance (P8) pre-save.
- **P2 remove** — `{remove_places, remove_transitions, token_policy: forbid(default)|reroute|drain, reroute:{place:target}}`. Model is add-only (D2, no remove API) → **REBUILD**: new PetriNet via `add_*` copying survivors (places keep `initial_marking` M0; arcs filtered); live marking rebased by name + policy. forbid: refuse if any removed place holds live tokens. reroute: tokens → named surviving place. drain: bounded loop firing enabled transitions (transition_order, deterministic) consuming from removed places; no-progress → error.
- **P3 disable** ≡ remove-with-policy scoped by `--subnet` (all nodes with prefix `f{N}_`) + ledger `kind:"net_disable"` carrying FULL removed structure (places+M0, transitions, arcs, live tokens — sufficient for inverse replay) + `overlay.disabled` append (archive visibility §1.4/§11 #8).
- **P4 undo** — inverse-replay the LATEST structural ledger record (net_splice add → remove those nodes forbid; net_splice remove / net_disable → re-add recorded structure); ledger `kind:"net_splice"` {mode:undo, undoes:<revision>}; nothing-to-undo → clean error envelope.
- **P5 repair** — the `load()` refusal case (D12): validate every overlay-referenced node exists in net, then realign `overlay.revision = sidecar.revision`; missing nodes → error listing them; ledger `kind:"net_splice"` {mode:repair}.
- **P6 sync** — no extra args. Un-bootstrapped → materialize supervisor skeleton: boundary port places `feature_ready`(M0=1) `resource_token`(M0=1) `goal_satisfied`(M0=0); **NO supervisor transitions in v1** (§1.1's start_feature/complete_goal deferred — subnets wire to ports directly); ledger `kind:"net_sync"` {bootstrap:true}. Then scan reality: feature dirs `2.requirements/features/feature_NNN.*` → number+slug; WORK.md `## Tasks` checkboxes → subnet M0 (`[ ]`→pending, `[~]`→active, `[x]`→done); WORK.md `## Projects` table → project membership. Diff vs `overlay.subnets` → **proposal envelope** `{add_subnets:[mutation…], disable_subnets:[…]}` — NEVER auto-applied (D4/v4: surfaced for agent approval → splice; net never self-mutates reality).
- **P7 subnet template** (deterministic lifecycle chain, per feature): places `f{N}_pending`/`f{N}_active`/`f{N}_done` (M0 from checkbox); transitions `f{N}_start` (in: `f{N}_pending` + `feature_ready`; out: `f{N}_active` + `feature_ready`), `f{N}_complete` (in: `f{N}_active`; out: `f{N}_done` + `goal_satisfied`).
- **P8 conformance gate** — every structure-changing op (splice all modes, sync bootstrap) runs `conformance.run_vectors(shared/petri-net/conformance/analysis-v1)` pre-save; failure → no write + error envelope; success recorded `{conformance:{vectors:9, ok:true}}`. `fire` exempt (§5.0). Proposal-only sync = read-only, no run.
- **P9 omt_complete drift hook** (`phase_gate.ts`, near `syncWorkMdFromLedger`): spawn `uv run scripts/omt/net_check.py invariant` FAIL-OPEN (quiet/nothrow/timeout — tdd_check pattern); `ok && drift.drifted` → append ⚠️ line (invariant op itself logs to `harness.net.drift.jsonl`); `net_not_bootstrapped` → silent.
- **P10 derived overlay** — recomputed at every `state.save()`: subnets keyed `feature_{N}` parsed from `f{N}_` prefix; `supervisor.{places,transitions}` = unprefixed nodes; per-subnet `ports` {entry: unprefixed places subnet transitions INPUT from; exit: unprefixed places they OUTPUT to; resources: capacity subset — feature_041 refines}; `disabled` list preserved across derivation. Overlay↔net drift impossible by construction; `load()` revision check stays as tamper guard (P5 repair path).

## 5. Blockers

None. (tests/ canary not yet approved THIS session — procedural, see §3 step 1.)

## 6. Read first on resume (ordered)

1. THIS file.
2. `.projects/meta/meta_harness_concurrent/PROJECT.md` (D1–D18) + `CURRENT_STATE.md` (pause entry on top).
3. `2.requirements/features/feature_040.net_composition_supervisor/FEATURE.md` (scope/deferred).
4. `.projects/meta/meta_harness_concurrent/ideas/idea-002-compositional-net-of-nets-architecture.md` v4 — §1.4 overlay, §3 splice/token policies, §5.0 op matrix, §5.1 bootstrap, §11.
5. **TA tags (omt_think{op:list}):** `scripts/omt/net/state.py:185` (rebuild+derived-overlay), `scripts/omt/net/cli.py:33` (splice/sync args), `.opencode/plugins/omt_net.ts:15` (TS args), `.opencode/lib/enforcer/phase_gate.ts:420` (drift hook).
6. Code: `scripts/omt/net/{state,cli,conformance,model}.py`, `tests/scripts/omt/test_net_{cli,state}.py` (spec conventions), `tests/features/feature_039.adaptive_net_engine/test_feature_039_sentinel.py` (sentinel pattern).

## 7. Process landmines for the resume session

- **Receipt round-robin:** `scripts/omt/net/{state,cli}.py`, `omt_net.ts`, `phase_gate.ts`, `.meta/META_HARNESS.omt` are harness-surface (e2e HARNESS_FILES) — ONE edit per file per e2e receipt; refresh = `uv run pytest tests/scripts/omt/test_omt_harness_e2e.py -q` (auto-writes receipt on pass). Receipt was refreshed at pause → resume starts clean.
- **TDD node-granularity** gotcha does NOT apply (tdd_mode false — manual cycles like feature_039).
- **WORK_BUDGET 6144** (drift pin) — WORK.md at 6116; any growth needs a trim or a pin bump (`.omt @budget work_md` + `test_omt_docs_drift_pins.py` — same-round files).
- **No `src/` edits** (D1); engine never imports `src/` (D2); `model.py`/`analysis.py`/`io.py`/`errors.py` are parity clones — DO NOT extend (F6); feature_040 logic goes in `state.py`/`cli.py` only.
- Rule-3 pause note: no new pause-verification test was created — there is NO implementation to verify yet; the pre-implementation state is already pinned by `test_net_cli.py::TestReservedOps` (splice/sync → not_implemented) + drift pins + e2e, all green at pause. Rule-4 note: no sub-agents used — analysis was already complete in-session; nothing parallelizable remained.

## 8. Pause verification (run at pause time)

- [x] 4 TA tags embedded (state.py:185, cli.py:33, omt_net.ts:15, phase_gate.ts:420)
- [x] e2e receipt refreshed + net suite green — e2e 1/1 (receipt re-pinned post-TA); net suite + drift pins 49/49; WORK.md 6098 ≤ 6144
- [x] WORK.md `[~]` row → paused pointer
- [x] CURRENT_STATE.md pause entry (newest on top)
- [x] User confirmed accuracy (2026-08-30)

---

## 9. Close-out record (filled after execution)

WORK.md updated (paused pointer row, 6098 bytes), CURRENT_STATE.md pause entry written, e2e receipt refreshed, all verification green at pause. **Session is safe to close.**

**Result:** paused at Programming-phase start (design locked, no impl); resume by reading `.sandbox/pause_2026-08-30c.md` → §3 next steps (phase + tests-canary → RED tests → GREEN state.py/cli.py).
