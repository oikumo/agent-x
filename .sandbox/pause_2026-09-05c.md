# PAUSE 2026-09-05c — feature_050.net_as_gate wrap-up (Phase A): diagnosis DONE, fixes NOT started

> Reversible + idempotent: re-running this pause on the same session state rewrites this same
> file, never duplicates. Follows `pause_2026-09-05.md` (048, closed) + `pause_2026-09-05b.md`
> (049, closed). Workflow (`.workflows/meta_harness/pause_dev_for_resume_later.md`) says
> `./sandbox/` — repo convention is `.sandbox/` with same-day letter suffix; THIS file is
> canonical, pointed from WORK.md + net_enforced_harness CURRENT_STATE.md.
> Session NOT yet marked paused in WORK.md (step 5 pending user confirm).

## Active task

**feature_050.net_as_gate** (major_feature, project **net_enforced_harness**) — WRAP-UP ONLY.
User decision this session: **Option 1** = stabilize 050 first, THEN
`feature_051.multi_session_concurrency` (meta_harness_concurrent). Later: *"just make the
phase a"* → **Phase B EXPLICITLY DEFERRED — do NOT start it on resume unless the user re-asks.**

Session state at pause: **read-only diagnosis complete, ZERO code changes made this session.**
All fixes below are specced to the line; resume = apply them.

## Net + repo state at pause

- Net **rev 53**, 12/15 places (D20): pending=2 active=0 done=5, enabled=[`work_start`];
  the 2 pending = feature_001/002 (agentx, **D1 out-of-scope**). agent_attention=1 (free).
- WORK.md Tasks re-rendered to rev 53 **at pause** (`sync --direction net_to_md`) — the rev-52
  stale render (active=1 done=4, NEXT work_complete) that broke harnessc is gone; NEXT is now
  correctly `work_start`. Note: that sync's reality scan reported reality_pending=0 (earlier 2)
  — scan-vs-render ordering, benign; net marking unchanged.
- feature_050 implementation is **uncommitted** on top of `8860ad1`: 12 files +334/−179
  (gate_driver.ts, omt_net.ts, cli.py, net_check.py, harnessc.py, META_HARNESS.omt, AGENTS.md,
  opencode.jsonc, .projects/meta/META.md, WORK.md, harness.ir.json, nav.index.jsonl) + new
  `scripts/omt/net/gate.py`, `tests/scripts/omt/test_net_gate.py`, feature_050 phase dirs
  (2.requirements/3.analysis/4.design — 5.implementation/6.testing MISSING), `.projects/meta/net_enforced_harness/`.
- **g.net:35 is LIVE** (compiled in harness.ir.json; AGENTS.md NEVER line; harnessc GATE_NEVER).
- **e2e receipt fresh**: `omt_harness_e2e_last_run.json` passed_at 2026-09-05T23:17:18Z ≥ all
  harness file mtimes → resume gets **ONE edit per file**, then must re-run the e2e.
- **Time windows**: 3 break-glass `skip scope=all` (22:38–22:45Z) until ~06:45Z; work_start fire
  receipt (22:34Z) until ~06:34Z. After both expire, harness edits need a fresh
  `fire work_start` (enabled, pending=2) or break-glass first.
- Unrelated bundled changes from the prior session (leave as-is, note in test report):
  `opencode.jsonc` +mkdir/rmdir allow; `@var root_allowlist` +`sandbox`; untracked `sandbox/`.

## Test failures — 5, all root-caused, none fixed

| # | Failure | Root cause | Fix |
|---|---------|-----------|-----|
| 1 | `test_omt_net_plugin_args.py::TestWhitelistMirrorsCli` | cli.py `invariant` subparser lacks `--expected-revision` (omt_net.ts OP_ARGS has it) | add the flag (mirror probe/fire/splice/sync/synthesize/mine/gate) |
| 2 | `test_harnessc.py::test_repo_omt_check_has_zero_errors` + `test_repo_projections_are_fresh` | WORK.md Pool line drift (stale rev-52 render vs net rev53) | **FIXED AT PAUSE** (sync net_to_md) — re-verify harnessc |
| 3 | same harnessc run, second error | omt_net.ts seed 358B vs .omt payload 359B: seed `gate(path,session).` vs payload `gate(path,session?).` | add the `?` in omt_net.ts seed |
| 4 | `test_net_history.py::TestLiveGolden::test_replay_matches_live_bundle` + `feature_043` dashboard sentinel `test_live_replay_matches_bundle` | history.py replay raises `invalid_replay: unknown ledger record: net_mine` (handles fire/splice/disable/sync/synthesize only; net_mine added by feature_044) | add `net_mine` to the no-mutation skip branch (~line 317–319, alongside net_synthesize) — mine is draft-only (D4), no live-bundle mutation |
| 5 | (latent, no test yet) | `.omt` `@gate g.net ... skip_ok=true` contradicts TS fallback `skip_ok:false` + design_001 (skip_ok=false) — IR is functional source → ANY session skip bypasses g.net | set `.omt` skip_ok=false (break-glass scope=all only — net_enforced_harness D3) + harnessc rebuild |

## feature_050 code defects to fix in wrap-up (uncommitted code)

1. **gate.py DEBUG block** (lines ~101–108): writes `.meta/.omt/gate_debug.log` on EVERY check
   → remove block + `rm .meta/.omt/gate_debug.log` (exists, 3.8 KB).
2. **gate_driver.ts DEBUG block** (`// DEBUG` + `require("node:fs")` → `/tmp/gate_debug.log`)
   → remove.
3. **gate.py `_has_recent_fire_receipt`**: accepts ANY `net_fire` (any transition incl.
   work_complete, any session) → filter to `_start`-suffix transitions (AGENTS.md NEVER:
   "fire(work_start) required"). Keep 8h window + any-session (identity = Phase B).
4. **cli.py `gate` op**: `net_available=True` hardcoded; `drifted/expected_revision/live_revision`
   never passed → ERR_NET_DRIFT_CONFLICT / ERR_NET_STALE_REV / ERR_NET_DOWN unreachable in the
   LIVE enforcer path. Fix: load state; `drifted = st.revision != read_ledger_net_records()[-1].revision`
   (same as `_invariant`); `NetNotBootstrappedError`/Exception → `net_available=False` (fail-closed, D3);
   pass `expected_revision=args.expected_revision`.
5. **Tests to add** (test_net_gate.py; canary `omt_skip{scope:"tests"}` first): receipt filter
   (ledger with only work_complete fire → ERR_NET_NOT_ENABLED; work_start fire → OK) + CLI gate op
   (unbootstrapped → ERR_NET_DOWN; bootstrapped + work_start receipt → OK; ledger revision
   mismatch → ERR_NET_DRIFT_CONFLICT). CLI pattern: in-process `cli.main(argv)` +
   OMT_NET_DIR/OMT_LEDGER_PATH env (see test_net_cli.py `bundle` fixture).

## Known limitations — documented, NOT fixed (Phase B scope, user-deferred)

- Receipt is any-session (no session→work binding; D20 "map in overlay" unimplemented —
  overlay.subnets={}).
- No flock: concurrent sessions race the 3-file bundle read-modify-write (tonight 4 sessions
  interleaved safely by luck).
- WIP=1: agent_attention held start→complete (invariant attention+active=1) — multi-session
  concurrent work structurally impossible.
- `@var net_paths` (src/,tests/,.opencode/,scripts/omt/) excludes META_HARNESS.omt / AGENTS.md /
  opencode.jsonc (compile-time spec edits gated only by g.receipt).
- WORK.md render not refreshed after fire (staleness class that hit tonight rev52→53).

## Resume runbook (exact order)

1. Read: THIS file → `.projects/meta/net_enforced_harness/PROJECT.md` →
   `4.design/features/feature_050.net_as_gate/{design_001_net_gate.md, operation_spec_001_net_gate.md}`
   → `git status` / `git diff` (the uncommitted 050 tree).
2. `omt_status` → `omt_phase{Programming, feature_050.net_as_gate}` (scope: "wrap-up: fix 050
   defects + suite green + test report + Done"). TDD FSM recorded done@22:09 — wrap-up fixes run
   as manual red→green or extend the cycle (declare first).
3. Edit round 1 — ONE edit per file (receipt fresh through 23:17Z vs current mtimes):
   cli.py (invariant flag + gate rewire = 2 sites → `uv run python` transform per
   GOTCHA receipt-round-robin, or two rounds with e2e between) · gate.py (debug removal +
   receipt filter = 2 sites, same) · history.py (net_mine skip — edit tool) ·
   gate_driver.ts (debug block — edit tool) · omt_net.ts (seed `?` — edit tool) ·
   `.meta/META_HARNESS.omt` (skip_ok=false — edit tool).
4. `rm .meta/.omt/gate_debug.log`.
5. `uv run pytest tests/scripts/omt/test_omt_harness_e2e.py -q` (receipt refresh).
6. `uv run scripts/omt/harnessc.py build && uv run scripts/omt/harnessc.py check` → 0 errors.
7. Canary skip → extend test_net_gate.py → `uv run pytest tests/scripts/omt/ -q`
   (incl. test_net_gate, plugin_args, net_history, harnessc, net suites) +
   `tests/features/feature_043.meta_net_dashboard/`.
8. Live gate verification (env-controlled): empty OMT_LEDGER_PATH → ERR_NET_NOT_ENABLED;
   work_start-only ledger → OK; real repo → allowed (break-glass/receipt may mask).
9. Full suite `uv run pytest -q` → green (the 4 baseline failures must be gone).
10. Test report `6.testing/features/feature_050.net_as_gate/test_report.md` + FEATURE.md
    artifact rows + status → net_enforced_harness PROJECT.md Status + CURRENT_STATE.md entry.
11. `omt_complete{feature_050.net_as_gate, advance_to: Done}` (D7 drift hook runs at exit).
12. Commit-ready: git commit is agent-DENIED — present the diff summary, user commits.

## Blockers

None. (Only the time windows above; after they expire, `fire work_start` first.)

## Artifacts a future session reads FIRST

1. THIS file.
2. `.projects/meta/net_enforced_harness/PROJECT.md` (D1–D3 locked: Alt A, D16 split, fail-closed+break-glass).
3. `.meta/software_development_process/4.design/features/feature_050.net_as_gate/design_001_net_gate.md` + `operation_spec_001_net_gate.md`.
4. `git diff` (uncommitted 050 implementation) + `tests/scripts/omt/test_net_gate.py`.
5. `.meta/.omt/omt_harness_e2e_last_run.json` (receipt freshness) + TA: thoughts in
   `scripts/omt/net/{gate,cli,history}.py`, `.opencode/plugins/omt_net.ts`,
   `.opencode/lib/enforcer/gate_driver.ts` (added at this pause).

## Result

**PAUSED & FINALIZED 2026-09-05 (~23:5xZ)** — Phase-A diagnosis complete (5 root causes specced,
WORK.md render fixed to rev 53, 0 code changes), user confirmed the resumption doc. WORK.md
carries the `[~]` paused row pointing here; net_enforced_harness/CURRENT_STATE.md has the
session entry. **Session is safe to close.** Resume by reading THIS file → runbook §Resume
starts at `omt_phase{Programming, feature_050.net_as_gate}`.

