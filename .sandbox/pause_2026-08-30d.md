# PAUSE 2026-08-30d — feature_041.resource_places_concurrency (scaffolded + design locked, impl NOT started)

> Resume point for `meta_harness_concurrent` core 3/3. Reversible/idempotent: re-running the pause on this state produces this same file.
> Read FIRST: this file → `.projects/meta/meta_harness_concurrent/PROJECT.md` (D1–D18) → `CURRENT_STATE.md` → FEATURE.md (`2.requirements/features/feature_041.resource_places_concurrency/`).

## Active task

- **feature_041.resource_places_concurrency** — minor_feature, project `meta_harness_concurrent` (core 3/3, the last core feature).
- Ledger state at pause: phases **Analysis ✅ → Design ✅ → Programming active**; tests canary skip issued THIS session (expires with it — re-issue at resume, phase FIRST then skip per GOTCHA_TESTS_CANARY_SHADOW).
- **No implementation code, no tests written yet.** GREEN/RED both pending.

## Completed so far (this session)

1. Scaffold: `new_feature.py "resource places concurrency" --type minor_feature --project meta_harness_concurrent` → FEATURE.md filled (summary/scope/deferred: synthesize→042, WORK.md projection→045) + project link (WORK.md + `.projects/meta/META.md` Projects rows auto-synced).
2. **Analysis ✅** — key findings:
   - `state.py:988-1011` `sync()` bootstrap = 3 boundary ports only (`feature_ready=1`/`resource_token=1`/`goal_satisfied=0`, NO supervisor transitions, NO resource places).
   - `state.py:137-185` `derive_overlay` seeds `ports.resources: []` with an explicit "feature_041 refines the subset" comment.
   - `state.py:963-985` `_subnet_mutation` lifecycle chain wires `feature_ready`/`goal_satisfied` only.
   - `cli.py:125-160` `_invariant` already computes `place_invariants()` + live-hold check → additive reporting lands there (D7: runs at every `omt_complete` exit).
   - Real SSOT at **rev 0** (3 supervisor places, 0 subnets — feature_040's sync proposal never applied, D4).
   - IDEA-002 v4 §2.2 locked catalog: **all 5 capacities = 1** (IDEA-005's example file shows 2/3 — sketch, NOT canonical).
3. **Design ✅ — R1–R8 locked** (also embedded as TA xrefs in the 4 source files):
   - **R1** bootstrap skeleton gains 5 capacity places: `agent_attention`/`src_edit_capacity`/`tests_capacity`/`harness_surface_round`/`e2e_receipt`, all M0=1.
   - **R2** `_subnet_mutation` wires `agent_attention`: `f{N}_start` claims (input), `f{N}_complete` releases (output) → serial-mirror conflict trap (IDEA-002 §2.3); arcs appended after the `feature_ready` arcs, deterministic order.
   - **R3** `derive_overlay` refines `ports.resources = sorted((entry ∪ exit) ∩ RESOURCE_PLACES)` — stays a pure function of the net (P10 intact).
   - **R4** `state.resource_report(st)` helper + `_invariant` envelope gains ADDITIVE `resources[]` (capacity/live/capacity_ok/holders — holders for `agent_attention` = subnets with `f{N}_active` marked) + `conflicts[]` (pending subnets whose `f{N}_start` is not enabled, `blocked_by` = empty unprefixed input places). No new ops; `RESERVED_OPS` stays `("synthesize",)`.
   - **R5** resync of pre-041 bundles emits ONE `add_resource_places` proposal entry (missing places + retrofit arcs for existing subnets lacking the `agent_attention` wiring) — deterministic, never auto-applied (D4), applied via `splice{mode:add}`.
   - **R6** lifecycle auto-sync hooks: `net.state.lifecycle_sync_hook(event)` called from `project.py` cmd_new/link/close/archive/reopen + `new_feature.py --project` link (AFTER the link ledger append) — lazy import, try/except fail-open, skip silently when unbootstrapped, proposal-only, one-line stdout.
   - **R7** tests: new `tests/scripts/omt/test_net_resources.py` + sentinel bridge `tests/features/feature_041.resource_places_concurrency/test_feature_041_sentinel.py` (subprocess bridge like 039/040).
   - **R8** no `.omt`/TS/budget churn (op enum unchanged → drift pins untouched); `scripts/omt` edits trip e2e HARNESS_FILES → ONE e2e refresh round at the end.
4. **Blast radius pinned** (feature_040 tests to UPDATE in GREEN, legitimate behavior evolution):
   - `test_net_sync.py:101,236` bootstrap place-set grows by 5; live_marking + overlay supervisor places too.
   - `test_net_sync.py:160-176` subnet-template arc set gains `("agent_attention","f001_start")` + `("f001_complete","agent_attention")`.
   - `test_net_sync.py:250-251` ports entry/exit gain `agent_attention`; `resources == ["agent_attention"]`.
   - `test_net_splice.py:184,400` `"resources": []` pins SURVIVE (p2 is not a resource place).
   - `test_net_cli.py:113` invariant envelope — additive keys, verify no exact-shape assert.
5. TA xrefs added: `state.py:58`, `cli.py:36`, `project.py:69`, `new_feature.py:70` (think-gate consults recorded for all four).
6. Green-at-pause verified: e2e 1/1 (receipt refreshed — clean for resume), net suite 68/68, harnessc check 0 err (253 records).

## Immediate next step (resume sequence)

1. `omt_phase{task_type:"minor_feature", phase:"Programming", feature:"feature_041.resource_places_concurrency", scope:"implement R1-R8 per pause_2026-08-30d.md"}` → THEN `omt_skip{scope:"tests"}` (order mandatory — GOTCHA_TESTS_CANARY_SHADOW).
2. RED: `tests/scripts/omt/test_net_resources.py` (bootstrap catalog places+M0; resync proposal on a hand-built 3-place legacy bundle — proposal-only, state untouched; apply → ports.resources refined; 2-feature conflict: f1 start fires → agent_attention=0 → f2 start NOT enabled, invariant envelope reports holder+capacity_ok+conflicts; f1 complete releases → f2 enabled; place_invariants conservation law `agent_attention + Σ f{N}_active = 1`; multi-resource fire conserves each pair §2.3.2; hooks: lifecycle op triggers sync proposal spy / fail-open on net error / skip when unbootstrapped) + sentinel bridge + the 3 test_net_sync.py pin updates.
3. GREEN: `state.py` (RESOURCE_PLACES, bootstrap, resync proposal, `_subnet_mutation`, `derive_overlay`, `resource_report`, `lifecycle_sync_hook`) + `cli.py` (`_invariant` envelope) + `project.py`/`new_feature.py` hooks.
4. Round: e2e refresh → harnessc build+check → drift pins 12/12 → full sentinel (baseline 1736 + new).
5. Dogfood: `omt_net{op:sync}` on the REAL bundle → apply `add_resource_places` via splice → rev 1; invariant green. (Subnet proposals for feature_001/002/041 stay UNAPPLIED — user decision.)
6. `6.testing/features/feature_041.../test_report.md` → CURRENT_STATE.md log → WORK.md row → `omt_complete` Testing → Done.

## Blockers / open decisions

- None blocking. **D17 (USER decision at the feature_041 exit review):** promote feature_045 (WORK.md net projection) into the core, or keep phase-2. Surface it at completion — do NOT self-decide.
- Rule-3 note (pause workflow): no implementation exists yet, so no verification tests could be created at pause; the RED suite is the resume's first move. Repo verified green-at-pause instead (e2e 1/1, net 68/68, harnessc 0 err).

## Artifacts a future session reads first

| Path | Why |
|---|---|
| `.sandbox/pause_2026-08-30d.md` | this file — design R1–R8 + resume sequence |
| `.projects/meta/meta_harness_concurrent/PROJECT.md` | D1–D18 locked decisions, scope guardrails (D1 no src/, D2 no library import, D4 proposals never auto-applied) |
| `.projects/meta/meta_harness_concurrent/CURRENT_STATE.md` | session log (pause entry on top) |
| `.../feature_041.resource_places_concurrency/FEATURE.md` | scope + deferred list |
| `scripts/omt/net/state.py` | sync/derive_overlay/_subnet_mutation/splice — TA xref at :58 |
| `scripts/omt/net/cli.py` | `_invariant` — TA xref at :36 |
| `scripts/omt/project.py` / `new_feature.py` | hook sites — TA xrefs at :69/:70 |
| `ideas/idea-002-...md` §2 | resource catalog + serial-mirror semantics (canonical capacities) |
| `tests/scripts/omt/test_net_sync.py` | the 3 pins to update |

## Pause bookkeeping

- WORK.md: updated — Tasks row added: `[~] **feature_041.resource_places_concurrency** — PAUSED` with pointer here.
- The paused row pushed `work_md` over budget (6595 B > 6144 B) → deliberate growth **6144 → 7168** (two +512 steps at once; DONE-row replacement needs the headroom), pinned in the same round: `.meta/META_HARNESS.omt` `@budget work_md` + `test_omt_docs_drift_pins.py` `WORK_BUDGET` (feature_039 convention). harnessc build+check 0 err (253 records), budgets OK (6595/7168).
- e2e receipt refreshed (post-TA + post-budget); drift pins green; net suite 68/68. **Session is safe to close.**
- Uncommitted at pause (by design — no commits requested): feature_041 scaffold dir, FEATURE.md, WORK.md, `.projects/meta/META.md`, CURRENT_STATE.md, this pause file, 4 TA xref comments, budget bump (.omt + pin).
