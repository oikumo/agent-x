"""RAG v2 model layer — retrieve-offload-delegate (feature_027).

Console-only sibling of v1 (``agentx.model.rag``). v1 stays untouched for the
TUI path; v2 grounds retrieval as a ``@tool`` on the deepagents stack
feature_025 ships. See ``4.design/features/feature_027.rag_v2/design_001_*.md``.
"""
