# Feature 047: Wip Limited Pool (TOMBSTONE — mis-scaffold, superseded by 048)

> **Status:** [x] Superseded — see `feature_048.wip_limited_pool`
> **Created:** 2026-09-05 (auto-scaffold collision with planned `047.session_start_menu`)
> **WORK.md task:** none (tombstone only — harnessc link resolution)

This directory exists solely so the `project_link` ledger record
(`feature_047.wip_limited_pool → meta_harness_concurrent`, written by the
2026-09-05 auto-scaffold before the D20 number was locked) resolves under
`harnessc check` (append-only ledger has no unlink). All real work ships as
**feature_048.wip_limited_pool** (D20 15-place cap, generic WIP pool).
The planned `047.session_start_menu` (D19) keeps number 047 in PROJECT.md
roadmap; its scaffold will take the next free number when started — the
number collision is documented here, not re-litigated.

No phase artifacts; no implementation; no tests. Do not use.
